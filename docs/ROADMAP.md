# Production roadmap

Coordination notes, delivery order, and planning context formerly embedded in `TODO.md`.
Executable open work stays in [`TODO.md`](../TODO.md).
Aged coordination history lives in [`ROADMAP_COORDINATION_ARCHIVE_2026-08-13.md`](./ROADMAP_COORDINATION_ARCHIVE_2026-08-13.md).


<!-- P0-154 implemented in current session -->
Coordination note (2026-08-18 P0-154): the performance report now runs an isolated Tier-2 `MultiMesh` battle benchmark at the authored 200-character target and hard cap, recording p95 frame time, draw calls, static/delta memory, density, capacity, and budget checks under `crowd_character_peak`. The wrapper rejects an authored/requested count above the cap and fails when crowd frame, draw-call, or memory budgets are exceeded. Verified in a clean imported worktree: `--filter=test_performance_benchmark` (5/5), standalone quick crowd probe (200/200), and `tools/run_performance_report.sh build/benchmarks/performance-smoke.json --quick` (`within_budget=true`, p95 7.587 ms, 6.377 MiB delta on the development baseline). Release minimum-hardware evidence still requires running the same command with `TARGET_HARDWARE=tools/benchmarks/minimum-hardware.json BENCHMARK_HEADLESS=0` on that declared machine; P0-153 remains the upstream asset-variation dependency, not a blocker for the deterministic benchmark contract.
<!-- R-575 / P0-154-N01 measured on 2026-08-18 -->
Coordination note (2026-08-18 R-575 / P0-154-N01): the declared Intel UHD 620 profile was run with a real non-headless renderer, but the detected host was an Apple M5 Pro MacBook Pro rather than the target machine, so minimum-hardware acceptance remains **BLOCKED**. The isolated `crowd_character_peak` profile still passed on the development host at 200/200 characters: p95 4.922 ms / 16.67 ms budget, 1 / 3 draw calls, 3.674 MiB / 96 MiB delta memory, `within_budget=true`. Raw output remains locally at `build/benchmarks/performance-minimum-hardware.json`; tracked evidence and the host boundary are in `docs/reports/p0_154_n01_minimum_hardware_crowd_evidence_2026-08-18.md`. Re-run on an actual Intel UHD 620-class Linux/Windows host before closing R-575.
<!-- R-454 decomposed in current session -->
Coordination note (2026-08-13 R-454): high-complexity parent **R-454** (historical elevation across nine urban exterior RRMaps) decomposed into atomic per-map rows **R-515**..**R-521** (complexity 1) plus verification chain **R-522** -> **R-523** -> **R-524**. Harbour pair remains complete under **R-478**; matrix contract remains **R-474**. Superseded broad rows **R-475**/**R-476**/**R-479**/**R-481**/**R-506** and legacy gate stubs **R-482**..**R-504** cancelled on the board. Evidence: `docs/reports/r454_task_decomposition_2026-08-13.md`. Next map claims: **R-515** Toompea southern slope, then parallel **R-517**..**R-521**.

<!-- P0-181 completed in current session -->
Coordination note (2026-08-28 P0-181): research reference plate retention landed in `docs/data/reference_plate_retention.json`, `docs/ASSET_STORAGE_POLICY.md`, `tools/optimize_reference_plates.py`, and retention caps inside `tools/research/fetch_reference_plates.py --verify`. Optimized 50 plates (273904275 -> 39090195 bytes in the touched set, -234814080); zero `history/reference/**` rasters remain over 8 MiB. Verified: `python3 tools/research/fetch_reference_plates.py --verify`, `python3 -m unittest tests.python.test_fetch_reference_plates -v`. Next size claim: **P0-180** music takes or **P0-182** runtime audio budget.

<!-- P0-185 hanging banner cloth shader relocated in current session -->
Coordination note (2026-08-29 P0-185): `map_view_hanging_banner_cloth.gdshader` now owns the indoor wall-banner cloth shader; consumers use `MapViewMaterialShaders.shader_resource()` with the shared preload constant. `map_view_material_shaders.gd` dropped from 823 to 783 lines. Verified: `--filter=test_faction_heraldry` (12/12), `--filter=test_map_view_material_resolution` (7/7), `--filter=test_map_view_decals` (8/8). Next P0-185 shader claim: relocate `fishing_net_wind`, then tree meshes / prop models / runtime ambient peel.

<!-- P0-185 puddle and cloth shaders relocated in current session -->
Coordination note (2026-08-28 P0-185): `map_view_puddle.gdshader` and `map_view_cloth.gdshader` now own the rainwater decal and outdoor sail/pennant cloth shaders; consumers use `MapViewMaterialShaders.shader_resource()` with the shared preload constants. `map_view_material_shaders.gd` dropped from 980 to 823 lines. Verified: `--filter=test_map_view_decals` (8/8), `--filter=test_map_view_material_resolution` (7/7), `--filter=test_boat_float_3d` (6/6), `--filter=test_faction_heraldry` (12/12), `--filter=test_merchant_boat_model` (1/1), plus `test_puddle_material_preserves_ground_and_uses_shallow_water_optics` in `test_map_view_3d_core`. Next P0-185 shader claim: relocate `hanging_banner_cloth` or `fishing_net_wind`, then tree meshes / prop models / runtime ambient peel.

<!-- P0-185 wear decal shader relocated in current session -->
Coordination note (2026-08-28 P0-185): `map_view_wear_decal.gdshader` now owns the wear/grime/blood decal shader; `MapViewMaterialShaders.shader_resource()` caches preloaded `*.gdshader` resources beside the existing inline `shader()` path. `map_view_material_shaders.gd` dropped from 989 to 980 lines. Verified: `--filter=test_map_view_decals` (8/8), `--filter=test_map_view_material_resolution` (7/7). Next P0-185 shader claim: relocate `puddle` or `cloth` inline sources, then tree meshes / prop models / runtime ambient peel.

<!-- P0-185 mammal shard completed in current session -->
Coordination note (2026-08-28 P0-185): wild mammal species profiles extracted into five P0-185 shards (`carnivore`, `wild_ungulate`, `mustelid`, `small_mammal`, `coastal`) beside the existing `livestock` and `urban_companion` shards. `map_view_mammal_species.gd` facade dropped from 760 to 564 lines (under the 600 soft cap). Verified: `--filter=test_map_view_mammal_species` (4/4), `--filter=test_map_view_urban_fauna` (15/15), `--filter=test_map_view_penned_fauna` (18/18). Bird catalog shards were already closed earlier in P0-185. Next extraction claim: **P0-185** material shaders, tree meshes, prop models, or runtime ambient peel.

<!-- P0-186 completed in current session -->
Coordination note (2026-08-28 P0-186): evidence-image retention landed in `docs/data/evidence_image_retention.json`, `docs/ASSET_STORAGE_POLICY.md`, `tools/verify_evidence_image_retention.py`, and `tools/optimize_evidence_images.py`. Lossless PNG optimize dropped `docs/reports/images` from 127926272 to 118485597 bytes (-9440675); removed 220 stray Godot `*.import` sidecars that violated `docs/reports/images/.gdignore`. Verified: `python3 tools/verify_evidence_image_retention.py --run-acceptance`, `python3 -m unittest tests.python.test_verify_evidence_image_retention -v`. Next size claim: **P0-180** music takes or **P0-185** view3d extractions.

<!-- P0-184 refreshed in current session -->
Coordination note (2026-08-19 P0-184): `docs/ARCHITECTURE.md` large-runtime audit refreshed again to 61 / 18 / 6 scripts over 400 / 600 / 800 lines. Notable drift since 2026-08-13: `map_view_mesh_builder_prop_models.gd` (1020), `map_view_material_shaders.gd` (952), `map_view_runtime_camera.gd` crossed into the 600 band (600), and `map_view_penned_fauna.gd` (412) joined the 400 band. Verified: inventory matches `wc -l scripts/**/*.gd`; next source-size claim remains **P0-185** (view3d extractions) or **P0-186** evidence-image retention.

<!-- P0-187 completed in current session -->
Coordination note (2026-08-13 P0-187): aged coordination history and archived planning notes moved to `docs/ROADMAP_COORDINATION_ARCHIVE_2026-08-13.md`; live `docs/ROADMAP.md` now keeps August 2026 closeouts, Current focus, and a link to the archive. Verified: `python3 tools/generate_active_docs_report.py --check`; byte size dropped below the 2026-08-11 ~121 KiB baseline. Next size/doc claim: **P0-185** view3d extractions or **P0-186** evidence-image retention.

<!-- P0-178 closed in current session -->
Coordination note (2026-08-21 P0-178): storage hygiene is green again after the 2026-08-13 removal of rejected `generated/comfyui/bird_wader_v1/candidates/northern_lapwing_hunyuan_candidate.glb` (commit `20606963`). Verified: `python3 tools/verify_storage_hygiene.py` exits 0 with zero temporary exceptions; active Markdown report regenerated and `--check` green. Next size claim: **P0-179** generated comfyui prune, **P0-180** music takes, or **P0-186** evidence-image retention.

<!-- P0-188 completed in current session -->
Coordination note (2026-08-12 P0-188): character model/animation visual realism review closed in `docs/reports/character_visual_realism_review_2026-08-12.md` with claimable contracts in `docs/CHARACTER_REALISM_BACKLOG.md`. Current hero bodies already ship UVs, procedural PBR maps, LOD1/LOD2, and a parametric face pass, but dialogue closeups still fail on unused vertex-colour albedo, hard beard edges, hair terracing, clay skin response, frozen faces, and thin profession/ambient motion. Next character presentation work: **P0-189** vertex-colour albedo, **P0-191** hair terracing, then **P0-190**/**P0-192** beard/skin, **P0-193** hair cards, **P0-194** plate refresh, and motion rows **P0-195**..**P0-198**. Keep **P0-183** as the parallel GLB size gate.

<!-- P0-177 completed in current session -->
Coordination note (2026-08-11 P0-177): repository size audit closed in `docs/reports/repository_size_audit_2026-08-11.md` with claimable contracts in `docs/STORAGE_SIZE_BACKLOG.md`. Working tree is ~2.11 GiB; largest owners are `music/` (~722 MiB), `history/` (~509 MiB), `archive/` / `generated/` / `assets/` (~200 MiB each). `python3 tools/verify_storage_hygiene.py` is red on `generated/comfyui/bird_wader_v1/candidates/northern_lapwing_hunyuan_candidate.glb` (12.67 MiB, not in LFS). Runtime scripts over 400 lines grew from the P0-065 count of 18 to 60. Next size work: **P0-178** hygiene green, then **P0-179** generated prune, **P0-180** music takes, **P0-181**/**P0-182** research/audio budgets, **P0-183** oversized GLBs, **P0-184**/**P0-185** architecture refresh and justified view3d splits, **P0-186**/**P0-187** evidence-image and ROADMAP history slim-down.

Coordination note (2026-08-13 P0-184): `docs/ARCHITECTURE.md` large-runtime audit refreshed (60 / 23 / 6 scripts over 400 / 600 / 800 lines). EE-agent readability queue and ordered split suggestions are in `docs/reports/agent_file_readability_split_plan_2026-08-13.md`. Next source-size claim: **P0-185** (view3d extractions) or **P0-186** evidence-image retention (**P0-187** ROADMAP archive closed).

<!-- R-294 / P4-025e completed in current session -->
Coordination note (2026-08-11 R-294 / P4-025e): Toompea's Spring-1343 historical acceptance gate now closes the hill-gate dating question. The `toompea_quarter` audit row records Pikk jalg and Lühike jalg as wooden gates with timber/earthen boundary treatment; exact carpentry and guard-house geometry remain bounded reconstruction. The acceptance report annotates all R-006 decisions 1-8 against the four A-012 documentation-only Blender studies and preserves the R-035 post-1346 exclusion lock. This does not claim runtime/gameplay sign-off or activate the prototype; `content/maps/toompea_quarter.rrmap` remains `active=false`. Verified: four `reference_*.png` plates are present at 1200x800 RGBA, the focused Toompea fabric contract is run separately, and the superseded open `C/U` dating sentence is absent.

Coordination note (2026-08-21 R-295 / P4-040): added the typed jurisdiction and hill-gate curfew contract for inactive Toompea. `toompea_danish` and `all_linn_lubeck` bind the two stable hill-gate transitions; `HillGateCurfewController` stores day/night `open` / `closed` state in the existing `GameState.location_states` save payload. The prototype remains `active=false`; focused coverage is `tests/godot/test_jurisdiction_hill_gates.gd`.
<!-- R-748 / R-715 inventory delivered in current session -->
Coordination note (2026-08-27 R-748): froze the existing reflective-water rollout inventory in [`docs/reports/r715_water_rollout_inventory.md`](reports/r715_water_rollout_inventory.md) with a focused contract test at `tests/godot/test_r715_water_rollout_inventory.gd`. The contract enumerates all four `MapTypes.WATER_TERRAINS` IDs and every water-bearing definition returned by `MapAuditRegistry` (13 compiled map IDs), and records the existing material, mesh, shoreline, weather, and tide ownership boundaries. This is an audit-only handoff: no shader, map geometry, weather, visual, or performance acceptance is claimed. R-529's Monastery east-ditch regression and R-713's blocked unified sky/weather evidence remain external blockers. Next bounded rows are R-750, R-751, R-752, R-754, R-755, R-756, and R-757.
<!-- R-752 shared view rollout delivered in current session -->
Coordination note (2026-08-27 R-752): added `tests/godot/test_r715_water_map_rollout.gd` and the completed 13-definition matrix in [`docs/reports/r715_water_rollout_inventory.md`](reports/r715_water_rollout_inventory.md). The contract builds each water-bearing definition through the shared `MapView3D` path, checks one water surface/material per compiled water terrain and one shared environment/weather presenter, and compares terrain fingerprints plus walkability signatures before and after view construction. No authored `.rrmap`, activation policy, stable ID, collision, navigation, or chunk ownership changed. Runtime execution remains pending the pre-existing `SkyWeather3D` parse cascade; R-529 Monastery east-ditch and R-713 sky/weather acceptance stay explicit external handoffs.
<!-- R-751 water surface geometry delivered in current session -->
Coordination note (2026-08-27 R-751): hardened empty water-contour sampling in [`map_view_mesh_builder_terrain_water.gd`](../scripts/map/view3d/map_view_mesh_builder_terrain_water.gd), exposed `MapViewShoreline3D.collect_rock_instances()` for deterministic shoreline placement checks, and added [`tests/godot/test_r715_water_surface_geometry.gd`](../tests/godot/test_r715_water_surface_geometry.gd). Verified: `--filter=test_r715_water_surface_geometry` (3/3). Headless Godot 4.7.1 cannot round-trip `MultiMesh.get_instance_transform()` under the dummy renderer, so shoreline bounds are asserted on the authored transform list while multimesh commit is checked by instance count only. Visual acceptance remains R-756; R-529 and R-713 stay external blockers.



## Current focus (2026-07-30)

- **Milestone:** Act 1 standalone candidate is release-accepted. **P4-012**, **P4-013**, and **P4-044** are closed for package SHA `ea3cf41493394ab6bd01e17de38011b05bf3ee199fd4710a08a4eb3dc`.
- **Current slice:** `act2-fire-of-rebellion` - Act 2 design (**P5-001**) is delivered in `docs/reports/p5_001_act2_design.md` and awaits Canon Keeper review on `docs/CANON.md` timeline additions.
- **Next playable checkpoint:** After canon accept of P5-001, claimable implementation starts at **P5-002** (world travel) and **P5-004** (night mission framework) in parallel when path allowlists do not overlap. Optional Compatibility exit-leak hygiene remains non-blocking (**P4-012-N01** / **P4-044-N01**).
- **Ready roles:** Narrative/Canon close P5-001 review and P7-008 CANON story/history fold (`review: canon`). Dev may prepare P5-002/P5-004 once Producer confirms the focus tick. Lower-band specialists stay on declared queues only when their deps are green. Legacy design may continue on claimable **P7-007** / **P7-005**; **P7-002** MAGIC, **P7-003** NATURAL/PSYCHE, **P7-004** Living City, **P7-008** story/history fold (pending canon), and **P7-009** cast/faction promotion (pending canon on first briefs) are delivered. The five remaining Wave A briefs are landed under **P7-013**; **P4-045** Blackheads candidate ledger stub is delivered pending canon, and face wiring **P4-046** is delivered pending Canon Keeper review.
- **Acceptance event:** Canon Keeper accepts Act 2 timeline labels; then Producer marks P5-001 `[x]` and opens implementation WIP.
- **Lower-band safeguards:** P0-166 waits for R-015/R-017 canon review, P0-167 waits for R-033 canon review, and P0-170 waits for A-007 canon approval. P0-163/P0-165 remain approved historical authoring work but need a later full-slice decomposition before autonomous claim. Claimable **P0-174** / **P0-175** remain available for older focus ticks; **P0-176** closed after P7-004. P7-008 regenerates the active-docs report in-row because its verify clause requires `--check` green.
- **Queue-integrity decision:** dropped the cancelled duplicate `P0-122` blind-readability row because the shipped bird-audio task already owns that stable ID. ADR 0013 remains the authoritative cancellation evidence; the audio P0-122 row keeps its ID and history.
- **Recovered blockers:** A-004 now waits only on its declared brief/contract dependencies in the Blender-enabled Art runtime; A-006 is narrowed to non-overlapping Kalev animation overrides; R-062 now requests rights-cleared type-comparanda instead of unavailable Lootsi raster rights.
- **Downstream reconciliation:** the Art legacy table has no open entry. Research requests map to existing Research-owned rows without creating or reordering R IDs: R-073 (Karrienpforte), R-069 (cart toll/plate), R-064/R-066 (street-cleaning clauses), R-068 (cart plate), R-061/R-062 (Lootsi finds/plates), R-063 (hygiene plate), R-023 (clothing plates), R-039 (Order cross), R-024 (Reval seal), R-020 (healing plate), R-035 (Small Castle drawings), R-030 (Pikk/Lai names), R-007/R-038 (council names), R-026 (Estonian burgher evidence), R-042 (steel/osmund weights), R-017/R-051 (liturgy plates), and R-032/R-052 (back-lane plates). Cross-role Easter and shoreline requests are executable as P0-166/P0-167; merchant-cart and burgher-house requests were already rowed under P0-163/P0-164, P2-063..P2-070, P4-037/P4-038, and A-004/A-008..A-011.
