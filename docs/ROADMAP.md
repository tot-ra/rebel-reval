# Production roadmap

<!-- P2-036 completed in current session -->
Coordination note (2026-07-30 P2-036): standing wader batch now ships authored `standing.glb` meshes for `grey_heron`, `northern_lapwing`, and `common_snipe` under `assets/birds/<species>/` with embedded albedo/normal/roughness, catalog neck/leg/bill overrides, lapwing crown crest, per-species reference plates, and a three-up sheet under `docs/reports/images/fauna/`. Hunyuan3D was unavailable so production used `tools/generate_waders.py`; optional candidate upgrade is **P2-036a**. Verified: `python3 tools/verify_bird_models.py`, `python3 tools/validate_asset_sources.py`, `python3 tools/verify_asset_lint.py`, `--filter=test_map_view_bird_meshes` (5/5), and `--filter=test_map_view_bird_flight` (8/8). Next fauna art step: **P2-037** raptor GLB batch.


<!-- P0-172 completed in current session -->
Coordination note (2026-07-30 P0-172): MapViewRuntime crowd install now uses positional `configure(200, hash(map_id))`; CrowdRenderer compile blockers cleared (directory preload, `Mesh.ARRAY_MATERIAL`, `Basis(Vector3)`), crowd shader `sampler2D` fixed, and MultiMesh sync preserves configured capacity. Verified: checked runner `--filter=test_map_view_crowd_renderer` (8/8), `--filter=test_act1_candidate_acceptance` (7/7), and `--filter=test_act1_packaged_acceptance` (8/8) with zero ambient SCRIPT ERROR. Next Act 1 gate: maintainer **P4-012**, then package **P4-013**.

<!-- P0-163 completed in current session -->
Coordination note (2026-07-30 P0-163): burgher-house typology contract now ships closed `house_tier` allowlist (`merchant_stone` / `merchant_timber` / `craft_boda`) through rrmap tokens, compiler building overrides, and `MapPropStyleVariants`, with diele/dornse depth split, hoist rules, ward bias, and Late-Gothic rejection documented in `docs/reports/burgher_house_typology_contract.md` and `docs/MAP_AUTHORING.md`. Verified: `--filter=test_burgher_house_typology_contract` (3/3). Next burgher-house step: art brief **A-008**, then exterior kits **P2-063**–**P2-065**.

Coordination notes, delivery order, and planning context formerly embedded in `TODO.md`.
Executable open work stays in [`TODO.md`](../TODO.md).

<!-- P2-062 completed in current session -->
Coordination note (2026-07-30 P2-062): smithy domestic-life polish now ships shared `SmithyStationReservations`, `SmithyDomesticLifePresenter` (held props, contact pose offsets, procedural one-shots, one effect root), vignette/Henning runtime snapshots through map world state, and an accelerated 20-minute soak in `test_kalev_smithy_domestic_life.gd`. Verified: `--filter=test_kalev_smithy_domestic_life` (5/5), `--filter=test_smithy_kalev_routine` (8/8), `--filter=test_smithy_henning` (5/5), `--filter=test_smithy_ambient_actors` (4/4), `--filter=test_smithy_routine_controller` (10/10); day/night captures under `docs/reports/images/kalev_smithy_domestic_life/`; acceptance in `docs/reports/kalev_smithy_domestic_life_acceptance.md`. Recorded forge SFX pack and bespoke animation library remain deferred.

Coordination note (2026-07-30 Toompea / R-006): solid dossiers `toompea-castle-and-upper-town` and `toompea-small-castle-interior` are now production inputs for inactive Domberg. Order: **P0-165** fabric contract (wooden hill gates, Danish-phase castle, cathedral construction site, vassal/curia keys, jurisdiction tags, exclusion list) -> art brief **A-012** -> map corrections **P4-025a**–**P4-025d** under umbrella **P4-025** -> acceptance **P4-025e** -> jurisdiction/curfew **P4-040** and Act 1 narrative colour **P4-041** -> Small Castle interior prototype **P4-039**. Decision: current `toompea_quarter` stone hill-gate towers are anachronistic for Spring 1343 and must become wooden gates; omit Pikk Hermann, Order convent, Catherine/baroque east wing, and completed tourist cathedral silhouette. Order castle handover 16 May stays Act 2 (`P5-001` / `P5-009`).

Coordination note (2026-07-30 merchant carts): R-068 `merchant-cart-and-transport-1340s` (plus R-069 toll gap) is now tracked as production work. Order: **P0-164** transport contract (`vehicle_class` = `cart_2w` / `wagon_4w` / `barrow` / `sledge`, ~1.3 m wheel track, no 1343 wheel tax) -> art brief **A-010** -> **A-004** two-wheel *Karren* GLB -> kit **P2-068** and corridor placement **P2-069** -> systems **P4-037** and quests **P4-038** -> traffic wiring **P2-070** -> art sign-off **A-011**. Decisions recorded in the dossier. Default urban vehicle is a single-horse two-wheel cart, not a war wagon or ox team. Verified (P0-164): `--filter=test_merchant_cart_transport_contract`; contract at `docs/reports/merchant_cart_transport_contract.md`.

Coordination note (2026-07-30 burgher houses): R-003 `burgher-house-plan` is now tracked as production work. Order: **P0-163** typology contract (`house_tier` = `merchant_stone` / `merchant_timber` / `craft_boda`) -> art brief **A-008** -> exterior kits **P2-063**–**P2-065** and plot dressing **P2-066** -> Lower Town wiring **P2-067** -> art sign-off **A-009**, then **P0-102** / **P0-101**. Decisions recorded in the dossier. Do not ship late-Gothic tourist facades as the 1343 default.

<!-- P2-035 completed in current session -->
Coordination note (2026-07-30 P2-035): standing waterfowl batch now ships authored `standing.glb` meshes for `mute_swan`, `mallard`, `greylag_goose`, and `great_cormorant` under `assets/birds/<species>/` with embedded albedo/normal/roughness, catalog long-neck overrides, per-species reference plates, and a four-up sheet under `docs/reports/images/fauna/`. Hunyuan3D was unavailable so production used `tools/generate_waterfowl.py`; optional candidate upgrade is **P2-035a**. Verified: `python3 tools/verify_bird_models.py`, `python3 tools/validate_asset_sources.py`, `python3 tools/verify_asset_lint.py`, `--filter=test_map_view_bird_meshes` (5/5), and `--filter=test_map_view_bird_flight` (8/8). Next fauna art step: **P2-036** wader GLB batch (now closed; see P2-036 note).

Coordination note (2026-07-29 P7-001 / ADR 0017): maintainer returned Legacy Design & Research Material to the campaign plan - magic, NATURAL/psyche, Living City meters, expanded cast/factions, and history/story seeds - while keeping MVP-first delivery and treating 2D/pixel sprites as inspiration only. Artifacts: `docs/adr/0017-legacy-design-reintroduction.md`, `docs/LEGACY_REINTRODUCTION.md`, README/AGENTS/CANON sync, P7 track in `TODO.md`. Next legacy steps: **P7-002** MAGIC.md, **P7-003** NATURAL/PSYCHE docs, **P7-004** Living City vs ledger, **P7-009** cast promotion plan. Tower-capture, party control, open world, and other-city campaigns stay out.

Coordination note (2026-07-29 P2-059): Kalev smithy domestic presentation now runs player-driven through `SmithyRoutineController` phase-entry prop variants, optional contextual interactables at authored activity points, held-socket tracking, and map-world-state persistence without autonomous player locomotion. Verified: `--filter=test_smithy_kalev_routine` (8/8) and `--filter=test_smithy_routine_controller` (10/10). Next domestic step: **P2-062** polish and acceptance pass.

<!-- P2-061 completed in current session -->
Coordination note (2026-07-29 P2-061): Mart and the forge cat now run phase-aware ambient routines through `SmithyRoutineController` instead of coordinate patrol loops. Mart stays hidden during prologue and while `flag.mart_missing` is set, then resumes apprentice work/meal/cleanup beats from `content/routines/mart_smithy.json`. The cat cycles sleep, groom, warmth, and feed anchors from `content/routines/forge_cat.json` without claiming human stations or the industrial forge. Verified: `--filter=test_smithy_ambient_actors` (4/4), `--filter=test_forge_cat` (4/4), `--filter=test_smithy_henning` (5/5), and `--filter=test_smithy_routine_controller` (10/10). Next domestic step: **P2-059** Kalev phase-aware household vignettes.

<!-- P2-034 completed in current session -->
Coordination note (2026-07-29 P2-034): harbour gull batch now ships authored gliding flap cycles for `herring_gull`, `common_gull`, and `common_tern` under `assets/birds/<species>/gliding_XX.glb` with embedded albedo/normal/roughness, catalog-matched metric scale, and reference plates plus a three-up sheet under `docs/reports/images/fauna/`. Hunyuan3D was unavailable so production used `tools/generate_harbour_gulls.py`; optional candidate upgrade is **P2-034a**. Verified: `python3 tools/verify_bird_models.py`, `python3 tools/validate_asset_sources.py`, `python3 tools/verify_asset_lint.py`, `--filter=test_map_view_bird_flight` (8/8), and `--filter=test_map_view_bird_meshes` (5/5). Next fauna art step: **P2-035** waterfowl GLB batch.

<!-- P1-042 completed in current session -->
Coordination note (2026-07-29 P1-042): SaveService now serializes concurrent writes through a static mutex and temp-file rotation protocol; combat `health_changed` and dialogue `started`/`finished` autosave stress tests prove signal handlers can persist without interrupting resolution or corrupting slots, and v1-to-v2 GameState payload migration is covered in envelope tests. Verified: `--filter=test_save_service` (10/10), `--filter=test_save_envelope` (13/13), and `--filter=test_save_signal_stress` (2/2). Next runtime step: **P0-145** character-surface texture generator or **P2-059** Kalev household vignettes.

<!-- P0-161 completed in current session -->
Coordination note (2026-07-29 P0-161): playable `kalev_smithy` and `lower_town_slice` now ship authored soot/mud/grime/wet_threshold decals through `map_wear_decals.gd` (forge pad, door thresholds, yard corners, Viru Gate lane) without changing grid fingerprints or routes. Verified: `--filter=test_map_view_decals` (8/8), `--filter=test_kalev_smithy_map`, and `--filter=test_lower_town_slice_map`. Next map step: **P0-162** migrate decal placements into rrmap source lines.

<!-- P0-142 completed in current session -->
Coordination note (2026-07-29 P0-157): projected wear/grime/blood decals now ship as GL-Compatibility soft-edge quads (`MapViewDecals` + `WEAR_DECAL_SHADER_CODE`) with six authored alpha masks under `assets/materials/decals/`, ground-height snapping, and before/after evidence in `docs/reports/images/decals/`. Verified: `--filter=test_map_view_decals` (6/6). Next visual step: **P0-161** author smithy/Lower Town placements, or **P0-144** shared-rig UV unwrap.

Coordination note (2026-07-29 P0-142): renderer evaluation spike now compares `gl_compatibility`, `mobile`, and `forward_plus` on the Lower Town slice through `tools/benchmarks/run_renderer_comparison.sh`, records frame time, texture memory, fidelity probes, and side-by-side captures in `docs/reports/renderer_evaluation.md`, and recommends staying on GL Compatibility for the shipped macOS preset while deferring Forward+ until minimum-hardware GPU capture. Verified: `python3 -m unittest tests.python.test_renderer_evaluation_report -v` and `python3 tools/generate_renderer_evaluation_report.py --check`. Next visual step: **P0-157** wear/grime decal system (renderer path chosen).

Coordination note (2026-07-29 P0-160): fauna GLB PBR contract now requires embedded normal and metallic-roughness maps on every textured body material under `assets/birds/**` and `assets/animals/**`; `tools/fauna_glb_inspect.py` and `ASSET_LINT_FAUNA_PBR` enforce the bar, and `tools/assets/backfill_bird_glb_pbr.py` backfills roughness on authored mallard, house sparrow, chicken, and goat GLBs. Verified: `python3 tools/verify_asset_lint.py`, `python3 -m unittest tests.python.test_fauna_glb_inspect tests.python.test_verify_asset_lint -v`, and `python3 tools/verify_bird_models.py`. Next fauna art step: **P2-034** harbour gull GLB batch with full PBR on import.

Coordination note (2026-07-29 P2-060): Captain Henning now runs the authored prologue visitor sequence (`enter -> wait -> inspect -> talk -> leave`) through `SmithyRoutineController` and `content/routines/henning_smithy_visit.json`; he stays hidden until the commission resolves, interrupts for arrival dialogue, resumes toward leave, and despawns instead of looping a resident patrol. Verified: `--filter=test_smithy_henning` (5/5), `--filter=test_forge_prologue_controller` (2/2), and `--filter=test_makers_mark_prologue` (3/3). Next domestic step: **P2-059** Kalev phase-aware household vignettes.

Coordination note (2026-07-29 P2-057): Kalev smithy living bay now authors the hooded `domestic_hearth`, wash corner, provision storage, table place-settings, linen/apron hooks, and door fuel/ash tools from the P2-054 through P2-056 kits while keeping forge props in the work bay and every protected route green. Verified: `--filter=test_kalev_smithy_map` passes 14/14; `--filter=test_smithy_routine_controller` stays green. Next domestic step: **P2-059** Kalev phase-aware household vignettes.

Coordination note (2026-07-29 P2-056): Smithy household clutter kit now ships as `assets/props/domestic/household/smithy_household_clutter_kit.glb` with seventeen `provision.*` / `household.*` style variants (individual provisions and tools plus closed/in-use/depleted/cleared groupings), pivot markers, and `PROP_KIND_HOUSEHOLD_CLUTTER` runtime selection. Verified: `--filter=test_smithy_household_clutter_assets` passes 5/5; `python3 tools/verify_asset_lint.py` stays green. Next domestic step: **P2-057** redress the living half of `kalev_smithy`.

Coordination note (2026-07-28 P2-053): Kalev smithy domestic-life brief now fixes 26x14 living-bay zones (`LZ-A` through `LZ-G`), phase occupancy (Kalev resident, Mart absent while `mart_missing`, Henning visitor-only, forge cat resident), a hooded masonry cooking hearth separate from `forge_furnace`, the approved `meal.kalev_midday_pottage` artisan meal, reuse versus new-model inventory for every fixture and provision, and stable activity-point IDs for **P2-054** through **P2-061**. Verified: `docs/reports/kalev_smithy_domestic_life_plan.md` cites project dossiers, labels uncertainties, and accounts for all existing `kalev_smithy.rrmap` props, anchors, transitions, and protected routes.

Coordination note (2026-07-28 P0-143): third-person and first-person cameras now attach frozen `CameraAttributesPractical` (subtle auto-exposure plus far-only DoF) through `MapViewRuntimeCamera`; orthographic top-down clears attributes for full map sharpness. Verified: `--filter=test_map_view_runtime_camera` (1/1) and `--filter=test_map_camera_modes` (8/8). Next visual-fidelity step: **P0-144** shared-rig UV unwrap or **P0-155** hero-prop ORM pass.

Coordination note (2026-07-28 P0-140): tiered character fidelity is ratified in ADR 0016 with frozen Tier 0/1/2 triangle and texture caps in `docs/VISUAL_FIDELITY_PLAN.md`; `tools/character_fidelity_tiers.py` classifies every runtime `assets/characters/**` GLB and `tools/verify_asset_lint.py` enforces budgets. Verified: `python3 tools/verify_asset_lint.py`, `python3 -m unittest tests.python.test_verify_asset_lint tests.python.test_character_fidelity_tiers -v`. Next visual-fidelity step: **P0-144** shared-rig UV unwrap or **P0-155** hero-prop ORM pass.

Coordination note (2026-07-28 R-055): `history/reference/plates.csv` manifest audit closed CSV comma drift on four Commons page URLs (`guild-structure.02/.04`, `churches-and-religious-houses.01/.03`), corrected the stale `Lai 5` upload hash (`0/0e` -> `f/fa`) on `architecture.burgher-house-plan.03` and `topography.back-lanes-east-of-pikk.04`, and re-fetched all eight previously `failed` plates (rate-limited rows via Commons `thumburl`). Verified: `python3 tools/research/fetch_reference_plates.py --verify` reports 137 plates, zero problems, zero `failed` rows. Next research hygiene step: **R-036** Raekoja hall phase plans.

Coordination note (2026-07-28 R-035): Toompea Small Castle interior dossier now closes Danish April 1343 hill zones (`SC-*`, `GC-*`, outer bailey) with explicit post-1346 Order layout exclusions; reference plates `.01` and `.02` fetched locally after Commons hash and CSV comma fixes. Verified: dossier `status: solid`; `python3 tools/research/fetch_reference_plates.py --slug toompea-small-castle-interior --verify` passes. Next architecture step: **R-036** Raekoja hall phase plans or Toompea map prototype when act scope opens.

Coordination note (2026-07-28 P2-049): medieval lighting kit v2 now ships flame-free holder meshes with `FlameAnchor` markers and runtime `CandleFlame3D` particle fire plus synchronized `CandleLight3D` flicker across all five `style_variant` fuels. Verified: `--filter=test_medieval_lighting_assets` (2/2), `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`. Next prop art step: **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 P0-136): rigged medieval pig now ships from the licensed hendrikReyneke Sketchfab source with preserved cloven-foot topology, visible eyes, and looping `Idle`/`Walk` skeletal clips; `normalize_dimensions` now scales from vertex extents so preserve_topology imports avoid stale bound-box shrink. Verified: `--filter=test_medieval_animal_models` (7/7), `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`. Next livestock art step: **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 P2-048): deterministic household chest and medieval storage-furniture GLBs now replace procedural box placeholders for `chest` and `shelf` props through `MapViewChestModels` and `MapViewStorageFurnitureModels`; strict `style_variant` allowlists gate wealth tiers (`chest.plain_coffer` / `chest.burgher` / `chest.merchant_strongbox`, `shelf.common_open` / `shelf.burgher_cupboard` / `shelf.elite_armarium`) with variant-aware climb heights and explicit interior assignments on smithy, town hall, Holy Spirit, and guild hall maps. Verified: `--filter=test_chest_assets` (5/5), `--filter=test_medieval_storage_furniture` (5/5), `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`. Next interior prop step: **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 P0-134): rigged medieval cattle now ships visible eyes, an articulated tail, and looping `Idle`/`Walk` skeletal clips from `tools/assets/build_medieval_animal_models.py`; `MapViewMedievalAnimalModels.sync_animation` drives penned and urban livestock actors from displacement. Verified: `--filter=test_medieval_animal_models` (4/4), `--filter=test_map_view_penned_fauna`, and `--filter=test_map_view_urban_fauna` stay green. Next livestock art step: **P0-132** sheep and pack-horse locomotion rigs or **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 R-046): law-courts-and-punishment dossier reference plates now fetched locally: four Sachsenspiegel court/oath/combat miniatures plus Schwäbisch Hall pillory engraving under `history/reference/power/law-courts-and-punishment/`; `plates.csv` URLs corrected (Commons hash drift and CSV quoting for the fol. 4 comma title). Verified: `python3 tools/research/fetch_reference_plates.py --slug law-courts-and-punishment --verify` passes with four `fetched` rows. Next power step: **R-043** forum pillory placement or **R-040** Reval law codex arms and watch.

Coordination note (2026-07-28 R-010): watch-duty-and-town-defence dossier now closes Spring 1343 burgher militia watch: nightly routine table, arms-by-status tiers, gate priorities, siege overlay, six reference plates (four fetched, two link-only), and production hooks for Henning checkpoint scenes. Verified: dossier `status: solid`; `python3 tools/research/fetch_reference_plates.py --slug watch-duty-and-town-defence --verify` passes. Next military step: **R-040** Reval law codex arms and watch ordinances.

Coordination note (2026-07-28 R-006): toompea-castle-and-upper-town dossier now closes Danish Toompea / All-linn boundary for Spring 1343: plateau and castle fabric table, Dome church construction-site read, vassal belt, Pikk jalg / Lühike jalg wooden gate control points, and six reference plates (four fetched locally, two link-only pending **R-045** Wikimedia retry). Verified: dossier `status: solid`; `python3 tools/research/fetch_reference_plates.py --slug toompea-castle-and-upper-town --verify` passes; `history/reference/plates.csv` rows `.01`-.`.06` registered. Next architecture step: **R-035** Small Castle interior.

Coordination note (2026-07-28 R-008): jurisdictions-of-reval dossier now closes Spring 1343 authority seams: master jurisdiction table, geographic seams, May 1343 timeline spine, Kalev smith legal hooks, and three fetched reference plates under `history/reference/power/jurisdictions-of-reval/`. Verified: dossier `status: solid`; `history/reference/plates.csv` rows `.01`-.`.03` registered. Next power step: **R-009** law courts and punishment.

Coordination note (2026-07-28 R-004): smithy-workshop-layout dossier now closes Kalev forge spatial contract: labelled forge/living floor plan, hearth-bellows-anvil-quench geometry, charcoal storage, smoke handling, curfew fire-risk rule, and four fetched reference plates under `history/reference/architecture/smithy-workshop-layout/`. Verified: dossier `status: solid`; `python3 tools/research/fetch_reference_plates.py --slug smithy-workshop-layout --verify` passes; `history/reference/plates.csv` rows `.01`–`.04` fetched. Next craft step: **R-013** blacksmith materials and techniques.

Coordination note (2026-07-28 R-003): burgher-house-plan dossier now closes Lower Town diele-dornse typology with labelled merchant and *boda* floor plans, room-zone prop table, eight reference plates under `history/reference/architecture/burgher-house-plan/`, and `status: solid`. Verified: dossier verify line (floor-plan + prop list); `history/reference/plates.csv` rows `.01`–`.08` registered. Next architecture step: **R-004** smithy workshop layout.

Coordination note (2026-07-28 R-005): harbour-and-shoreline dossier now closes 1343 wet margin decisions: two harbour zones (merchant landing below Coastal Gate, Kalamaja fishing shore), shoreline displacement vs modern coast, Härjapea delta at Lootsi, ship-type table, and six reference plates (three fetched, three link-only). Verified: dossier `status: solid`; `history/reference/plates.csv` rows `.01`–`.06` registered.

Coordination note (2026-07-28 R-031): Old Market / Vanaturu kael dossier now closes the 1343 east-throat boundary: *forum inferior* and *Vana turg* are absent in April 1343; Vanaturu kael is a 4-6 m paved lane from Raekoja plats SE to the Viru/Vene convergence, with labelled authoring polygon and four reference plates. Verified: dossier `status: solid`; `history/reference/plates.csv` rows `.01`-.`.04` fetched.

Coordination note (2026-07-28 P2-047): Kalamaja net-yard `fishing_nets` now ship tuned GPU wind deformation (pinned rack top, hemp/float/sinker sway) with 12 signed weather captures under `docs/reports/images/view3d/harbor_east_fishing_nets/` and `tools/verify_harbor_fishing_net_wind_captures.py`. Verified: `python3 tools/verify_harbor_fishing_net_wind_captures.py`, `python3 -m unittest tests.python.test_verify_harbor_fishing_net_wind_captures -v`, and `--filter=test_fishing_nets` (3/3). Next fauna art step: **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 P2-046): deterministic `fishing_nets.glb` and four Hanseatic `trade_goods` GLBs now replace sphere-and-box harbor placeholders through `map_view_fishing_net_models.gd`, `map_view_trade_goods_models.gd`, and `map_view_mesh_builder_prop_models.gd`; stable map IDs keep eastern furs, western cloth, Livonian grain, and barrelled herring reads distinct. Verified: `--filter=test_fishing_nets` (2/2), `--filter=test_hanseatic_trade_goods_assets` (2/2), `--filter=test_reval_harbor_map`, `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-045): deterministic `tanning_frame.glb` now replaces the procedural box placeholder for `PROP_KIND_TANNING_FRAME` through `map_view_tanning_frame_models.gd`; irregular hide silhouette, perimeter lacing, and ground braces stay readable at gameplay scale. Verified: `--filter=test_tanning_frame_assets` (2/2), `--filter=test_district_life_prop_kinds`, `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-044): deterministic portable `market_stall.glb` now replaces procedural stall placeholders for `PROP_KIND_STALL` through `map_view_market_stall_models.gd` while preserving rrmap anchors and market-day visibility toggles. Verified: `--filter=test_market_stall_assets` (2/2), `--filter=test_district_life_prop_kinds`, `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-033): custom bird GLB pipeline now prefers authored meshes from `assets/birds/<species>/<pose>.glb` and `gliding_00`..`07` flap frames through `MapViewBirdAssets`, with procedural P0-117 fallback and `tools/verify_bird_models.py` guarding scale, triangle budget, pose naming, and `SOURCES.csv` provenance. Verified: `python3 tools/verify_bird_models.py`, `python3 -m unittest tests.python.test_verify_bird_models -v`, and isolated `test_map_view_bird_meshes` (4/4). Next fauna art step: **P2-034** harbour gull GLB batch.

Coordination note (2026-07-28 P2-043): deterministic medieval `wooden_cart.glb` now replaces procedural box-and-cylinder placeholders for both `cart` and `farm_cart` prop kinds through `map_view_cart_models.gd`. Verified: `--filter=test_wooden_cart_assets` (2/2), `--filter=test_rural_life_prop_kinds`, `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-032): MapBlueprint compiler v7 now compiles `gate_variant` and `grille_variant` from `.rrmap` gate_arch landmarks so P2-029 hardware survives the rrmap pipeline; Holy Spirit Church reciprocal door on `market_civic_quarter` now uses `spawn_offset_px=0,96` so arrivals land south of the chapel mass. Verified: `tools/validate_map_blueprints.gd`, `--filter=test_lower_town_slice_map`, `--filter=test_medieval_gate_assets`, and `--filter=test_holy_spirit_church`.

Coordination note (2026-07-28 P0-133): Town Hall and Holy Spirit Church interiors now have conversion-plan index rows, scene-inventory coverage, registry-aligned mandatory anchors, and regenerated 1600x900 map-audit captures (~68 KiB each). Verified: `python3 tools/verify_map_audit.py`, `python3 tools/verify_map_conversion_plan.py`, and `--filter=test_map_quality_audit::test_capture_policy_and_pngs_are_present`.

Coordination note (2026-07-28 P2-030): Kalev smithy now ships deterministic Blender-authored `smithy_bed.glb` at `bed` with oak/linen/wool embedded albedos, Godot-extracted sidecar provenance, and a generic bed fallback on other maps. Verified: `--filter=test_forge_prop_meshes` (8/8), `python3 tools/validate_asset_sources.py`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-029): deterministic 1343 Reval gate kit now ships `oak_double_gate.glb`, `ironbound_double_gate.glb`, and `raised_portcullis.glb` from `tools/generate_medieval_gate_kit.py`; `gate_arch` landmarks load parked-open leaves and a raised grille at `viru_gate_arch` and `viru_foregate_arch` without blocking the walkable passage. Verified: `--filter=test_medieval_gate_assets` (2/2), `--filter=test_map_view_3d_fortification`, and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P0-132): production hay fiber texture (`assets/materials/production/hay_fibers.png`) now drives triplanar rick/wagon geometry, terrain hay/straw blends, and batched meadow stubble; ComfyUI livestock GLBs for cattle, sheep, and pack horse share one loader across static props and ambient penned/urban fauna actors. Verified: `--filter=test_hay_assets` (4/4), `--filter=test_medieval_animal_models` (3/3), and `python3 tools/verify_asset_lint.py`.

Coordination note (2026-07-28 P2-028): Kalev smithy now ships deterministic Blender-authored `smithy_furnace.glb` and `smithy_bellows.glb` at `forge_furnace` and `forge_bellows`; the furnace keeps engine-driven embers, coal, flames, sparks, and day/night fire lighting while non-smithy maps retain procedural fallbacks. Verified: `--filter=test_forge_prop_meshes` (8/8).

Coordination note (2026-07-28 P2-027): Kalev smithy now ships a deterministic Blender-authored `smithy_anvil.glb` at `forge_anvil` while courtyard anvils keep the procedural fallback; Captain Henning's seated routine snaps to the authored `work_chair` prop so sit animations cannot drift from the chair GLB. Verified: `--filter=test_forge_prop_meshes` (6/6), `--filter=test_smithy_henning` (3/3), and `python3 tools/verify_asset_lint.py`.

## Current focus (2026-07-30)

- **Milestone:** Act 1 standalone candidate is release-accepted. **P4-012**, **P4-013**, and **P4-044** are closed for package SHA `ea3cf41493394ab6bd01e17de38011b05bf3ee199fd4710a08a4eb3dc`.
- **Current slice:** `act2-fire-of-rebellion` - Act 2 design (**P5-001**) is delivered in `docs/reports/p5_001_act2_design.md` and awaits Canon Keeper review on `docs/CANON.md` timeline additions.
- **Next playable checkpoint:** After canon accept of P5-001, claimable implementation starts at **P5-002** (world travel) and **P5-004** (night mission framework) in parallel when path allowlists do not overlap. Optional Compatibility exit-leak hygiene remains non-blocking (**P4-012-N01** / **P4-044-N01**).
- **Ready roles:** Narrative/Canon close P5-001 review and P7-008 CANON story/history fold (`review: canon`). Dev may prepare P5-002/P5-004 once Producer confirms the focus tick. Lower-band specialists stay on declared queues only when their deps are green. Legacy design may continue on claimable **P7-007** / **P7-005**; **P7-002** MAGIC, **P7-003** NATURAL/PSYCHE, **P7-004** Living City, **P7-008** story/history fold (pending canon), and **P7-009** cast/faction promotion (pending canon on first briefs) are delivered. The five remaining Wave A briefs are landed under **P7-013**; **P4-045** Blackheads candidate ledger stub is delivered pending canon, and face wiring **P4-046** is delivered pending Canon Keeper review.
- **Acceptance event:** Canon Keeper accepts Act 2 timeline labels; then Producer marks P5-001 `[x]` and opens implementation WIP.
- **Lower-band safeguards:** P0-166 waits for R-015/R-017 canon review, P0-167 waits for R-033 canon review, and P0-170 waits for A-007 canon approval. P0-163/P0-165 remain approved historical authoring work but need a later full-slice decomposition before autonomous claim. Claimable **P0-174** / **P0-175** remain available for older focus ticks; **P0-176** closed after P7-004. P7-008 regenerates the active-docs report in-row because its verify clause requires `--check` green.
- **Queue-integrity decision:** dropped the cancelled duplicate `P0-122` blind-readability row because the shipped bird-audio task already owns that stable ID. ADR 0013 remains the authoritative cancellation evidence; the audio P0-122 row keeps its ID and history.
- **Recovered blockers:** A-004 now waits only on its declared brief/contract dependencies in the Blender-enabled Art runtime; A-006 is narrowed to non-overlapping Kalev animation overrides; R-062 now requests rights-cleared type-comparanda instead of unavailable Lootsi raster rights.
- **Downstream reconciliation:** the Art legacy table has no open entry. Research requests map to existing Research-owned rows without creating or reordering R IDs: R-073 (Karrienpforte), R-069 (cart toll/plate), R-064/R-066 (street-cleaning clauses), R-068 (cart plate), R-061/R-062 (Lootsi finds/plates), R-063 (hygiene plate), R-023 (clothing plates), R-039 (Order cross), R-024 (Reval seal), R-020 (healing plate), R-035 (Small Castle drawings), R-030 (Pikk/Lai names), R-007/R-038 (council names), R-026 (Estonian burgher evidence), R-042 (steel/osmund weights), R-017/R-051 (liturgy plates), and R-032/R-052 (back-lane plates). Cross-role Easter and shoreline requests are executable as P0-166/P0-167; merchant-cart and burgher-house requests were already rowed under P0-163/P0-164, P2-063..P2-070, P4-037/P4-038, and A-004/A-008..A-011.

## Coordination history

Coordination note (2026-07-31 P4-045): `faction.blackheads` ships as a **candidate** ledger seat (events/IDs only) via `FactionCandidateSeats` and `docs/data/faction_blackheads_candidate.json`. Content stub `content/examples/support/faction.blackheads.candidate_hooks.json` records Johann / Hinrik / Mart Weaver hooks. Journal launch snapshot stays exactly eight (`FactionLedger.ACTIVE_FACTIONS`); README launch table unchanged; Lizard Union has no ledger ID. Verified: `--filter=test_faction_ledger` 11/11; `python3 tools/validate_content.py content/examples/valid content/examples/support`. Pending Canon Keeper. Face content_links wiring is **P4-046** after **P7-013**.

Coordination note (2026-08-01 P4-046): candidate hooks now link `char.johann_von_minden`, `char.hinrik_cartographer`, and `char.mart_weaver` plus one deterministic offline dialogue per face. Each dialogue records its existing `ledger.blackheads.*` event; `FactionLedger.ACTIVE_FACTIONS` remains exactly eight, README launch roster and Lizard Union exclusion are unchanged. Verified: `python3 tools/validate_content.py content/examples/valid content/examples/support` and `--filter=test_faction_ledger` (11/11). Pending Canon Keeper.


Coordination note (2026-07-31 P7-009): Cast and faction promotion plan landed in `docs/cast_faction_promotion.md` with 18 promote-first NPCs (priority, act gates, Tier-1 art default) and faction candidates beyond the eight launch factions. First briefs: Old Toomas, Martin of the Cloaks, Konrad Preen, Lembit Helme. Lizard Union stays an intrigue cell, not a ninth launch ledger, until a later ADR. Legacy 2D art remains inspiration only. Next legacy design: **P7-007** mini-game shortlist and **P7-005** combat/night addendum; brief follow-on **P7-013**; Blackheads ledger stub **P4-045**.

Coordination note (2026-07-31 P7-008): Canon/story pass folded usable beats from `history/HISTORY.md`, `history/TIMELINE.md`, and `story/STORY.md` into `docs/CANON.md` with confidence labels: extended 1342-1346 timeline (Padise, Pskov Otepää colour, Saaremaa/Pöide/Karja/Maasilinna, 1346 sale), political landscape, and a promote/adapt/defer/reject story-seed table. Rejected Chaos meter, Act 1-as-1342, wife/daughter hostage, and Pärnu as Sõjamäe rewrite. Plague 1351 stays non-canon. Pending Canon Keeper `review: canon`. Next legacy design: **P7-007** mini-game shortlist and **P7-009** cast/faction promotion (evaluate deferred Lizard Union). Active-docs report regenerated in-row to satisfy P7-008 verify.

Coordination note (2026-07-31 P7-004): Living City contract landed in `docs/SYSTEMS/LIVING_CITY.md`. Hope (`living_city.hope`) and Fear (`living_city.fear`) are independent city meters that complement `record_faction_event` with explicit `record_living_city_event` ops; Balance of Power aggregate, tower-majority victory, and NPC-allegiance sums stay rejected. Worked Bell and Chain forge examples update ledger and meters together without a morality score. CANON, GAMEPLAY, GAME-PILLARS, and LEGACY_REINTRODUCTION point at the contract. Next legacy design: **P7-007** / **P7-008** / **P7-009**; runtime remains **P7-012**. Active-docs refresh for this tick is claimable **P0-176** (P0-175 still covers the P7-003 tick).

Coordination note (2026-07-30 P7-003): NATURAL and Hingepuu psyche contracts landed in `docs/SYSTEMS/NATURAL.md` and `docs/SYSTEMS/PSYCHE.md`. Defines seven `aspect.*` ranks with authored point grants, first Hingepuu allocation (no custom-hero creator), save fields, art-bible UI needs, psyche states / four faces, and an additive host that extends (does not replace) Duty/Fury/Mercy reflection. CANON and LEGACY_REINTRODUCTION point at the new contracts. Next legacy design: **P7-004** Living City vs ledger, then **P7-007** / **P7-008** / **P7-009**. Runtime remains **P7-011** (after P7-010 magic foundation). Active-docs refresh stays on claimable **P0-175**.

Coordination note (2026-07-30 P7-002): Dual-school magic contract landed in `docs/SYSTEMS/MAGIC.md`. Defines `school.pagan` cookbook forging (fail-closed, ADR 0003), fixed `school.divine` rites, act-gated element budgets, hammer conduit rules, content ID forms (`spell.*` / `rite.*` / `element.*`), and a slice-safe partial ship that keeps demo/Act 1 playable with magic disabled. CANON and LEGACY_REINTRODUCTION point at the new contract. Next legacy design: **P7-003** NATURAL/PSYCHE, **P7-004** Living City vs ledger. Runtime remains **P7-010**. Active-docs refresh stays on claimable **P0-174**.

Coordination note (2026-07-30 P5-001): Act 2 design delivered in `docs/reports/p5_001_act2_design.md` with machine table `docs/data/act2_design_manifest.json`. Covers Viru act-boundary opening, three siege phases, mission list mapped to P5-002..P5-009 / P5-005, eight-faction war-state overlay, and CANON timeline entries for Paide, Kanavere, Sõjamäe, 16 May Order leverage, and the Swedish fleet. Verified: `python3 -m unittest tests.python.test_act2_design_manifest -v`. Pending: Canon Keeper review on CANON labels; then **P0-174** active-docs refresh and implementation claims on **P5-002** / **P5-004**.

Coordination note (2026-07-30 P4-044): Act 1 release acceptance is closed in `docs/reports/p4_044_act1_release_acceptance.md` against the exact P4-013 package (`build/act1/rr.dmg`, sha256 ea3cf41493394ab6bd01e17de38011b05bf3ee199fd4710a08a4eb3dc1eafbdc). Verified: `tools/verify_act1_release.sh` (package bind, traversal, accessibility, licences, content budget, `--filter=test_act1_release_acceptance` 7/7, `--filter=test_act1_traversal` 8/8, clean-install packaged smoke PASS). No unresolved critical/high finding. Producer may stop the Act 1 standalone-candidate focus.

Coordination note (2026-07-30 P4-012): Act 1 maintainer gate is closed in `docs/reports/p4_012_act1_gate.md`. P4-042-F01 and P4-043-F01 are closed after P0-172; Act 1 traversal (8/8), candidate acceptance (7/7), packaged acceptance (8/8), six cycle filters, and `SKIP_EXPORT=1 tools/verify_supported_platform.sh` install/start/save/load/exit are green. Next Act 1 step: **P4-013** package export, then **P4-044** release acceptance. **P0-171** remains the claimable active-docs refresh after this focus tick.

Archived focus note (2026-07-23 P0-053 closeout): slice surface kit is closed. `tools/verify_slice_surface_captures.py` now enforces 1280x720 day/night evidence for `kalev_smithy` and `lower_town_slice`, checks non-blank detail, and requires day frames to read brighter than night; `python3 tools/verify_slice_surface_captures.py` and `python3 -m unittest tests.python.test_verify_slice_surface_captures -v` pass 5/5 alongside the existing asset lint and Godot wiring/capture suites. Next surface-dependent step: **P0-040** maintainer ART_BIBLE v2 freeze.

Coordination note (2026-07-23 ADR 0014): maintainer confirmed authorial acceptance workflow. External playtest rounds (P3-003, P3-004, P3-006) and independent-historian gate on P0-072 are cancelled. Slice and act gates (P3-014, P4-012, P5-010, P6-007) now use maintainer playable review plus automated traversal; P0-111 is maintainer sign-off only.

Coordination note (2026-07-24 P1-036): `MapCompositionAudit`, `docs/data/map_composition_thresholds.json`, `tools/verify_map_composition.py`, and `tools/audit_map_composition.gd` now enforce signed P0-072 bands on `kalev_smithy`, `monastery_quarter`, `viru_gate_foreland`, and `town_hall`; remaining registry maps keep threshold cards with `enforce=false` until their district environment passes land. Verified: `python3 tools/verify_map_composition.py`, `python3 -m unittest tests.python.test_verify_map_composition -v`, and `tools/run_composition_tests.gd` (6/6).

Coordination note (2026-07-23 P0-121): `tools/release_candidate_check.py` now automates the repository-side release-candidate scaffold for AGPL license, asset provenance manifest, macOS export preset, clean-clone CI indicators, and the P1-028 accessibility baseline; `python3 tools/release_candidate_check.py` and `python3 -m unittest tests.python.test_release_candidate_check -v` pass 8/8. Full P3-012 / P4-013 / P6-008 gates still require maintainer playable review, accessibility checklist, and campaign acceptance evidence per ADR 0014.

Coordination note (2026-07-23 P0-053): coastal harbour equilibrium tide now follows the lunar calendar through `SkyWeather3D.tide_level`, updates coastal water shaders via `MapViewMaterials.apply_coastal_tide`, and stays isolated from rivers. Procedural thatch roofs use slope-oriented reed relief, recessed timber gable infill, and a dedicated reed UV pattern distinct from field straw. Per-building wall and roof materials now derive unique pattern seeds and one of four weathering bands (fresh, worn, damp, repaired) from each stable building ID so adjacent houses no longer share one baked albedo map. Verified: `--filter=test_coastal_sea_3d` (7/7), `--filter=test_sky_weather_3d` (28/28), `--filter=test_map_view_3d_lighting` (13/13), `--filter=test_thatch_roof_dressing` (1/1), `--filter=test_building_surface_weathering` (4/4), and `--filter=test_enemy_combat_state_machine` extended for P5-008 knight/crossbowman archetypes.
Coordination note (2026-07-22 P5-008): Order knight and crossbowman archetypes now resolve through `EnemyArchetype.from_id()` and reuse `EnemyCombatStateMachine`; mission allies use `MissionAllyScript` plus `MissionAllyController` for scripted in-range support without party-control UI; `--filter=test_enemy_archetype_p5_008` passes 7/7.
Coordination note (2026-07-22 P4-018 / P1-038): quest package manifests, branch maps, and landmark beat bindings now live under `content/packages/`; `tools/verify_quest_packages.py` and `tools/generate_quest_package_tests.py --check` validate beat IDs against `docs/data/landmark_integrations.json` and emit Godot traversal tests under `tests/godot/generated/`; the `act1_south_quarter_probe` example passes content validation and `--filter=test_quest_package_act1_south_quarter_probe` (2/2).
Coordination note (2026-07-24 P0-111): maintainer signed all five human-review rows in `docs/HISTORICAL_AUDIT.md` per ADR 0014; `python3 tools/verify_historical_dossier.py` now reports `human review signed`. P1-036 and district quality passes may enforce the signed ranges; world-travel wrapper maps remain outside the thirteen-card urban dossier scope.
Coordination note (2026-07-24 P1-029): `tools/verify_asset_lint.py` now enforces style-lock texture dimensions and seamless edges, character scale contract constants, runtime character glb presence and naming, equipment prop grip mounts, portrait dimensions when files exist, and provenance rows for lint-scoped assets; `python3 tools/verify_asset_lint.py` and `python3 -m unittest tests.python.test_verify_asset_lint -v` pass 10/10. Next composition gate: district environment passes that flip `enforce=true` on their threshold cards.
Coordination note (2026-07-24 P0-115): Baltic shrub kit is closed at 20/20 through `MapViewBushSpecies`, `MapViewBushMeshes`, and species-batched scatter in `map_view_mesh_builder_scatter.gd`; legacy `bush.dense` / `bush.scrub` remain weighted group aliases; shrub bands documented in `docs/HISTORICAL_AUDIT.md` shared constraint 9. Verified: `--filter=test_map_view_bush_species`. Next fauna step: **P2-024** urban mammals and **P0-106** penned livestock actors.
Coordination note (2026-07-25 P3-016): vertical-slice end-to-end suite now fingerprints traversal, flow, save-matrix, release, and platform contracts through `VerticalSliceE2EModel` and `tools/verify_slice_e2e.sh`. Verified: `tools/verify_slice_e2e.sh`, `python3 tools/report_slice_e2e.py --check`, `python3 -m unittest tests.python.test_report_slice_e2e -v`, and `--filter=test_vertical_slice_e2e`. Next slice validation step: none in P3; resume environment realism under **P0-100** / **P2-021**.
Coordination note (2026-07-25 P3-015): vertical-slice release is tagged `v0.1.0-slice` with frozen save envelope v1, game-state v2, map-world-state v2, and content-schema v1 fingerprints in `docs/data/slice_release_manifest.json`; published fixture `content/saves/released/save.slice_prologue_complete.json` loads through `SaveEnvelope` and satisfies `checkpoint.prologue_complete`. Verified: `tools/verify_slice_release.sh`, `python3 tools/report_slice_release.py --check`, `python3 -m unittest tests.python.test_report_slice_release -v`, and `--filter=test_vertical_slice_release`. Next slice validation step: **P3-016** end-to-end branch traversal and save compatibility across declared platforms.
Coordination note (2026-07-25 P3-014): vertical-slice MVP maintainer gate is closed in `docs/reports/p3_014_slice_gate.md` with README acceptance criteria, playable-review findings, and green sub-gates P3-001 through P3-013; `python3 tools/release_candidate_check.py` now includes a slice-gate check. Verified: `python3 -m unittest tests.python.test_release_candidate_check -v` and `--filter=test_vertical_slice` (31/31). Next slice step: **P3-015** tagged release with frozen save/content schema versions.
Coordination note (2026-07-25 P3-012): vertical-slice supported-platform declaration now fingerprints macOS universal as the only supported desktop export through `VerticalSlicePlatformModel`, `docs/data/slice_platform_manifest.json`, and `tools/report_slice_platform.py`; packaged install/start/save/load/exit proof runs through `tools/verify_supported_platform.sh` with `-- --verify-packaged-platform` inside `Reval Rebel.app`. Verified: `python3 tools/report_slice_platform.py --check`, `python3 -m unittest tests.python.test_report_slice_platform -v`, and `--filter=test_vertical_slice_platform` / `--filter=test_packaged_platform_smoke`. Next slice validation step: **P3-014** maintainer gate report.
Coordination note (2026-07-25 P3-008): slice information design is closed through `VerticalSliceInformationDesignModel` with 23 catalogued beats and six in-game historical context bindings; `python3 tools/report_slice_information_design.py --check`, `python3 -m unittest tests.python.test_report_slice_information_design -v`, and `--filter=test_vertical_slice_information_design` (4/4) pass; maintainer grayscale/muted review recorded in `docs/reports/p3_008_information_design.md`. Next slice validation step: **P3-011** performance budget or **P3-014** maintainer gate report.
Coordination note (2026-07-25 P2-005): watchman and sergeant visual variants now ship as generated bodies (`watchman.glb`, `sergeant.glb`) with spear vs pauldron/helmet reads, distinct walk overrides, and combat-room shape silhouettes that do not depend on tint alone; `--filter=test_character_rig` passes 18/18 including `test_watchman_and_sergeant_are_distinguishable_without_color_cues`. Next character step: **P2-004** cast portraits/models and optional map-runtime wiring of `EnemyArchetype.character_scene` on patrol hosts. (guard hold/toggle, screenshake, reduced flashing, enhanced focus contrast) through `GameplayAccessibilitySettings` and the Esc settings overlay; remapping remains on the Controls overlay with a direct entry point from settings; lightning flashes and combat camera shake respect the new toggles while reduced motion still suppresses shake. Verified: `python3 tools/report_accessibility_checklist.py --check`, `python3 -m unittest tests.python.test_report_accessibility_checklist -v`, `--filter=test_gameplay_accessibility_settings` (7/7), and `--filter=test_game_settings_overlay` (9/9). Next slice validation step: **P3-011** performance budget.
Coordination note (2026-07-25 P3-005): vertical-slice branch consequence review now fingerprints every retained major choice group (`makers_mark.ledger`, `watch_buckle.forge`, `bitter_brew.forge`, `reflection.conviction`) through `VerticalSliceBranchConsequenceModel`; preserve/alter ledger branches now diverge mechanically (`rel.henning_trust` vs `rel.mart_trust`). Verified: `python3 tools/report_slice_branch_consequences.py --check`, `python3 -m unittest tests.python.test_report_slice_branch_consequences -v`, and `--filter=test_vertical_slice_branch_consequences` (2/2). Next slice validation step: **P3-007** accessibility options or **P3-011** performance budget.
Coordination note (2026-07-25 P3-013): vertical-slice third-party notices and asset/license report now map every exported non-original asset to `docs/THIRD_PARTY_NOTICES.md`, `CREDITS.md`, and approved `assets/SOURCES.csv` rows through `docs/data/slice_third_party_manifest.json`; `python3 tools/report_slice_third_party.py --check` and `python3 -m unittest tests.python.test_report_slice_third_party -v` pass 5/5; desktop export now bundles the notices and bundled license texts. Next slice validation step: **P3-005** branch consequence review or **P3-007** accessibility options.
Coordination note (2026-07-25 P3-001): vertical-slice traversal validation now documents three intended aftermath endings and nine deliberate invalid transitions through `VerticalSliceTraversalModel`, `docs/data/slice_traversal_manifest.json`, and `--filter=test_vertical_slice_traversal` (7/7); `python3 tools/report_slice_traversal.py --check` and `python3 -m unittest tests.python.test_report_slice_traversal -v` pass 2/2. Next slice validation step: **P3-005** branch consequence review or **P3-013** third-party notices.
Coordination note (2026-07-25 P2-017): vertical-slice input completion now records keyboard/mouse and gamepad runs for every shipped action through `VerticalSliceInputCatalog`, `SliceInputDriver`, and `--filter=test_vertical_slice_input_completion` (6/6); honest-branch prologue-through-reflection traversal uses real input events only (no `Input.action_press` or `_for_test` fallbacks); forge commission, night outcome, and reflection overlays seed focus for confirm. Next slice validation step: **P3-001** invalid-state traversal.
Coordination note (2026-07-25 P2-014): vertical-slice soundtrack budget now inventories every release theme track through `docs/data/slice_soundtrack_manifest.json`; gameplay themes (`forge`, `town`) ship three unique approved tracks totaling 359.8 s under the 720 s cap while the menu loop stays budget-exempt; `python3 tools/report_slice_soundtrack.py --check` and `python3 -m unittest tests.python.test_report_slice_soundtrack -v` pass 6/6. Next slice polish step: **P2-017** input completion.
Coordination note (2026-07-25 P2-016): vertical-slice save/reload matrix now round-trips ten named checkpoints (prologue through reflection) for every Bitter Brew branch via `VerticalSliceSaveMatrix`, shared `vertical_slice_flow_harness`, and `--filter=test_vertical_slice_save_matrix` (2/2); phase, quest states, forged records, pressures, relationships, equipment, and map world state survive file save/load through `SessionState.replace_state`. Next slice steps: **P2-017** input completion, **P3-001** invalid-state traversal.
Coordination note (2026-07-25 P2-012): full vertical-slice branch traversal now runs headlessly through prologue, investigation, forge commission, night consequence, aftermath commit, and reflection for honest, subtle, and secret Bitter Brew families via `VerticalSliceFlowModel` and `--filter=test_vertical_slice_flow` (2/2). Next slice steps: **P2-016** save/reload matrix, **P2-017** input completion, **P3-001** invalid-state traversal.
Coordination note (2026-07-25 P2-015): quest-tool HUD strip now shows at most three authored `visible_in_pouch` items (hammer, spearhead, crisis forge work) through `QuestPouchModel`, `QuestPouchHud`, and `QuestPouchController` on the player host; forged commission objects count without bag cells. Verified: `--filter=test_quest_pouch` (4/4). Next slice quest step: **P2-012** full vertical-slice flow.
Coordination note (2026-07-25 P2-010): three Bitter Brew aftermath families now commit through `mechanism.bitter_brew_crisis` after the night encounter resolves: honest work shows Aita at the open brewery with Mart's respect dialogue, secret feature boards the yard with Mart emboldened, and subtle defect leaves Jürgen's contract banner with Mart's disgust; `MapPatrolBarkPresenter` surfaces outcome-specific watch barks during consequence and reflection phases. Verified: `--filter=test_bitter_brew_aftermath` (3/3). Next slice quest step: **P2-011** reflection screen.
Coordination note (2026-07-25 P2-009): watch checkpoint night consequence now runs on `reval_east` during `phase.investigation_night` through `BitterBrewNightConsequence`: forged `subtle_defect` unlocks bypass, `secret_feature` unlocks escape, and `honest_work` limits the player to surrender or combat while every route commits `encounter.watch_checkpoint` quest states. Verified: `--filter=test_bitter_brew_night_consequence` (4/4). Next slice quest step: **P2-010** aftermath visibility.
Coordination note (2026-07-25 P2-008): `commission.bitter_brew` now runs in the forge after investigation reaches `investigation_ready`: three forging options (brewery bands, tampered seal, flawed cart lock) commit forged records, outcome flags, and option-specific `object_reveal` feedback text through `BitterBrewCommissionController` and `ForgeFeedbackSequence`; bed rest stays locked until the commission resolves. Verified: `--filter=test_bitter_brew_commission` (6/6). Next slice quest step: **P2-009** night consequence routes.
Coordination note (2026-07-25 P2-007): daytime `A Bitter Brew` investigation now runs on `reval_east` through `BitterBrewInvestigation`: four inspection sites (public well, brewery yard, merchant barrels, watch cistern) record journal facts and advance `quest.bitter_brew` to `investigation_ready` during `phase.investigation_morning`. Verified: `--filter=test_bitter_brew_investigation` (3/3). Next slice quest step: **P2-008** forge commission audiovisual outcomes.
Coordination note (2026-07-25 P2-006): playable Maker's Mark prologue now runs in the forge through `ForgePrologueController`: tutorial hints, watch-buckle commission, branching Henning arrival, chest discovery, three ledger outcomes, and bed rest gated until `ledger_committed`. Verified: `--filter=test_makers_mark_prologue` (3/3), `--filter=test_phase_advance_triggers`, and `--filter=test_demo_walkthrough`.
Coordination note (2026-07-25 P3-018): in-game settings overlay opens on Esc through `GameSettingsController` when no dialogue, commission, inventory, journal, map, reflection, or other modal is active; sliders persist music/SFX volume and dialogue accessibility (text size, speed, contrast, subtitle background, reduced motion) through `UserSettings`. Verified: `--filter=test_game_settings_overlay` (7/7) and `--filter=test_audio_settings` (3/3).
Coordination note (2026-07-25 P1-041): main-menu Load now lists every valid save slot through `SaveService.list_saves()`, opens a modal overlay with phase/timestamp metadata, and resumes the recorded scene/spawn via `DoorNavigator`; the label stays hidden when no saves exist and keyboard focus skips it. Verified: `--filter=test_main_menu_load` (4/4), `--filter=test_save_service` (7/7), and `--filter=test_input_bindings` (9/9).
Coordination note (2026-07-25 P3-017): developer debug overlay is reachable from QuickAccessMenu `Debug` and shows FPS, music/bird/insect/roof-audio telemetry plus pause/slower/faster/reset time controls; comma/period/P dev keys were removed from `MapViewRuntime` in favor of the visual controls. Verified: `--filter=test_debug_overlay` and `--filter=test_quick_access_menu`.
Coordination note (2026-07-26 P2-021a): gameplay-parity scaffold for `kalev_smithy` and `lower_town_slice` now fingerprints 11/11 Lower Town anchors against `lower_town_slice.parity.json`, smithy anchors/transitions in `kalev_smithy.rrmap`, six matched before/after capture paths, and delegates collision/navigation/route proofs to `test_lower_town_slice_map` and `test_kalev_smithy_map` through `tools/verify_map_conversion_parity.py`. Verified: `python3 -m unittest tests.python.test_verify_map_conversion_parity -v` (6/6) and `GODOT_BIN=/Applications/Godot.app/Contents/MacOS/Godot python3 tools/verify_map_conversion_parity.py`. Next parity step: **P2-021** maintainer visual sign-off after **P0-101**.
Coordination note (2026-07-27 P0-131): `MapViewStaticBatcher` now keys merge groups by vertex surface format so indexed thatch ridge poles no longer batch with non-indexed vertex-coloured slope shells; the old merge dropped reed triangles and filled missing `COLOR` attributes with opaque black. Verified: `--filter=test_map_view_static_batcher` (4/4) and `--filter=test_thatch_roof_dressing` (1/1).
Coordination note (2026-07-27 P4-028l): Toompea eastern slope now swaps from the Domberg district playlist to the Danish King's Garden `garden` theme through fade-volume `music_theme` overrides wired by `MapMusicZoneBinder` while the player stands inside `fade.danish_kings_garden`; contract documented in `docs/MAP_AUTHORING.md`. Verified: `--filter=test_map_music_zone_binder` (3/3). Next Toompea step: **P4-025** environment pass.
Coordination note (2026-07-27 P4-028m): Town Hall `raekoda` playlist restored from archive to `music/revel_center/raekoda/` with Git LFS tracking; `tools/test_music_director.gd` now verifies both dedicated Town Hall tracks load without resource errors. Next civic step: **P4-022** central environment pass.
Coordination note (2026-07-27 P4-028k): St. Olaf's Church (Oleviste) interior now ships as developer-only `oleviste_church` with reciprocal transition from Monastery District `st_olaf_silhouette`, bell playlist under `music/revel_north/oleviste/`, and registry coverage in blueprint/catalog/audit manifests. Verified: `--filter=test_oleviste_church_map` (4/4), `--filter=test_oleviste_church` (3/3), updated transition manifest and music-director tests. Next civic step: **P4-007** folklore quest or **P4-022** central environment pass.
Coordination note (2026-07-27 P4-028j): Holy Spirit Church interior now ships as developer-only `holy_spirit_church` with reciprocal transition from Central District `church_silhouette`, hymn playlist under `music/revel_center/holy spirit church/`, and registry coverage in blueprint/catalog/audit manifests. Verified: `--filter=test_holy_spirit_church_map` (4/4), `--filter=test_holy_spirit_church` (4/4), updated transition manifest and music-director tests. Next civic step: **P4-028k** St. Olaf interior or **P4-022** central environment pass.
Coordination note (2026-07-28 P4-010): Act 1 content-budget report now fingerprints three districts, seven core characters, two named faction casts, five day/night cycles, eight substantial quests, one climax, dialogue word cap (3658/12000), and unique district audio duration (8269.6s/9000s) through `docs/data/act1_content_budget_manifest.json`, `tools/report_act1_content_budget.py`, and planned faction-line warnings for **P4-021**. Verified: `python3 tools/report_act1_content_budget.py --check` and `python3 -m unittest tests.python.test_report_act1_content_budget -v` (3/3). Next Act 1 step: **P4-012** maintainer gate.
Coordination note (2026-07-27 P4-011): Act 1 branch traversal suite now fingerprints three act-boundary endings (`seal`, `break`, `open`), five deliberate invalid transitions, six delegated cycle test filters, and three boundary save fixtures through `Act1TraversalModel`, `docs/data/act1_traversal_manifest.json`, and `content/saves/act1_fixtures_manifest.json`. Verified: `--filter=test_act1_traversal` (8/8), `python3 tools/report_act1_traversal.py --check`, and `python3 -m unittest tests.python.test_report_act1_traversal -v`. Next Act 1 step: **P4-010** content-budget report or **P4-012** maintainer gate.
Coordination note (2026-07-27 P4-009): Act 1 transition envelope now aggregates Mart, Aita, Kaja, Henning, Jürgen, Ellen, forge ledger/conviction/pressures, and Lower Town plus North Merchant district tiers when St. George's Night commits; `GameState.act1_transition` persists through save payload for Act 2 migration. Verified: `--filter=test_act1_aftermath` (4/4) and `--filter=test_st_georges_night_cycle` (4/4). Next Act 1 step: **P4-010** content-budget report or **P4-011** branch traversal suite.
Coordination note (2026-07-27 P4-008): **St. George's Night** playable climax now arms `quest.st_georges_night` on `phase.act1_climax`, offers Seal/Break/Open choices at Viru Gate `checkpoint_east`, records `flag.act_boundary.viru_*` plus `flag.act_transition.act1_recorded`, and surfaces aftermath watch barks through `StGeorgesNightAftermath`. Verified: `--filter=test_st_georges_night_cycle` (4/4) and `--filter=test_quest_package_st_georges_night` (2/2). Next Act 1 step: **P4-009** aftermath aggregation or **P4-011** branch traversal suite.
Coordination note (2026-07-27 P4-008a): **St. George's Night** act-boundary branch map and state table now live in `docs/SCENES/st-georges-night.md`, `docs/SCENES/st-georges-night-branch-map.md`, and the `content/packages/st_georges_night/` design package with three act-boundary families (`flag.act_boundary.viru_seal`, `flag.act_boundary.viru_break`, `flag.act_boundary.viru_open`) that set `flag.act_transition.act1_recorded` through `mechanism.st_georges_night_gate` stacking prior `flag.act_climax_viru_*` bias from `quest.bell_and_chain`. Verified: `python3 -m unittest tests.python.test_st_georges_night_branch_map -v`, `python3 tools/verify_quest_packages.py`, and `--filter=test_quest_package_st_georges_night` (2/2). Next Act 1 step: **P4-008** playable climax implementation.
Coordination note (2026-07-27 P4-007): **Root and Ember** folklore cycle now introduces Ellen Luik on Lower Town, forge commission `commission.root_and_ember` with Ember/Root technique paths, and peaceful hearth install via `mechanism.root_and_ember_hearth`; three branches set `flag.ellen.belief_honored`, `flag.ellen.remedy_trusted`, or `flag.ellen.skepticism_respected` without literal magic confirmation. Verified: `--filter=test_root_and_ember_cycle` (4/4) and `--filter=test_quest_package_root_and_ember` (2/2). Next Act 1 step: **P4-008** St. George's Night climax.
Coordination note (2026-07-27 P4-005): **The Price of a Name** branch map and state table now live in `docs/SCENES/the-price-of-a-name.md`, `docs/SCENES/the-price-of-a-name-branch-map.md`, and the `content/packages/price_of_a_name/` design package with three forging branches (`honest_work`, `subtle_defect`, `secret_feature`) that set `flag.mart.name_cleared`, `flag.mart.name_redirected`, or `flag.mart.name_concealed` through relationships, detention, false evidence, and Hingepuu reflection hooks. Verified: `python3 -m unittest tests.python.test_price_of_a_name_branch_map -v`, `python3 tools/verify_quest_packages.py`, and `--filter=test_quest_package_price_of_a_name` (2/2). Next Act 1 step: **P4-006** playable cycle implementation.
Coordination note (2026-07-27 P4-004): **Bread and Iron** playable cycle now wires civic market investigation on Lower Town, forge commission `commission.bread_and_iron`, weighing-house install at `checkpoint_east`, and `mechanism.bread_and_iron_scales` aftermath through `BreadAndIronInvestigation`, `BreadAndIronCommissionController`, `BreadAndIronInstallConsequence`, and `BreadAndIronAftermath`; three forging branches set `flag.family.raide_supplied`, `flag.family.raide_rationed`, or `flag.family.raide_debt`. Verified: `--filter=test_bread_and_iron_cycle` (4/4), `--filter=test_quest_package_bread_and_iron` (2/2), and `python3 -m unittest tests.python.test_bread_and_iron_branch_map -v`. Next Act 1 step: **P4-005** approved branch map for `The Price of a Name`.
Coordination note (2026-07-27 P4-003): **Bread and Iron** branch map and state table now live in `docs/SCENES/bread-and-iron.md`, `docs/SCENES/bread-and-iron-branch-map.md`, and the `content/packages/bread_and_iron/` design package with three forging branches (`honest_work`, `subtle_defect`, `secret_feature`) that set `flag.family.raide_supplied`, `flag.family.raide_rationed`, or `flag.family.raide_debt` through evidence, supplier intake, inventory, and location-state effects. Verified: `python3 -m unittest tests.python.test_bread_and_iron_branch_map -v`, `python3 tools/verify_quest_packages.py`, and `--filter=test_quest_package_bread_and_iron` (2/2). Next Act 1 step: **P4-004** playable cycle implementation.
Coordination note (2026-07-27 P4-002): **The Bell and the Chain** playable cycle now wires investigation sites at Viru Gate, forge commission `commission.bell_and_chain`, curfew install at `checkpoint_east`, and `mechanism.bell_and_chain_gate` aftermath through `BellAndChainInvestigation`, `BellAndChainCommissionController`, `BellAndChainNightConsequence`, and `BellAndChainAftermath`; three forging branches set `flag.act_climax_viru_seal`, `flag.act_climax_viru_break`, or `flag.act_climax_viru_open`. Verified: `--filter=test_bell_and_chain_cycle` (4/4) and `--filter=test_quest_package_bell_and_chain` (2/2). Next Act 1 step: **P4-003** approved branch map for `Bread and Iron` or **P4-004** playable cycle.
Coordination note (2026-07-27 P4-001): **The Bell and the Chain** branch map and state table now live in `docs/SCENES/the-bell-and-the-chain.md`, `docs/SCENES/the-bell-and-the-chain-branch-map.md`, and the `content/packages/bell_and_chain/` design package with three forging branches (`honest_work`, `subtle_defect`, `secret_feature`) that set `flag.act_climax_viru_seal`, `flag.act_climax_viru_break`, or `flag.act_climax_viru_open` for **P4-008**. Verified: `python3 -m unittest tests.python.test_bell_and_chain_branch_map -v`, `python3 tools/verify_quest_packages.py`, and `--filter=test_quest_package_bell_and_chain` (2/2). Next Act 1 step: **P4-002** playable cycle implementation.
Coordination note (2026-07-27 P4-033): Lower Town now authors a visible north-quarter iron convoy on `patrol iron_convoy` through `SupplyChainConvoy` and `SupplyChainController`; players can observe the porter, block the route via `dialogue.supply.iron_convoy`, and hear different merchant aftermath through `dialogue.merchant.iron_shipment` when `quest.iron_shipment` resolves to `delivered` or `disrupted`. Verified: `--filter=test_supply_chain_events` (8/8). Next Act 1 step: **P4-036** environmental consequence visualization.
Coordination note (2026-07-27 P4-035): Act 1 investigative quest `quest.stolen_iron` now authors four clue facts, three suspects, and confrontation dialogue tiers through `InvestigativeQuestModel` plus `journal_evidence` on the quest record. Verified: `--filter=test_investigative_quest` (6/6) and `python3 tools/validate_content.py content/examples/valid content/examples/support`. Next Act 1 step: **P4-033** supply-chain visibility or **P4-036** environmental consequence visualization.
Coordination note (2026-07-27 P4-032): market-day schedule now resolves Wednesday/Saturday from `GameCalendar.weekday_index` through `MarketDayModel`, sets `flag.market_day_active`, toggles expanded `market_stall_turg_*` props via `MapView3D.set_prop_visible`, and wires merchant talk plus crowd bark on Lower Town through `MarketDayController`. Verified: `--filter=test_market_day_events` (5/5). Next Act 1 step: **P4-033** supply-chain visibility or **P4-035** investigative multi-step quests.
Coordination note (2026-07-27 P4-030): district trade prices now resolve for `trade.iron` and `trade.bread` through `TradePriceModel`, dialogue `{trade_price:*}` tokens, and forge commission material charges keyed off smithy district pressure; `loc.kalev_smithy` maps to `district.lower_town` for pricing. Verified: `--filter=test_trade_price_model` (5/5) and `python3 tools/validate_content.py content/examples/valid content/examples/support`. Next Act 1 step: **P4-032** market-day events or **P4-033** supply-chain visibility.
Coordination note (2026-07-27 P4-034): social reputation events now fire once when faction standing crosses authored thresholds through `SocialReputationModel` and `SocialReputationController` on Lower Town; Harju Kings trusted standing (>= 2) sets `flag.reputation.harju_kings_trusted`, presents a crowd cheer bark with SFX, and unlocks merchant gossip via `dialogue.reputation.harju_kings_trusted`. Verified: `--filter=test_social_reputation_events` (4/4). Next Act 1 step: **P4-032** market-day events or **P4-035** investigative multi-step quests.
Coordination note (2026-07-27 P4-031): NPC relationship memory now records discrete `memory.<character>.<action>` keys in `GameState`, exposes `memory_recorded` / `record_memory` content ops, and resolves dialogue `entry_variants` before `start_node_id` through `DialogueEntryResolver`; Mart support dialogue covers helped, betrayed, and ignored openings plus an apology-gated choice. Verified: `--filter=test_relationship_memory` (6/6), updated `--filter=test_state_rule_evaluator` (8/8), and `python3 tools/validate_content.py content/examples/valid content/examples/support`. Next Act 1 step: **P4-029** time-sensitive commissions or **P4-034** social reputation events.
Coordination note (2026-07-26 P4-017): district pressure now resolves per-district patrol speed, price tier, and bark pools from explicit `flag.district.*` flags plus faction ledger standing through `DistrictPressureModel`; `MapPhaseBinder` applies patrol speed scales and content gains `district_pressure_at_least` / `district_price_tier_at_least` gates. Verified: `--filter=test_district_pressure_model` (4/4), `--filter=test_phase_transitions` (13/13), and `--filter=test_state_rule_evaluator` (8/8). Next Act 1 step: **P4-029** time-sensitive commissions or **P4-031** NPC relationship memory.
Coordination note (2026-07-26 P4-016a): slice quest outcomes now write faction ledger events through content-only `record_faction_event` effects on Maker's Mark ledger transitions and Bitter Brew forging options; journal Factions tab reflects real play instead of test fixtures. Verified: `--filter=test_faction_ledger` (8/8), `--filter=test_makers_mark_prologue` (3/3), and `--filter=test_bitter_brew_commission` (6/6). Next Act 1 step: **P4-017** district pressure states.
Coordination note (2026-07-26 P4-016): eight-faction ledger foundation is closed through `FactionLedger`, `GameState.record_faction_event()`, `faction_standing_at_least` / `record_faction_event` content ops, and a Factions tab on the journal overlay; standing is derived only from explicit events with no aggregate morality field. Verified: `--filter=test_faction_ledger` (6/6) and updated `--filter=test_state_rule_evaluator` (8/8). Next Act 1 step: **P4-016a** wire slice quest outcomes to ledger events, then **P4-017** district pressure states.
Coordination note (2026-07-26 P4-028f): `south_quarter` now authors P2-025 district-life props at Rataskaev well (`wash_tub`, buckets), the knights' court (`weapon_rack`, Livonian banner, service barrels), the Karja gate service yard (`charcoal_pile`), and swordsmith lane (`iron_scrap_pile`); patrol walkability stays green. Verified: `--filter=test_south_quarter_prototype_map` (2/2) and `--filter=test_market_prototype_maps` (18/18). Next south-quarter step: **P4-028i** stable horses and **P4-024** environment pass.
Coordination note (2026-07-26 P4-028g): `toompea_quarter` now authors Danish-crown banners beside the castle and cathedral close, herb/wash terrace clutter south of St Mary's, and `farm_cart` stable yards; `archbishops_garden` adds a kitchen herb rack, renames the orchard hay store, and keeps timber orchard fences with a gardener cart beside `orchard_store`. Verified: `--filter=test_toompea_quarter_prototype_map` passes 5/5 and `--filter=test_archbishops_garden_prototype_map` passes 5/5 including patrol walkability. Next Toompea step: **P4-025** environment pass or **P4-028f** south-quarter dressing.
Coordination note (2026-07-26 P4-028d): `monastery_quarter` service-life dressing now authors P2-025 trade props at the convent well (`herb_drying_rack`, `wash_tub`, `market_goods_pallet`), guild and Pikk merchant stalls, workshop rows (`boat_timber_stack`, service `farm_cart`), and the convent almonry cart while retiring generic `service_barrels` on Lai. Verified: `--filter=test_monastery_quarter_prototype_map` passes 6/6 including service-life dressing and patrol walkability. Next monastery step: **P4-023b** ordinary fabric pass or **P4-023c** St Olaf exceptional landmark.
Coordination note (2026-07-26 P4-028e): `market_civic_quarter` stall row now carries P2-025 trade props - `fish_splitting_table`, `sail_cloth_bale`, `malt_sack_pile`, and `cargo_crates` beside the four named stalls; `civic_well` apron adds `market_goods_pallet` and `wash_tub` while the weigh table and notice cart stay in place; generic `market_barrels` retired on the square. Verified: `--filter=test_market_prototype_maps` passes 18/18 including stall-life dressing and patrol walkability. Next civic step: **P4-022** central environment pass or **P4-019** activation when P0-040 lands.
Coordination note (2026-07-26 P4-028b): `reval_harbor_north` merchant landing now uses P2-025 trade props beside cargo sheds and the ropewalk - `cargo_crates` and `trade_goods` in west and mid yards, `trade_goods` on tally ground, and paired `rope_coil` tallies replacing barrel stand-ins; the quay crane cart and offshore `merchant_boat` roadstead scenery stay unchanged. Verified: `--filter=test_reval_harbor_map` passes 12/12 including merchant-landing dressing, patrol walkability, and gate-to-shore route checks. Next harbour step: **P4-026** harbour environment pass.
Coordination note (2026-07-26 P4-028c): `north_quarter` workshop rows now carry P2-025 trade props beside matching building IDs - `sail_cloth_bale` at `sailmakers_loft`, `cooper_staves` at `cooperage_shed`, `rope_coil` at `ropemakers_shed`, and `market_goods_pallet` in west/east warehouse courts; generic barrel/crate stand-ins were retired where dedicated kinds exist. Verified: `--filter=test_north_quarter` passes 7/7 including workshop-life dressing and patrol walkability. Next merchant step: **P4-028e** central market/civic dressing or **P4-020** north-quarter activation when P0-040 lands.
Coordination note (2026-07-26 P4-023g): `north_quarter` merchant pens now spawn cattle and sheep through `MapViewPennedFauna` with static `timber_fence` dressing retained on the map; six authored placements stay west of the Pikk patrol corridor with no collision or `GameState` writes. Verified: `--filter=test_map_view_penned_fauna` (15/15) and `--filter=test_north_quarter_prototype_map` (6/6). Next merchant step: **P4-028c** workshop-life dressing.
Coordination note (2026-07-26 P0-108): `viru_gate_foreland` now closes the rural perimeter pass with named P0-115 bush species on road and river margins, crop scatter on west-bank field strips (`crop.rye`, `crop.barley`, `crop.flax`), and regression tests that wolf/brown-bear actors stay outside the Viru road patrol corridor. Verified: `--filter=test_viru_gate_foreland_map` (11/11), `--filter=test_map_view_penned_fauna` (11/11), and `python3 tools/verify_map_composition.py`. Next rural step: **P4-023g** merchant-district cattle/sheep actors.
Coordination note (2026-07-26 P0-106): penned livestock and wild-margin mammals now spawn on `lower_town_slice` and `viru_gate_foreland` through `MapViewPennedFauna` with pen, tether, and flee behaviors, a ten-actor cap, and no `GameState` writes. Verified: `--filter=test_map_view_penned_fauna` (10/10) and `python3 tools/verify_asset_lint.py`. Next fauna step: **P0-108** foreland perimeter pass and **P4-023g** merchant-district cattle/sheep actors.
Coordination note (2026-07-26 P2-024): urban ambient mammals now spawn on `lower_town_slice` through `MapViewUrbanFauna` with eight authored placements for cats, dogs, horses, and rats; tether, wander, idle, and flee behaviors stay visual-only with an eight-actor cap and no `GameState` writes. Verified: `--filter=test_map_view_urban_fauna` (7/7) and `python3 tools/verify_asset_lint.py`. Next fauna step: **P0-106** penned livestock actors.
Coordination note (2026-07-25 P0-118): ambient mammal model kit is closed at 30/30 through `MapViewMammalSpecies` and `MapViewMammalMeshes` with twelve silhouette groups, three cached poses per species, and district spawn-weight stubs; reference sheet lives at `docs/reports/images/fauna/p0_118_mammal_reference_sheet.png`. Verified: `--filter=test_map_view_mammal_species` (3/3) and `python3 tools/verify_asset_lint.py`. Next fauna step: **P2-024** urban runtime actors, then **P0-106** penned livestock behavior.
Coordination note (2026-07-24 P0-103 / P0-114): Estonian tree species kit is closed at 20/20 through `MapViewTreeSpecies`, `MapViewTreeMeshes`, and `TerrainVegetation`; shared constraint 9 in `docs/HISTORICAL_AUDIT.md` documents mixed, orchard, hedgerow, wetland-margin, and coastal-alvar authoring weights; reference sheet lives at `docs/reports/images/fauna/p0_103_tree_reference_sheet.png` via `tools/capture_tree_reference_sheet.gd`. Verified: `--filter=test_map_view_tree_species` (11/11) and `python3 tools/verify_asset_lint.py`. Next flora step: **P0-115** Baltic shrub kit.
Coordination note (2026-07-24 P1-022): forge commissions now run a five-beat narrative feedback sequence (`heat`, `hammer_rhythm`, `quench`, `maker_stamp`, `object_reveal`) through `ForgeFeedbackOverlay` before committing forged records; no temperature, strike-accuracy, or timing-score state is exposed. Verified: `--filter=test_forge_feedback` (4/4) and `--filter=test_forge_commission` (13/13). Next slice quest step: **P2-008** `A Bitter Brew` commission audiovisual outcomes.
Coordination note (2026-07-24 P0-105): ambient bird flight and song integration is closed on outdoor maps through `MapViewBirdAmbientAudio` and `MapViewBirdFlight` in `MapViewRuntime`, with district contexts for Lower Town, harbour, and foreland, night-margin owl scheduling, three-voice / four-actor caps, and performance telemetry from **P0-127**. Verified: `--filter=test_map_view_bird_ambient_audio` (8/8), `--filter=test_map_view_bird_flight` (7/7), and `tools/run_performance_report.sh build/benchmarks/performance-smoke.json --quick`. Next ambient-life step: **P2-024** urban mammals and **P0-106** penned livestock.
Coordination note (2026-07-24 P0-127): foreland bird context now has regression coverage in ambient audio and flight suites (`viru_gate_foreland` schedules at least three distinct song cues and gliding species); Lower Town quick performance report records `bird_audio_peak` and `bird_flight_peak` within the authored three-voice / four-actor caps. Verified: `--filter=test_map_view_bird_ambient_audio` (7/7), `--filter=test_map_view_bird_flight` (7/7), and `tools/run_performance_report.sh build/benchmarks/performance-smoke.json --quick`. Next ambient-life step: **P0-105** integration after **P0-100** / **P2-024** land.
Coordination note (2026-07-24 P0-105b): district-context gliding bird actors now cross outdoor maps through `MapViewBirdFlight` with a four-actor cap, deterministic species selection from `MapViewBirdSpecies` spawn weights, and interior suppression when exterior surroundings are hidden. Verified: `--filter=test_map_view_bird_flight` (6/6). Next ambient-life step: **P0-105** flock paths and performance budget on Lower Town quick report.
Coordination note (2026-07-24 P0-105a): district-context ambient bird song playback now schedules positional 3D calls through `MapViewBirdAmbientAudio` on outdoor maps with a three-voice cap, deterministic species selection from `MapViewBirdSpecies` spawn weights, and interior suppression when exterior weather is hidden. Verified: `--filter=test_map_view_bird_ambient_audio` (6/6). Next ambient-audio step: **P0-105** flight meshes and visible bird actors.
Coordination note (2026-07-24 P0-124): enclosed-interior rain-on-roof audio now plays through `SkyWeatherRoofAudio` while `SkyWeather3D.rain_suppressed` is true; the 48 s `sounds/weather/rain_roof.mp3` loop is synthesized in-house (AGPL project author) and crossfades with `rain_intensity()`. Verified: `python3 tools/verify_weather_audio_clips.py`, `python3 -m unittest tests.python.test_verify_weather_audio_clips -v`, `--filter=test_weather_audio_clips` (3/3), and the roof-audio assertions in `--filter=test_sky_weather_3d`. Next ambient-audio step: **P0-105** runtime bird flight and song playback.
Coordination note (2026-07-24 P0-075 defect): lunar path no longer reuses the sun's declination arc. `SkyWeather3D` now applies a ~5.14° orbital inclination with a draconic latitude term, exposes `sun_moon_separation_degrees()`, and the sky shader fades near-sun crescents in daytime solar glare so new-moon disks do not read as stuck behind the sun. The lunar albedo disk now aligns to celestial north with a corrected V flip so the NASA near-side mosaic keeps familiar maria orientation as the moon crosses the sky. Verified: `--filter=test_sky_weather_3d` (30/30). Roof-drum rain audio closed under **P0-124**.
Coordination note (2026-07-24 P0-123): P0-122 raw bird takes now process into one canonical ``call.mp3`` / ``song.mp3`` per catalog cue under ``sounds/birds/<species>/`` with high-pass and loudness normalisation; ``tools/audio/process_bird_clips.py``, ``tools/verify_bird_audio_clips.py``, and ``MapViewBirdSpecies.stream_path_for_cue()`` resolve every cue to an imported ``AudioStream``; ``python3 tools/audio/sync_bird_sources.py`` keeps ``assets/SOURCES.csv`` aligned with raw and processed clips. Verified: ``python3 tools/verify_bird_audio_clips.py``, ``python3 -m unittest tests.python.test_process_bird_clips -v``, and ``--filter=test_bird_audio_clips`` (1/1). Next ambient-audio step: **P0-105** runtime bird flight and song playback.
Coordination note (2026-07-24 P4-028h): Viru Gate foreland travel-apron dressing now authors `road_inn` beside the Viru road with barrels, hay, scrub margins, and pack-horse clutter while retaining every P0-107 rural-life kind; harbour and fishing-shore props stay absent. Verified: `--filter=test_viru_gate_foreland_map` (9/9). Next rural step: **P0-108** perimeter pass and **P0-106** penned livestock actors.
Coordination note (2026-07-24 P0-107): rural-life static prop kit landed with ten peri-urban kinds (`kitchen_garden`, `field_strip`, `hay_wagon`, `pasture_fence`, `pigsty`, `chicken_run`, `flax_drying_frame`, `root_cellar_mound`, `orchard_row`, `farm_cart`) wired through `MapTypes.RURAL_LIFE_PROP_KINDS`, 2D/3D mesh builders, and `docs/MAP_AUTHORING.md`; `viru_gate_foreland` now authors every kind for foreland reads. Verified: `--filter=test_rural_life_prop_kinds` (4/4) and `--filter=test_viru_gate_foreland_map` (8/8). Next rural step: **P4-028h** foreland dressing and **P0-106** penned livestock actors.
Coordination note (2026-07-24 P0-122e): replaced P0-122b procedural gap calls for `great_cormorant` and `white_tailed_eagle` with north-European iNaturalist field takes (CC0 Friedrichshafen cormorant, CC BY Brandenburg eagle); `fetch_bird_songs.py` supports `source: inaturalist`; `python3 tools/audio/verify_curated_bird_recordings.py`, `python3 tools/verify_bird_audio_manifest.py`, and `python3 tools/verify_bird_audio_clips.py` pass. Follow-up: **P0-122f** Estonian/Baltic permission-based swaps when loodusheli.ee or PlutoF grants land.
Coordination note (2026-07-24 P0-122b): maintainer-recorded interim procedural calls now replace the Wikimedia cormorant and `H. leucogaster` eagle stand-in via `tools/audio/generate_gap_bird_clips.py` (CC0, `source: maintainer`); `python3 tools/audio/verify_curated_bird_recordings.py`, `python3 tools/verify_bird_audio_manifest.py`, and `python3 tools/verify_bird_audio_clips.py` pass. Next audio step: **P0-122e** Baltic field replacements when permission or recording lands.
Coordination note (2026-07-24 P0-126): repaired broken local Markdown links in `docs/lore/four_kings_act2_lore.md` (typoed `LANDMARK_NARRATIVE_INTEGRATIVE.md`, table-row anchors that do not exist in district-heading docs, and a missing `history/HISTORY.md` anchor). Verified: `python3 tools/generate_active_docs_report.py --check` reports zero issues.
Coordination note (2026-07-24 P0-122c): freesound.org scan (CC0 / CC BY, 15-90 s) found no clip that satisfies a Baltic field-take bar for `great_cormorant` or `white_tailed_eagle`; interim maintainer calls closed under **P0-122b**; **P0-122e** tracks real Baltic replacements.
Coordination note (2026-07-24 sky weather): lunar orbit no longer reuses the solar declination arc - `lunar_declination_degrees()` applies the ~5.14 deg orbital inclination and the sky shader fades near-sun crescents in solar glare so the prologue waning crescent does not read as stuck behind the sun; `--filter=test_sky_weather_3d` passes 30/30.
Coordination note (2026-07-23 P0-122): thirty commercial-friendly bird clips now ship under `sounds/birds/` with `tools/audio/curated_bird_recordings.json`, `tools/verify_bird_audio_manifest.py`, and CI verification; `great_cormorant` and `white_tailed_eagle` use documented gap fills until **P0-122b**.
Coordination note (2026-07-23 P1-039): `tools/generate_quest_package_tests.py` accepts `--skip-failing` so one broken package does not block regeneration for the rest of the corpus during authoring; `python3 -m unittest tests.python.test_generate_quest_package_tests -v` passes 8/8.

Maintainer decision: environmental realism is the top production priority after **P0-078**.
Sky/weather (**P0-075**) is accepted; do not reopen unless fixing a defect.

Delivery order remains strict: playable demo, vertical-slice MVP, Act 1, Act 2, Act 3.
Demo packaging (**D-003** through **D-004c**) is closed.

Player-facing discoverability is part of every feature's definition of done.
Any new player action, screen, or overlay must add a visible quick-access or contextual entry point;
a hotkey alone is not sufficient.

Coordination note (2026-07-22 P4-023a / P4-023e): Monastery District street/surface pass and guild-row facade correction are closed on `monastery_quarter`. Packed-earth base, cobble spines, and mud lanes pass `--filter=test_monastery_quarter_prototype_map` (5/5); `great_guild_front`, `blackheads_corner`, and `brotherhood_wing` now use generic merchant plaster masses with stable IDs while later Great Guild / Blackheads monuments stay excluded per `docs/HISTORICAL_AUDIT.md`. Next monastery steps: **P4-023b** ordinary fabric, **P4-023c** / **P4-023d** exceptional landmarks.
Coordination note (2026-07-20 evening re-audit, committed `HEAD` `606a6e5`): the morning audit's trust findings (13 masked `SCRIPT ERROR` exceptions behind 482 "passes", 2 Python contract failures, failing content corpora, stale report, audit/matrix drift) are resolved history - P0-061 made the harness fail on runtime exceptions, and P0-062/P0-062a/P0-063 cleared the errors and reconciled the validators. Verified on this HEAD: `python3 -m unittest discover -s tests/python`, `verify_map_audit.py`, `verify_migration_matrix.py`, `verify_map_conversion_plan.py`, `verify_map_activation.py`, and `generate_active_docs_report.py --check` all pass, and `--filter=test_character_rig` passes 15/15. The macOS export evidence (414 MiB `rr.dmg`) still proves package generation only, not packaged play - that remains D-004.
Coordination note (2026-07-20 P0-062b): restored multi-street walkable patrols on `north_quarter`, `south_quarter`, and `toompea_quarter` after the P0-062a spawn-adjacent temporary loops. Open baseline packaging step remains D-004; reviewable chunk-boundary warnings stay under P0-067a.
Coordination note (2026-07-22 D-004b): README-embedded animated capture from release-binary walkthrough frames is linked and reproducible via `tools/verify_packaged_demo.sh` and `tools/build_demo_walkthrough_gif.sh`; Start, Mart talk, and spearhead pickup into the open bag are visible in `docs/reports/images/demo_walkthrough/demo_walkthrough.gif`.
Coordination note (2026-07-20 D-004 closeout): packaged demo gate closed. Verified: `--filter=test_demo_walkthrough` (2/2), `tools/verify_packaged_demo.sh` export + packaged launch, and frame walkthrough linked from `README.md` / `docs/reports/demo_walkthrough_d004.md`. Follow-ups: D-004a (release-only defect notes) and optional D-004b (human video capture).
Coordination note (2026-07-20 D-004a): no release-only move-talk-pickup defects remain. Packaged `--quit-after` launch is green without `--path`; `verify_packaged_demo.sh` no longer probes release binaries with path overrides; triage in `docs/reports/d004a_release_only_triage.md`. Optional follow-ups: D-004b (human video), D-004c (in-binary packaged walkthrough).
Coordination note (2026-07-21 D-004c): packaged in-binary move-talk-pickup automation landed. The extracted release app starts from its real main menu, proves movement, completes Mart dialogue, picks up the anvil spearhead through runtime `Interactable` paths, prints `D-004C_PACKAGED_WALKTHROUGH_PASS`, and exits 0 without editor, `--path`, or scene overrides.
Coordination note (2026-07-20 P1-031): district/world map overlay on every `player.tscn` host; `toggle_world_map` (M) plus quick-access `Districts [M]`; graph nodes/edges match `DoorNavigator` active manifest; `--filter=test_world_map_overlay` passes 5/5. Optional click-to-travel is **P1-031a**.
Coordination note (2026-07-20 P0-063a): active Markdown discovery now prunes hidden/tool-cache directories before classifying active or excluded docs; `.pytest_cache/README.md` regression coverage is part of Python discovery, and the committed report stays stable when pytest creates its cache.
Coordination note (2026-07-20 P1-031a in progress): mouse click on a connected district-map neighbor travels through `DoorNavigator.go_to_scene` with the edge's registered destination spawn; current-scene node stays non-interactive; unknown/disconnected nodes do not travel.
Coordination note (2026-07-20 P1-031a): district-map click-to-travel landed; neighbor clicks resolve the authored edge spawn via `WorldMapGraph.resolve_travel_spawn` and call `DoorNavigator.go_to_scene`; current-scene / unknown / disconnected nodes stay non-traveling; `--filter=test_world_map_overlay` passes 7/7.
Coordination note (2026-07-20 P1-031b in progress): gamepad/keyboard focus navigation on district-map neighbor nodes; Confirm/ui_accept travels the focused connected district; current-scene node stays non-focusable and non-interactive.
Coordination note (2026-07-20 P1-031b): district-map keyboard/gamepad focus travel landed; open seeds focus on a travelable neighbor, arrow/D-pad neighbors are wired spatially, Confirm/`ui_accept` uses the same `resolve_travel_spawn` contract as click; current-scene node stays `FOCUS_NONE`; `--filter=test_world_map_overlay` passes 8/8.
Coordination note (2026-07-20 P1-031c): focused travelable district nodes now use a thick warm focus outline distinct from their unfocused style; the current-scene gold highlight stays unchanged; `--filter=test_world_map_overlay` passes 8/8.
Coordination note (2026-07-21 P1-028): persistent keyboard/mouse and gamepad rebinding now covers all 18 shipped slice actions; the Controls overlay captures mouse inputs before GUI consumption, provides deterministic two-column focus navigation, and replacement input events complete keyboard/mouse and gamepad focus/confirm flows.
Coordination note (2026-07-20 P0-067a): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on `north_quarter`, `south_quarter`, `toompea_quarter`, and `reval_harbor` are owned in `docs/reports/map_chunk_boundary_review_p0_067a.md` with ADR 0010 ownership; audit still prints the warnings. Slice-map follow-up is **P0-067b**.
Coordination note (2026-07-20 P0-067b): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on playable slice maps `kalev_smithy` and `lower_town_slice`, plus registry leftover `market_civic_quarter`, are owned in `docs/reports/map_chunk_boundary_review_p0_067b.md` with ADR 0010 ownership; audit still prints the warnings. Object chunk streaming on slice maps stays non-production until **P0-067c**.
Coordination note (2026-07-21 P0-067c): object chunk streaming is production-ready on `kalev_smithy` and `lower_town_slice` at 32x32 cells; the executable ownership fixture locks all 69 retained warning records, exclusions remain baked/static, transitions persistent, and full-load/parity/route checks pass. Prototype-map activation remains separately gated.
Coordination note (2026-07-21 P0-037): shared low-poly rig production gate closed. Kalev and swap-only variants share the retargeted skeleton and canonical clips at the frozen 64 px scale; strict focused verification passes 16/16. An isolated Innkeeper rebuild plus verification took 17.42 seconds, canonical `pickup` integration took 21 seconds, and the reproducible procedure/evidence is in `docs/reports/character_rig_production_p0_037.md`.
Coordination note (2026-07-20 P0-069): restored exact map conversion-plan coverage for `scenes/tests/night_encounter_stub.tscn` as a developer-only retained test scene; the scene inventory records the same partial integration host and its reconciled counts. Python discovery and the standalone conversion-plan validator pass.
Coordination note (2026-07-21 P0-064): storage ownership policy and CI validator landed; generated `bin/rr.zip`, benchmark JSON, and Python bytecode are removed/ignored. Nine existing standard-Git binaries at least 10 MiB are checksum-locked pending authenticated LFS/external migration in **P0-070**. Existing quarantine classification drift is owned by **P0-071**.
Coordination note (2026-07-21 P0-071): active visual-source quarantine drift is closed. Unknown-rights NPC/tower sprites and sidecars are mirrored under `quarantine/`; runtime uses the existing NPC atlas and an internal procedural turret; the deterministic greybox floor has complete provenance; CI enforces zero active unapproved media exceptions.
Coordination note (2026-07-22 P0-070): Git LFS migration closeout is complete. Verified on current `HEAD`: `python3 tools/manage_lfs_assets.py verify` (110 objects / 569,387,736 bytes), `python3 tools/verify_storage_hygiene.py`, `python3 tools/validate_asset_sources.py`, `python3 tools/quarantine_assets.py --check`, `python3 -m unittest discover -s tests/python -v` (92/92), `python3 tools/generate_active_docs_report.py --check`, and the full Godot headless suite exit 0. Pruned 241 stale `assets/SOURCES.csv` rows left after the P0-030 import-path prune so provenance validation matches on-disk assets; `docs/storage_binary_exceptions.json` remains empty.
Coordination note (2026-07-21 P0-078): clean-clone baseline triage assigns Lower Town wall-walk/parity drift to the already-active **P0-076**; the post-P0-076 follow-up owns the remaining direction-sign runtime errors, rebound mouse-confirm failure, prototype route/adjacency failures, banner geometry regression, and generated fixture UID cleanup required before P0-070 can satisfy its literal green-test exit contract.
Coordination note (2026-07-22 P0-076): tower wall-walk passage wiring is complete on current `HEAD`: typed RRMap fields compile and validate, eight Lower Town round towers expose authored visual/collision openings, builder call sites receive map bounds, and canonical parity stays stable. Verified in a clean detached worktree with `test_map_view_3d_fortification` (7/7), `test_map_tower_doors` (6/6), `test_map_rrmap_parser` (11/11), and map audit (11 registered/discovered, 0 errors, no unknown typed fields). The remaining smithy entrance/courtyard baseline failures are explicitly owned by **P0-078**.
Coordination note (2026-07-22 P0-072 in progress): research and executable coverage now match all eleven retained registry blueprints, including `viru_gate_foreland`, with separate confidence-labelled vegetation-species, garden/agricultural-use, and fauna bands backed by archaeobotanical and zooarchaeological sources. `tools/verify_historical_dossier.py` enforces registry/category/source coverage and prevents P0-072/P1-036 closeout while review is pending. Maintainer sign-off remains **P0-111** per ADR 0014.
Coordination note (2026-07-21 P1-028 in progress): auditing every vertical-slice input action and adding persistent keyboard/mouse plus gamepad rebinding with focus-safe settings navigation and automated coverage.
Coordination note (2026-07-21 P0-073): Archbishop's Garden map-audit/conversion baseline is reconciled without release activation; its scene target, explicit `active=false` gate, scene inventory, migration matrix, conversion plan, and canonical 1600x900 audit capture now agree, and all four contract checks pass.
Coordination note (2026-07-21 P0-075): sky/weather compile regression closed. `_advance_gust()` restores the rain-front gust envelope; weather profiles push `storm_intensity` and wind-scaled cloud drift into the sky shader; `--filter=test_sky_weather_3d` passes 22/22 with zero script compile errors.
Coordination note (2026-07-22 P0-078 in progress): direction-sign contract drift from the Lower Town `.rrmap` migration is closed on current work: tests now assert the four authored signs (`viru gate and eastern road`, `to town centre`, `to knights district`, `karja gate`) and `--filter=test_direction_sign_3d` is green. Rebound mouse-confirm focus is green under `--filter=test_input_bindings`. Smithy entrance/courtyard bucket is closed: `smithy_door_transition` now carries `building_id=kalev_smithy` with `transition_visual=door`, and `viru_gate_south_jamb` shifts east so the courtyard apron stays walkable at the tested cells; `--filter=test_lower_town_slice_map` passes 14/14 and the parity fixture is refreshed. Remaining P0-078 buckets: prototype route/adjacency drift, banner geometry, and generated fixture UID cleanup.
Coordination note (2026-07-22 P0-078 active implementation): selected as the highest-priority dependency-free high-complexity task on current `HEAD`; running the required clean import and complete Godot suite first, then fixing every remaining failure by root cause without weakening assertions.
Coordination note (2026-07-22 P0-078): clean-clone Godot baseline is green. Verified: `tools/run_godot_checked.sh clean-import` and `--require-test-summary full-suite` pass 742/742 tests with zero failures. Closed the remaining prototype adjacency/patrol drift (harbor north/east reciprocal edges, Toompea and Kalamaja patrols), Viru Gate arch/jamb geometry, species-aware woodland density assertions, rebound mouse `ui_accept` focus flow in `UserSettings`, and detached-runtime `MusicDirector` lookup in `MapViewRuntime`. **P0-070** closeout is unblocked.
Coordination note (2026-07-22 P2-011): Hingepuu reflection overlay landed with `ReflectionModel`, `ReflectionOverlay`, and `ReflectionController` on the player host. Reflection morning auto-opens when `flag.reflection.completed` is false; quick-access exposes a mouse-reachable `Reflect` button; Duty/Fury/Mercy each apply one allowlisted effect; visual consequence marks and a plain-text summary mirror prior forged records and pressures; `--filter=test_reflection_overlay` passes 5/5.
Coordination note (2026-07-22 P0-120): `tools/update_todo_counts.py` now reports separate P3-P6 act-band rows in the TODO quick-reference table instead of a single collapsed P3+ bucket; suffix IDs such as `P4-027a` bucket correctly; `python3 -m unittest tests.python.test_update_todo_counts -v` passes 4/4.
Coordination note (2026-07-22 P0-080): `tools/update_todo_counts.py` regenerates the TODO quick-reference table from task rows, including suffix IDs such as `D-004a` and `P1-031a`; `python3 -m unittest tests.python.test_update_todo_counts -v` passes and the summary table is refreshed.
Coordination note (2026-07-22 P0-079): procedural sky resource baking (cloud noise, lunar albedo, star catalog, rain particles) now lives in `SkyWeatherResources`; `SkyWeather3D` keeps weather simulation and astronomy facades. `--filter=test_sky_weather_3d` passes 27/27 and `--filter=test_map_view_3d_lighting` passes 12/12 with zero behavior regressions.
Coordination note (2026-07-22 P0-081): all ten P1-037/P1-037a Estonia `world_travel` developer wrappers are reconciled with map audit, migration matrix, outdoor-prototype policy, and `assets/UI/estonia_world_map.png` C2PA provenance while every destination stays `release=false`; `MapAuditRegistry` executes every `world.*` definition and audit captures exist for each wrapper.
Coordination note (2026-07-21 P0-074): Viru Gate Foreland is reconciled as an `active=false` / `release=false` prototype across conversion plan, scene inventory, migration matrix, audit manifest, and the existing 1600x900 capture. Map discovery now ignores `.a2gent-worktrees` so local agent mirrors cannot fake inventory drift.
Coordination note (2026-07-22 P0-077): depth-layered harbour water already landed in `7b0f7e6` and is independently closed on current `HEAD`. A clean detached worktree import followed by the strict runner passes `test_coastal_sea_3d` 6/6, `test_map_view_3d_core` 15/15, and `test_map_view_3d_lighting` 11/11 with zero unallowlisted diagnostics or generated untracked files. The committed 1280x720 harbour capture records readable shallow bed detail against dark deep water.
Coordination note (2026-07-21 P1-035): manual load and debug presets now share one ordered `SessionState.replace_state()` path. `state_replaced(previous, current, reason)` emits once after the new bag is bound to `ContentDB`; phase, world-item, 3D equipment, inventory, journal, forge/dialogue, and interaction consumers rebind, and focused load/debug verification passes 2/2 (related state/phase/dialogue/world suites pass 68/68).
Coordination note (2026-07-21 P1-030): repeatable Lower Town performance reporting landed on the real production scene. `tools/run_performance_report.sh` records frame-time distributions, static/delta memory, authoritative actor count, detected host data, and a separate declared target-hardware profile; generated JSON remains ignored. Focused tests pass 3/3 and the quick report contract passes. P3-011 still owns selection and acceptance of minimum hardware.
Coordination note (2026-07-21 P1-034): `WorldMapOverlay` is now a 230-line public facade over focused `WorldMapLocalView` and `WorldMapFastTravelView` children; stable node names and public map/travel APIs remain intact, and only `WorldMapController` calls `DoorNavigator`. `--filter=test_world_map_overlay` passes 9/9 and `--filter=test_quick_access_menu` passes 5/5.
Coordination note (2026-07-22 P1-037a): extended the Estonia global-map graph with six campaign traversal placeholders - Rebel Kings' Camp, Kanavere Bog, Sõjamäe, Paide Castle, Pärnu, and Pöide Castle - with reciprocal roads/ferries, manifest spawns, and conversion-plan inventory coverage; all remain `release=false`. See `docs/reports/global_map_mockups.md`.
Coordination note (2026-07-22 P1-037): three map layers are now Local map, District map, and Estonia map. The `release=false` Estonia mockup graph includes Sacred Grove, Harju, Padise, Saaremaa, Rebel Kings' Camp, Kanavere, Sõjamäe, Paide, Pärnu, and Pöide; reciprocal roads/ferries connect neighboring sites while Reval remains linked through its south/east/west/harbour gates. The Estonia basemap runtime copy lives at `assets/UI/estonia_world_map.png`; see `docs/reports/global_map_mockups.md` for selection and route decisions.
Coordination note (2026-07-21 district-life planning): Kalamaja (`reval_harbor_east`) still uses `barrels` placeholders for `drying_nets_*` props; harbour research in `docs/reports/reval_harbour_1343_research.md` already names net yards, fish sheds, smoke sheds, and boatwright ground but the 3D vocabulary does not. District-life work is split: **P2-025** shared static prop kinds and mesh builders, **P2-026** Lower Town slice dressing, and **P4-028a** through **P4-028h** per-district specialization passes after the relevant environment tasks.
Coordination note (2026-07-20 P1-024c): shared health/death/guard/parry/dodge resolution landed in `CombatVitals` for player and combat actors; `--filter=test_combat_vitals` passes 10/10. Next combat step is **P1-024d** (Iron technique).
Coordination note (2026-07-20 P1-024d): Iron forge technique layers onto AttackProfile / CombatVitals - equipped Iron jams braced guards into open hits (parry and dodge still win); `--filter=test_forge_technique_iron` passes 5/5. Next combat step is **P1-024** (integrated combat room). Technique equip UI discoverability is **P1-024e**.
Coordination note (2026-07-20 P1-024): integrated combat test room at `scenes/tests/combat_room.tscn` with readable `CombatFeedbackHud`, hammer light/charged, guard/parry, dodge, Iron via quick-access, and keyboard/mouse/gamepad coverage in `--filter=test_combat_room`. Next combat step is **P1-025** (watchman/sergeant enemy state machine).
Coordination note (2026-07-20 P1-025 in progress): shared `EnemyCombatStateMachine` for watchman/sergeant archetypes (patrol → detect → telegraph → attack → react → disengage). Scene wiring stays **P1-025a**.
Coordination note (2026-07-20 P1-025): shared watchman/sergeant `EnemyCombatStateMachine` landed; `--filter=test_enemy_combat_state_machine` passes 8/8. Next combat step is **P1-025a** (wire archetypes into the combat room / encounter stub).
Coordination note (2026-07-20 P1-025a in progress): wiring watchman/sergeant into `scenes/tests/combat_room.tscn` with readable detect/telegraph/attack feedback; keep P1-024 dummies as the player-attack smoke host.
Coordination note (2026-07-20 P1-025a): watchman/sergeant wired into `scenes/tests/combat_room.tscn` via `CombatRoomEnemy`; readable detect/telegraph/attack feedback; `--filter=test_combat_room` passes 8/8. Next combat step is **P1-026** (surrender/escape/bypass outcomes).
Coordination note (2026-07-20 P1-026): authored surrender/escape/bypass outcomes via `EncounterOutcomeResolver`; non-lethal closes force-disengage without killing and write the same `GameState` quest keys as `kill`; combat-room mouse buttons; `--filter=test_encounter_outcome` passes 5/5. Next combat step is **P1-027** (retry checkpoint after failure).
Coordination note (2026-07-20 P1-027 in progress): combat failure retry via `EncounterCheckpoint` - arm GameState snapshot at encounter start, restore on death without clearing completed dialogue or prior quest progress; combat-room Retry button.
Coordination note (2026-07-20 P1-027): `EncounterCheckpoint` arms a GameState payload at encounter start; player death exposes a mouse-reachable Retry control that restores dialogue/quest without writing a false outcome; `--filter=test_encounter_checkpoint` passes 4/4. Night-scene wiring stays **P1-027a**.
Coordination note (2026-07-20 P1-025b in progress): optional night-encounter stub outside `combat_room` that reuses `CombatRoomEnemy` / `EnemyCombatStateMachine` for both archetypes so P2-009 / P1-027a can host a watch route without forking AI; keep unreachable from release demo navigation.
Coordination note (2026-07-20 P1-025b): night-encounter stub at `scenes/tests/night_encounter_stub.tscn` reuses `CombatRoomEnemy` / `EnemyCombatStateMachine`; `--filter=test_night_encounter_stub` passes 5/5 (both archetypes detect-to-disengage; unreachable from release demo navigation). Next combat step is **P1-027a** (checkpoint retry on the stub) or **P1-026a** (content-authored outcome package).
Coordination note (2026-07-20 P1-027a in progress): wire `EncounterCheckpoint` into `scenes/tests/night_encounter_stub.tscn` so player death outside the combat room exposes the same mouse-reachable Retry path; keep the stub unreachable from release demo navigation.
Coordination note (2026-07-20 P1-027a): night stub arms `EncounterCheckpoint` at reset, player death shows mouse-reachable Retry, restore keeps pre-fight dialogue/quest; `--filter=test_night_encounter_stub` passes 6/6. Next combat step is **P1-026a** (content-authored outcome package).
Coordination note (2026-07-20 P1-026a): `encounter.watch_checkpoint` + `quest.bitter_brew` package loads through ContentDB; schema/validator wired; combat room resolves from content; `--filter=test_encounter_outcome` passes 7/7 (escape-from-content + unknown-kind rejection). Follow-up **P1-026b** wires the same content package into the night stub UI for P2-009.
Coordination note (2026-07-20 P1-026b in progress): mouse-reachable surrender/escape/bypass on `scenes/tests/night_encounter_stub.tscn` resolving via ContentDB `encounter.watch_checkpoint` (same quest contract as the combat room); keep the stub unreachable from release demo navigation.
Coordination note (2026-07-20 P1-026b): night stub wires ContentDB `encounter.watch_checkpoint` surrender/escape/bypass mouse controls; `--filter=test_night_encounter_stub` passes 8/8 (escape-from-content + unknown-kind rejection; still unreachable from release demo). Next combat/slice step is **P2-009** (night consequence host) once forge/investigation deps land; optional discoverability polish for outcome buttons in quick-access stays out of scope for the stub.
Coordination note (2026-07-21 monastery planning): `monastery_quarter` had a map-wide cobblestone base that violated `docs/HISTORICAL_AUDIT.md` / `docs/MAP_AUTHORING.md` street-surface rules. Prototype base is now packed earth with cobble limited to Pikk/Lai/civic/vene spines, mud secondary lanes, and compact stone closes. Full district quality remains split: **P4-023a** streets/surfaces, **P4-023b** ordinary fabric, **P4-023c/d** St Olaf and St Michael exceptional models, **P4-023e** later guild/Blackheads facade correction. Revised **P4-023** covers North only.
Coordination note (2026-07-22 environmental realism priority): maintainer decision - environmental realism is the top production priority after **P0-078** baseline green. The sky/weather cycle (**P0-075**) is accepted; do not reopen unless fixing a defect. Prioritize historically correct streets, building fabric, and landmark silhouettes over slice quest authoring until Lower Town passes the **P2-021** composition gate. Living-world work is ordered: place correctness (**P0-072**, **P2-022**, **P2-023**), surfaces (**P0-053**, **P2-003**), vegetation (**P2-027**, **P2-028**), daily-life dressing (**P2-025**, **P2-026**, **P2-030**), then fauna and birds (**P2-024**, **P2-031**, **P2-029**), then district rollout (**P4-022** through **P4-026**, **P4-028***, **P4-029**).
Coordination note (2026-07-20 P1-024e): quick-access `IronTechniqueButton` equips and clears Iron via mouse; `GameState.equipped_forge_technique()` and attack-profile guard-pierce update; `--filter=test_quick_access_menu` passes 5/5. Next combat step is **P1-024** (integrated combat room).

## Archived planning notes

Delivery order remains strict: playable demo, vertical-slice MVP, Act 1, Act 2, Act 3. D-004 packaging is closed; resume visual acceptance (P0-053 / P0-037) and vertical-slice systems without waiting on further packaging work. Prototype and slice chunk-boundary ownership reviews are closed under P0-067a / P0-067b; production chunk-streaming readiness remains **P0-067c**.
Player-facing discoverability is part of every feature's definition of done. Any new player action, screen, or overlay must add a visible entry point to the persistent quick-access menu or an equally visible contextual prompt; a hotkey alone is not sufficient. Its verification must cover both mouse activation and the documented shortcut so players and developers can find shipped functionality without reading source code.
### 1. Stabilize ownership and make verification honest (blocks all feature work)
1. **P0-061 / P0-062 / P0-062a / P0-063** - **Done 2026-07-20.** Harness fails on runtime exceptions; named fixtures and full-suite exit are green; content/Python/map-audit/matrix/docs gates and discovery CI are restored. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
2. **P0-060** - **Done 2026-07-20.** Stale DEF-003/004/005 retired; DEF-001/002/006 retained with fresh repro; README/AGENTS status matches D-003 done / D-004 open.
3. **P0-066** - **Done 2026-07-20** (`606a6e5`). Shared-rig gait overrides and regenerated NPC bodies landed; `--filter=test_character_rig` passes 15/15.
4. **P0-067** - **Done 2026-07-20.** District-adjacency/terrain mesh polish and procedural mesh reuse landed green; `run_map_pipeline_ci.sh audit` and `--filter=test_map_view_3d_core` pass. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
5. **P0-062b** - **Done 2026-07-20.** Prototype watch routes again cover Pikk/Lai/guild, King's/Karja/Viru, and Lühike/Pikk Jalg/south-slope corridors with walkable segments and multi-street span assertions.
6. **P0-067a** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for the four inactive/dev-gated prototypes; activation still blocked until split or re-assert. Slice maps continue under **P0-067b**.
7. **P0-067b** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for `kalev_smithy`, `lower_town_slice`, and leftover `market_civic_quarter`. Object chunk streaming on slice maps stays non-production until **P0-067c**.
Required exit condition for the packaging gate: clean clone import, startup smokes, the full Godot suite, Python discovery, content validation, map pipeline, docs validation, and export all complete with zero unallowlisted errors. Exit code `0` alone is not sufficient.
### 2. Freeze release scope and make status documentation accurate
1. **P0-060** - **Done.** Re-triaged known runtime defects and updated README/AGENTS status claims.
2. **P0-064 and P0-065** - record repository-storage and architecture cleanup, but do not let broad refactoring delay D-004 unless it blocks a clean clone or reliable test run.
### 3. Finish and package the demo
- **D-003 is complete:** movement, Mart conversation, anvil spearhead pickup, bag UI, and session re-entry exist on the current slice.
- **D-004 is complete:** `tools/verify_packaged_demo.sh` exports the macOS `rr` build, proves packaged launch, runs the headless move-talk-pickup proof, and links the frame walkthrough from `README.md`.
- **D-004a is complete:** no release-only move-talk-pickup defects remain; see `docs/reports/d004a_release_only_triage.md`. Optional: D-004b human video; D-004c in-binary packaged walkthrough.
### 4. Close the visual acceptance gate and rebuild Reval environments in evidence-first order
Maintainer priority (2026-07-22): realism of place and daily life over slice quest content. Weather is frozen at **P0-075**; do not spend cycles on sky/weather unless fixing a regression.
1. **P0-072** - first establish sourced, map-specific 1343 Reval targets for street layout, property density, ordinary and exceptional architecture, relief, vegetation species mixes, agricultural land use, domestic and wild fauna ranges, and landmark states; do not encode the current cobblestone coverage as the historical baseline.
2. **P0-053** - finish the surface/weathering kit; P0-037 shared-rig production evidence is complete.
3. **P0-038, then P0-040** - measure the candidate and freeze ART_BIBLE v2 with maintainer sign-off before broader production art ([ADR 0013](adr/0013-authorial-visual-direction-without-blind-ux-panels.md)).
4. **P2-003, P1-029, and P1-036** - after the art gate, build and lint the historically grounded modular environment kit and enforce the sourced composition ranges with a map-quality audit.
5. **P2-022, then P2-023** - highest player-visible realism pass on the playable Lower Town: historically correct streets and plot pattern, dense property footprints, period architecture and materials, relief, and sparse plausible vegetation. **P2-021** visual parity follows these passes.
6. **P2-027, then P2-028** - replace decorative tree silhouettes with species-accurate Estonian trees, then expand ground-cover to at least twenty historically plausible grasses, herbs, and meadow plants for urban margins and open ground.
7. **P2-025 and P2-026** - land the shared district-life static prop kit, then dress the playable Lower Town slice so yards and street frontages read as specific trades without NPC actors.
8. **P2-030** - rural and agricultural static-life kit for forelands, garden plots, hay meadows, pastures, and peri-urban margins so life outside the wall reads as 1343 Estonian countryside rather than empty filler.
9. **P2-024, then P2-031** - urban ambient fauna (cats, dogs, horses, rats) first; then domestic stock (chickens, ducks, geese, pigs, cows) and cautious wild margins (hares, foxes, wolves, bears) with flee/tether behavior and no navigation blocking.
10. **P2-029** - ambient bird flight and song after core layout and fauna budgets are stable.
11. **P4-022 through P4-026** - apply the same sourced quality bar district by district after slice approval. Central, then North (**P4-023**) and Monastery (**P4-023a** through **P4-023e**), precede their activation gates; south, Toompea/garden, and harbor remain inactive prototypes unless separately approved.
12. **P4-028a through P4-028h** and **P4-029** - per-district static life dressing, then foreland/rural perimeter passes using the agricultural and fauna kits.
### 5. Resume vertical-slice systems only after the environment baseline is credible
1. **P1-025 through P1-027** - add enemies, non-lethal outcomes, and retry state (enemy state machine is **P1-025**, done; room wiring **P1-025a** is done; night stub **P1-025b** is done; outcomes **P1-026** are done; retry checkpoint **P1-027** is done; night-scene retry wiring **P1-027a** is done; content outcome package **P1-026a** is done; night-stub outcome UI from content **P1-026b** is done).
2. **P1-022** follows P0-040; **P1-031** district map overlay is done; click-to-travel polish is **P1-031a** (done); keyboard/gamepad focus travel is **P1-031b** (done).
3. Then build the authored slice in order: P2-006 tutorial, P2-007 investigation, P2-008 forge choice, P2-009 night consequence, P2-010 aftermath, P2-011 reflection, P2-012 end-to-end slice. Slice quest authoring (**P2-006** through **P2-012**) stays below **P2-021** environment realism until Lower Town reads as historically grounded Reval rather than a gameplay greybox.
