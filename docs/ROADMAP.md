# Production roadmap

Coordination notes, delivery order, and planning context formerly embedded in `TODO.md`.
Executable open work stays in [`TODO.md`](../TODO.md).

## Current focus (2026-07-23)

1. **P0-072** - sourced 1343 Reval environment dossier per retained map (structural closeout complete; maintainer sign-off closed under **P0-111** on 2026-07-24).
Coordination note (2026-07-23 P0-053 closeout): slice surface kit is closed. `tools/verify_slice_surface_captures.py` now enforces 1280x720 day/night evidence for `kalev_smithy` and `lower_town_slice`, checks non-blank detail, and requires day frames to read brighter than night; `python3 tools/verify_slice_surface_captures.py` and `python3 -m unittest tests.python.test_verify_slice_surface_captures -v` pass 5/5 alongside the existing asset lint and Godot wiring/capture suites. Next surface-dependent step: **P0-040** maintainer ART_BIBLE v2 freeze.
2. **P0-040** - maintainer freeze of ART_BIBLE v2 after P0-038 technical baseline ([ADR 0013](adr/0013-authorial-visual-direction-without-blind-ux-panels.md) cancelled blind UX panels P0-039/P0-122; [ADR 0014](adr/0014-authorial-acceptance-gates-without-external-playtests.md) cancelled external playtest gates P3-003/P3-004/P3-006 and rewrote slice/act acceptance).
Coordination note (2026-07-23 ADR 0014): maintainer confirmed authorial acceptance workflow. External playtest rounds (P3-003, P3-004, P3-006) and independent-historian gate on P0-072 are cancelled. Slice and act gates (P3-014, P4-012, P5-010, P6-007) now use maintainer playable review plus automated traversal; P0-111 is maintainer sign-off only.
3. **P2-003 / P1-029 / P1-036** - modular environment kit, asset lint, composition audit.
Coordination note (2026-07-24 P1-036): `MapCompositionAudit`, `docs/data/map_composition_thresholds.json`, `tools/verify_map_composition.py`, and `tools/audit_map_composition.gd` now enforce signed P0-072 bands on `kalev_smithy`, `monastery_quarter`, `viru_gate_foreland`, and `town_hall`; remaining registry maps keep threshold cards with `enforce=false` until their district environment passes land. Verified: `python3 tools/verify_map_composition.py`, `python3 -m unittest tests.python.test_verify_map_composition -v`, and `tools/run_composition_tests.gd` (6/6).
4. **P2-022 / P2-023 / P2-021** - Lower Town realism, landmark pass, parity gate.
5. **P2-025 through P2-031** - district-life dressing, vegetation, fauna.
6. Slice quest authoring (**P2-006** through **P2-012**) stays below environment realism until Lower Town reads as historically grounded Reval.

Coordination note (2026-07-23 P0-121): `tools/release_candidate_check.py` now automates the repository-side release-candidate scaffold for AGPL license, asset provenance manifest, macOS export preset, clean-clone CI indicators, and the P1-028 accessibility baseline; `python3 tools/release_candidate_check.py` and `python3 -m unittest tests.python.test_release_candidate_check -v` pass 8/8. Full P3-012 / P4-013 / P6-008 gates still require maintainer playable review, accessibility checklist, and campaign acceptance evidence per ADR 0014.

Coordination note (2026-07-23 P0-053): coastal harbour equilibrium tide now follows the lunar calendar through `SkyWeather3D.tide_level`, updates coastal water shaders via `MapViewMaterials.apply_coastal_tide`, and stays isolated from rivers. Procedural thatch roofs use slope-oriented reed relief, recessed timber gable infill, and a dedicated reed UV pattern distinct from field straw. Per-building wall and roof materials now derive unique pattern seeds and one of four weathering bands (fresh, worn, damp, repaired) from each stable building ID so adjacent houses no longer share one baked albedo map. Verified: `--filter=test_coastal_sea_3d` (7/7), `--filter=test_sky_weather_3d` (28/28), `--filter=test_map_view_3d_lighting` (13/13), `--filter=test_thatch_roof_dressing` (1/1), `--filter=test_building_surface_weathering` (4/4), and `--filter=test_enemy_combat_state_machine` extended for P5-008 knight/crossbowman archetypes.
Coordination note (2026-07-22 P5-008): Order knight and crossbowman archetypes now resolve through `EnemyArchetype.from_id()` and reuse `EnemyCombatStateMachine`; mission allies use `MissionAllyScript` plus `MissionAllyController` for scripted in-range support without party-control UI; `--filter=test_enemy_archetype_p5_008` passes 7/7.
Coordination note (2026-07-22 P4-018 / P1-038): quest package manifests, branch maps, and landmark beat bindings now live under `content/packages/`; `tools/verify_quest_packages.py` and `tools/generate_quest_package_tests.py --check` validate beat IDs against `docs/data/landmark_integrations.json` and emit Godot traversal tests under `tests/godot/generated/`; the `act1_south_quarter_probe` example passes content validation and `--filter=test_quest_package_act1_south_quarter_probe` (2/2).
Coordination note (2026-07-24 P0-111): maintainer signed all five human-review rows in `docs/HISTORICAL_AUDIT.md` per ADR 0014; `python3 tools/verify_historical_dossier.py` now reports `human review signed`. P1-036 and district quality passes may enforce the signed ranges; world-travel wrapper maps remain outside the thirteen-card urban dossier scope.
Coordination note (2026-07-24 P1-029): `tools/verify_asset_lint.py` now enforces style-lock texture dimensions and seamless edges, character scale contract constants, runtime character glb presence and naming, equipment prop grip mounts, portrait dimensions when files exist, and provenance rows for lint-scoped assets; `python3 tools/verify_asset_lint.py` and `python3 -m unittest tests.python.test_verify_asset_lint -v` pass 10/10. Next composition gate: district environment passes that flip `enforce=true` on their threshold cards.
Coordination note (2026-07-24 P0-115): Baltic shrub kit is closed at 20/20 through `MapViewBushSpecies`, `MapViewBushMeshes`, and species-batched scatter in `map_view_mesh_builder_scatter.gd`; legacy `bush.dense` / `bush.scrub` remain weighted group aliases; shrub bands documented in `docs/HISTORICAL_AUDIT.md` shared constraint 9. Verified: `--filter=test_map_view_bush_species`. Next fauna step: **P2-024** urban mammals and **P0-106** penned livestock actors.
Coordination note (2026-07-25 P2-014): vertical-slice soundtrack budget now inventories every release theme track through `docs/data/slice_soundtrack_manifest.json`; gameplay themes (`forge`, `town`) ship three unique approved tracks totaling 359.8 s under the 720 s cap while the menu loop stays budget-exempt; `python3 tools/report_slice_soundtrack.py --check` and `python3 -m unittest tests.python.test_report_slice_soundtrack -v` pass 6/6. Next slice polish step: **P2-017** input completion.
Coordination note (2026-07-25 P2-016): vertical-slice save/reload matrix now round-trips ten named checkpoints (prologue through reflection) for every Bitter Brew branch via `VerticalSliceSaveMatrix`, shared `vertical_slice_flow_harness`, and `--filter=test_vertical_slice_save_matrix` (2/2); phase, quest states, forged records, pressures, relationships, equipment, and map world state survive file save/load through `SessionState.replace_state`. Next slice steps: **P2-017** input completion, **P3-001** invalid-state traversal.
Coordination note (2026-07-25 P2-012): full vertical-slice branch traversal now runs headlessly through prologue, investigation, forge commission, night consequence, aftermath commit, and reflection for honest, subtle, and secret Bitter Brew families via `VerticalSliceFlowModel` and `--filter=test_vertical_slice_flow` (2/2). Next slice steps: **P2-016** save/reload matrix, **P2-017** input completion, **P3-001** invalid-state traversal.
Coordination note (2026-07-25 P2-015): quest-tool HUD strip now shows at most three authored `visible_in_pouch` items (hammer, spearhead, crisis forge work) through `QuestPouchModel`, `QuestPouchHud`, and `QuestPouchController` on the player host; forged commission objects count without bag cells. Verified: `--filter=test_quest_pouch` (4/4). Next slice quest step: **P2-012** full vertical-slice flow.
Coordination note (2026-07-25 P2-010): three Bitter Brew aftermath families now commit through `mechanism.bitter_brew_crisis` after the night encounter resolves: honest work shows Aita at the open brewery with Mart's respect dialogue, secret feature boards the yard with Mart emboldened, and subtle defect leaves Jürgen's contract banner with Mart's disgust; `MapPatrolBarkPresenter` surfaces outcome-specific watch barks during consequence and reflection phases. Verified: `--filter=test_bitter_brew_aftermath` (3/3). Next slice quest step: **P2-011** reflection screen.
Coordination note (2026-07-25 P2-009): watch checkpoint night consequence now runs on `reval_east` during `phase.investigation_night` through `BitterBrewNightConsequence`: forged `subtle_defect` unlocks bypass, `secret_feature` unlocks escape, and `honest_work` limits the player to surrender or combat while every route commits `encounter.watch_checkpoint` quest states. Verified: `--filter=test_bitter_brew_night_consequence` (4/4). Next slice quest step: **P2-010** aftermath visibility.
Coordination note (2026-07-25 P2-008): `commission.bitter_brew` now runs in the forge after investigation reaches `investigation_ready`: three forging options (brewery bands, tampered seal, flawed cart lock) commit forged records, outcome flags, and option-specific `object_reveal` feedback text through `BitterBrewCommissionController` and `ForgeFeedbackSequence`; bed rest stays locked until the commission resolves. Verified: `--filter=test_bitter_brew_commission` (6/6). Next slice quest step: **P2-009** night consequence routes.
Coordination note (2026-07-25 P2-007): daytime `A Bitter Brew` investigation now runs on `reval_east` through `BitterBrewInvestigation`: four inspection sites (public well, brewery yard, merchant barrels, watch cistern) record journal facts and advance `quest.bitter_brew` to `investigation_ready` during `phase.investigation_morning`. Verified: `--filter=test_bitter_brew_investigation` (3/3). Next slice quest step: **P2-008** forge commission audiovisual outcomes.
Coordination note (2026-07-25 P2-006): playable Maker's Mark prologue now runs in the forge through `ForgePrologueController`: tutorial hints, watch-buckle commission, branching Henning arrival, chest discovery, three ledger outcomes, and bed rest gated until `ledger_committed`. Verified: `--filter=test_makers_mark_prologue` (3/3), `--filter=test_phase_advance_triggers`, and `--filter=test_demo_walkthrough`.
Coordination note (2026-07-25 P3-018): in-game settings overlay opens on Esc through `GameSettingsController` when no dialogue, commission, inventory, journal, map, reflection, or other modal is active; sliders persist music/SFX volume and dialogue accessibility (text size, speed, contrast, subtitle background, reduced motion) through `UserSettings`. Verified: `--filter=test_game_settings_overlay` (7/7) and `--filter=test_audio_settings` (3/3).
Coordination note (2026-07-25 P1-041): main-menu Load now lists every valid save slot through `SaveService.list_saves()`, opens a modal overlay with phase/timestamp metadata, and resumes the recorded scene/spawn via `DoorNavigator`; the label stays hidden when no saves exist and keyboard focus skips it. Verified: `--filter=test_main_menu_load` (4/4), `--filter=test_save_service` (7/7), and `--filter=test_input_bindings` (9/9).
Coordination note (2026-07-25 P3-017): developer debug overlay is reachable from QuickAccessMenu `Debug` and shows FPS, music/bird/insect/roof-audio telemetry plus pause/slower/faster/reset time controls; comma/period/P dev keys were removed from `MapViewRuntime` in favor of the visual controls. Verified: `--filter=test_debug_overlay` and `--filter=test_quick_access_menu`.
Coordination note (2026-07-25 P0-118): ambient mammal model kit is closed at 30/30 through `MapViewMammalSpecies` and `MapViewMammalMeshes` with twelve silhouette groups, three cached poses per species, and district spawn-weight stubs; reference sheet lives at `docs/reports/images/fauna/p0_118_mammal_reference_sheet.png`. Verified: `--filter=test_map_view_mammal_species` (3/3) and `python3 tools/verify_asset_lint.py`. Next fauna step: **P2-024** urban runtime actors, then **P0-106** penned livestock behavior.
Coordination note (2026-07-24 P0-103 / P0-114): Estonian tree species kit is closed at 20/20 through `MapViewTreeSpecies`, `MapViewTreeMeshes`, and `TerrainVegetation`; shared constraint 9 in `docs/HISTORICAL_AUDIT.md` documents mixed, orchard, hedgerow, wetland-margin, and coastal-alvar authoring weights; reference sheet lives at `docs/reports/images/fauna/p0_103_tree_reference_sheet.png` via `tools/capture_tree_reference_sheet.gd`. Verified: `--filter=test_map_view_tree_species` (11/11) and `python3 tools/verify_asset_lint.py`. Next flora step: **P0-115** Baltic shrub kit.
Coordination note (2026-07-24 P1-022): forge commissions now run a five-beat narrative feedback sequence (`heat`, `hammer_rhythm`, `quench`, `maker_stamp`, `object_reveal`) through `ForgeFeedbackOverlay` before committing forged records; no temperature, strike-accuracy, or timing-score state is exposed. Verified: `--filter=test_forge_feedback` (4/4) and `--filter=test_forge_commission` (13/13). Next slice quest step: **P2-008** `A Bitter Brew` commission audiovisual outcomes.
Coordination note (2026-07-24 P0-105): ambient bird flight and song integration is closed on outdoor maps through `MapViewBirdAmbientAudio` and `MapViewBirdFlight` in `MapViewRuntime`, with district contexts for Lower Town, harbour, and foreland, night-margin owl scheduling, three-voice / four-actor caps, and performance telemetry from **P0-127**. Verified: `--filter=test_map_view_bird_ambient_audio` (8/8), `--filter=test_map_view_bird_flight` (7/7), and `tools/run_performance_report.sh build/benchmarks/performance-smoke.json --quick`. Next ambient-life step: **P2-024** urban mammals and **P0-106** penned livestock.
Coordination note (2026-07-24 P0-127): foreland bird context now has regression coverage in ambient audio and flight suites (`viru_gate_foreland` schedules at least three distinct song cues and gliding species); Lower Town quick performance report records `bird_audio_peak` and `bird_flight_peak` within the authored three-voice / four-actor caps. Verified: `--filter=test_map_view_bird_ambient_audio` (7/7), `--filter=test_map_view_bird_flight` (7/7), and `tools/run_performance_report.sh build/benchmarks/performance-smoke.json --quick`. Next ambient-life step: **P0-105** integration after **P0-100** / **P2-024** land.
Coordination note (2026-07-24 P0-105b): district-context gliding bird actors now cross outdoor maps through `MapViewBirdFlight` with a four-actor cap, deterministic species selection from `MapViewBirdSpecies` spawn weights, and interior suppression when exterior surroundings are hidden. Verified: `--filter=test_map_view_bird_flight` (6/6). Next ambient-life step: **P0-105** flock paths and performance budget on Lower Town quick report.
Coordination note (2026-07-24 P0-105a): district-context ambient bird song playback now schedules positional 3D calls through `MapViewBirdAmbientAudio` on outdoor maps with a three-voice cap, deterministic species selection from `MapViewBirdSpecies` spawn weights, and interior suppression when exterior weather is hidden. Verified: `--filter=test_map_view_bird_ambient_audio` (6/6). Next ambient-audio step: **P0-105** flight meshes and visible bird actors.
Coordination note (2026-07-24 P0-124): enclosed-interior rain-on-roof audio now plays through `SkyWeatherRoofAudio` while `SkyWeather3D.rain_suppressed` is true; the 48 s `sounds/weather/rain_roof.mp3` loop is synthesized in-house (AGPL project author) and crossfades with `rain_intensity()`. Verified: `python3 tools/verify_weather_audio_clips.py`, `python3 -m unittest tests.python.test_verify_weather_audio_clips -v`, `--filter=test_weather_audio_clips` (3/3), and the roof-audio assertions in `--filter=test_sky_weather_3d`. Next ambient-audio step: **P0-105** runtime bird flight and song playback.
Coordination note (2026-07-24 P0-075 defect): lunar path no longer reuses the sun's declination arc. `SkyWeather3D` now applies a ~5.14° orbital inclination with a draconic latitude term, exposes `sun_moon_separation_degrees()`, and the sky shader fades near-sun crescents in daytime solar glare so new-moon disks do not read as stuck behind the sun. The lunar albedo disk now aligns to celestial north with a corrected V flip so the NASA near-side mosaic keeps familiar maria orientation as the moon crosses the sky. Verified: `--filter=test_sky_weather_3d` (30/30). Roof-drum rain audio closed under **P0-124**.
Coordination note (2026-07-24 P0-123): P0-122 raw bird takes now process into one canonical ``call.mp3`` / ``song.mp3`` per catalog cue under ``sounds/birds/<species>/`` with high-pass and loudness normalisation; ``tools/audio/process_bird_clips.py``, ``tools/verify_bird_audio_clips.py``, and ``MapViewBirdSpecies.stream_path_for_cue()`` resolve every cue to an imported ``AudioStream``; ``python3 tools/audio/sync_bird_sources.py`` keeps ``assets/SOURCES.csv`` aligned with raw and processed clips. Verified: ``python3 tools/verify_bird_audio_clips.py``, ``python3 -m unittest tests.python.test_process_bird_clips -v``, and ``--filter=test_bird_audio_clips`` (1/1). Next ambient-audio step: **P0-105** runtime bird flight and song playback.
Coordination note (2026-07-24 P4-028h): Viru Gate foreland travel-apron dressing now authors `road_inn` beside the Viru road with barrels, hay, scrub margins, and pack-horse clutter while retaining every P0-107 rural-life kind; harbour and fishing-shore props stay absent. Verified: `--filter=test_viru_gate_foreland_map` (9/9). Next rural step: **P0-108** perimeter pass and **P0-106** penned livestock actors.
Coordination note (2026-07-24 P0-107): rural-life static prop kit landed with ten peri-urban kinds (`kitchen_garden`, `field_strip`, `hay_wagon`, `pasture_fence`, `pigsty`, `chicken_run`, `flax_drying_frame`, `root_cellar_mound`, `orchard_row`, `farm_cart`) wired through `MapTypes.RURAL_LIFE_PROP_KINDS`, 2D/3D mesh builders, and `docs/MAP_AUTHORING.md`; `viru_gate_foreland` now authors every kind for foreland reads. Verified: `--filter=test_rural_life_prop_kinds` (4/4) and `--filter=test_viru_gate_foreland_map` (8/8). Next rural step: **P4-028h** foreland dressing and **P0-106** penned livestock actors.
Coordination note (2026-07-24 P0-122e): replaced P0-122b procedural gap calls for `great_cormorant` and `white_tailed_eagle` with north-European iNaturalist field takes (CC0 Friedrichshafen cormorant, CC BY Brandenburg eagle); `fetch_bird_songs.py` supports `source: inaturalist`; `python3 tools/audio/verify_curated_bird_recordings.py`, `python3 tools/verify_bird_audio_manifest.py`, and `python3 tools/verify_bird_audio_clips.py` pass. Follow-up: **P0-122f** Estonian/Baltic permission-based swaps when loodusheli.ee or PlutoF grants land.
Coordination note (2026-07-24 P0-122b): maintainer-recorded interim procedural calls now replace the Wikimedia cormorant and `H. leucogaster` eagle stand-in via `tools/audio/generate_gap_bird_clips.py` (CC0, `source: maintainer`); `python3 tools/audio/verify_curated_bird_recordings.py`, `python3 tools/verify_bird_audio_manifest.py`, and `python3 tools/verify_bird_audio_clips.py` pass. Next audio step: **P0-122e** Baltic field replacements when permission or recording lands.
Coordination note (2026-07-24 P0-126): repaired broken local Markdown links in `docs/lore/four_kings_act2_lore.md` (typoed `LANDMARK_NARRATIVE_INTEGRATIVE.md`, table-row anchors that do not exist in district-heading docs, and a missing `history/HISTORY.md` anchor). Verified: `python3 tools/generate_active_docs_report.py --check` reports zero issues.
Coordination note (2026-07-24 P0-122c): freesound.org scan (CC0 / CC BY, 15-90 s) found no clip that satisfies a Baltic field-take bar for `great_cormorant` or `white_tailed_eagle`; interim maintainer calls closed under **P0-122b**; **P0-122e** tracks real Baltic replacements.
Coordination note (2026-07-24 sky weather): lunar orbit no longer reuses the solar declination arc - `lunar_declination_degrees()` applies the ~5.14 deg orbital inclination and the sky shader fades near-sun crescents in solar glare so the prologue waning crescent does not read as stuck behind the sun; `--filter=test_sky_weather_3d` passes 30/30.
Coordination note (2026-07-23 P0-122): thirty commercial-friendly bird clips now ship under `sounds/birds/` with `tools/audio/curated_bird_recordings.json`, `tools/verify_bird_audio_manifest.py`, and CI verification; `great_cormorant` and `white_tailed_eagle` use documented gap fills until **P0-122b**.
Coordination note (2026-07-23 P1-039): `tools/generate_quest_package_tests.py` accepts `--skip-failing` so one broken package does not block regeneration for the rest of the corpus during authoring; `python3 -m unittest tests.python.test_generate_quest_package_tests -v` passes 8/8.

Maintainer decision: environmental realism is the top production priority after **P0-078**.
Sky/weather (**P0-075**) is accepted; do not reopen unless fixing a defect.

Delivery order remains strict: playable demo, vertical-slice MVP, Act 1, Act 2, Act 3.
Demo packaging (**D-003** through **D-004c**) is closed.

Player-facing discoverability is part of every feature's definition of done.
Any new player action, screen, or overlay must add a visible quick-access or contextual entry point;
a hotkey alone is not sufficient.

## Coordination history

Coordination note (2026-07-22 P4-023a / P4-023e): Monastery District street/surface pass and guild-row facade correction are closed on `monastery_quarter`. Packed-earth base, cobble spines, and mud lanes pass `--filter=test_monastery_quarter_prototype_map` (5/5); `great_guild_front`, `blackheads_corner`, and `brotherhood_wing` now use generic merchant plaster masses with stable IDs while later Great Guild / Blackheads monuments stay excluded per `docs/HISTORICAL_AUDIT.md`. Next monastery steps: **P4-023b** ordinary fabric, **P4-023c** / **P4-023d** exceptional landmarks.
Coordination note (2026-07-20 evening re-audit, committed `HEAD` `606a6e5`): the morning audit's trust findings (13 masked `SCRIPT ERROR` exceptions behind 482 "passes", 2 Python contract failures, failing content corpora, stale report, audit/matrix drift) are resolved history - P0-061 made the harness fail on runtime exceptions, and P0-062/P0-062a/P0-063 cleared the errors and reconciled the validators. Verified on this HEAD: `python3 -m unittest discover -s tests/python`, `verify_map_audit.py`, `verify_migration_matrix.py`, `verify_map_conversion_plan.py`, `verify_map_activation.py`, and `generate_active_docs_report.py --check` all pass, and `--filter=test_character_rig` passes 15/15. The macOS export evidence (414 MiB `rr.dmg`) still proves package generation only, not packaged play - that remains D-004.
Coordination note (2026-07-20 P0-062b): restored multi-street walkable patrols on `north_quarter`, `south_quarter`, and `toompea_quarter` after the P0-062a spawn-adjacent temporary loops. Open baseline packaging step remains D-004; reviewable chunk-boundary warnings stay under P0-067a.
Coordination note (2026-07-22 D-004b): README-embedded animated capture from release-binary walkthrough frames is linked and reproducible via `tools/verify_packaged_demo.sh` and `tools/build_demo_walkthrough_gif.sh`; Start, Mart talk, and spearhead pickup into the open bag are visible in `docs/reports/images/demo_walkthrough/demo_walkthrough.gif`.
Coordination note (2026-07-20 D-004 closeout): packaged demo gate closed. Verified: `--filter=test_demo_walkthrough` (2/2), `tools/verify_packaged_demo.sh` export + packaged launch, and frame walkthrough linked from `README.md` / `docs/reports/demo_walkthrough_d004.md`. Follow-ups: D-004a (release-only defect notes) and optional D-004b (human video capture).
Coordination note (2026-07-20 D-004a): no release-only move-talk-pickup defects remain. Packaged `--quit-after` launch is green without `--path`; `verify_packaged_demo.sh` no longer probes release binaries with path overrides; triage in `docs/reports/d004a_release_only_triage.md`. Optional follow-ups: D-004b (human video), D-004c (in-binary packaged walkthrough).
Coordination note (2026-07-21 D-004c): packaged in-binary move-talk-pickup automation landed. The extracted release app starts from its real main menu, proves movement, completes Mart dialogue, picks up the anvil spearhead through runtime `Interactable` paths, prints `D-004C_PACKAGED_WALKTHROUGH_PASS`, and exits 0 without editor, `--path`, or scene overrides.
Coordination note (2026-07-20 P1-031): district/world map overlay on every `player.tscn` host; `toggle_world_map` (M) plus quick-access `Districts [M]`; graph nodes/edges match `DoorNavigator` active manifest; `--filter=test_world_map_overlay` passes 5/5. Optional click-to-travel is **P1-031a**.
Coordination note (2026-07-20 P0-063a): active Markdown discovery now prunes hidden/tool-cache directories before classifying active or excluded docs; `.pytest_cache/README.md` regression coverage is part of Python discovery, and the committed report stays stable when pytest creates its cache.
Coordination note (2026-07-20 P1-031a in progress): mouse click on a connected district-map neighbor travels through `DoorNavigator.go_to_scene` with the edge's registered destination spawn; current-scene node stays non-interactive; unknown/disconnected nodes do not travel.
Coordination note (2026-07-20 P1-031a): district-map click-to-travel landed; neighbor clicks resolve the authored edge spawn via `WorldMapGraph.resolve_travel_spawn` and call `DoorNavigator.go_to_scene`; current-scene / unknown / disconnected nodes stay non-traveling; `--filter=test_world_map_overlay` passes 7/7.
Coordination note (2026-07-20 P1-031b in progress): gamepad/keyboard focus navigation on district-map neighbor nodes; Confirm/ui_accept travels the focused connected district; current-scene node stays non-focusable and non-interactive.
Coordination note (2026-07-20 P1-031b): district-map keyboard/gamepad focus travel landed; open seeds focus on a travelable neighbor, arrow/D-pad neighbors are wired spatially, Confirm/`ui_accept` uses the same `resolve_travel_spawn` contract as click; current-scene node stays `FOCUS_NONE`; `--filter=test_world_map_overlay` passes 8/8.
Coordination note (2026-07-20 P1-031c): focused travelable district nodes now use a thick warm focus outline distinct from their unfocused style; the current-scene gold highlight stays unchanged; `--filter=test_world_map_overlay` passes 8/8.
Coordination note (2026-07-21 P1-028): persistent keyboard/mouse and gamepad rebinding now covers all 18 shipped slice actions; the Controls overlay captures mouse inputs before GUI consumption, provides deterministic two-column focus navigation, and replacement input events complete keyboard/mouse and gamepad focus/confirm flows.
Coordination note (2026-07-20 P0-067a): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on `north_quarter`, `south_quarter`, `toompea_quarter`, and `reval_harbor` are owned in `docs/reports/map_chunk_boundary_review_p0_067a.md` with ADR 0010 ownership; audit still prints the warnings. Slice-map follow-up is **P0-067b**.
Coordination note (2026-07-20 P0-067b): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on playable slice maps `kalev_smithy` and `lower_town_slice`, plus registry leftover `market_civic_quarter`, are owned in `docs/reports/map_chunk_boundary_review_p0_067b.md` with ADR 0010 ownership; audit still prints the warnings. Object chunk streaming on slice maps stays non-production until **P0-067c**.
Coordination note (2026-07-21 P0-067c): object chunk streaming is production-ready on `kalev_smithy` and `lower_town_slice` at 32x32 cells; the executable ownership fixture locks all 69 retained warning records, exclusions remain baked/static, transitions persistent, and full-load/parity/route checks pass. Prototype-map activation remains separately gated.
Coordination note (2026-07-21 P0-037): shared low-poly rig production gate closed. Kalev and swap-only variants share the retargeted skeleton and canonical clips at the frozen 64 px scale; strict focused verification passes 16/16. An isolated Innkeeper rebuild plus verification took 17.42 seconds, canonical `pickup` integration took 21 seconds, and the reproducible procedure/evidence is in `docs/reports/character_rig_production_p0_037.md`.
Coordination note (2026-07-20 P0-069): restored exact map conversion-plan coverage for `scenes/tests/night_encounter_stub.tscn` as a developer-only retained test scene; the scene inventory records the same partial integration host and its reconciled counts. Python discovery and the standalone conversion-plan validator pass.
Coordination note (2026-07-21 P0-064): storage ownership policy and CI validator landed; generated `bin/rr.zip`, benchmark JSON, and Python bytecode are removed/ignored. Nine existing standard-Git binaries at least 10 MiB are checksum-locked pending authenticated LFS/external migration in **P0-070**. Existing quarantine classification drift is owned by **P0-071**.
Coordination note (2026-07-21 P0-071): active visual-source quarantine drift is closed. Unknown-rights NPC/tower sprites and sidecars are mirrored under `quarantine/`; runtime uses the existing NPC atlas and an internal procedural turret; the deterministic greybox floor has complete provenance; CI enforces zero active unapproved media exceptions.
Coordination note (2026-07-22 P0-070): Git LFS migration closeout is complete. Verified on current `HEAD`: `python3 tools/manage_lfs_assets.py verify` (110 objects / 569,387,736 bytes), `python3 tools/verify_storage_hygiene.py`, `python3 tools/validate_asset_sources.py`, `python3 tools/quarantine_assets.py --check`, `python3 -m unittest discover -s tests/python -v` (92/92), `python3 tools/generate_active_docs_report.py --check`, and the full Godot headless suite exit 0. Pruned 241 stale `assets/SOURCES.csv` rows left after the P0-030 import-path prune so provenance validation matches on-disk assets; `docs/storage_binary_exceptions.json` remains empty.
Coordination note (2026-07-21 P0-078): clean-clone baseline triage assigns Lower Town wall-walk/parity drift to the already-active **P0-076**; the post-P0-076 follow-up owns the remaining direction-sign runtime errors, rebound mouse-confirm failure, prototype route/adjacency failures, banner geometry regression, and generated fixture UID cleanup required before P0-070 can satisfy its literal green-test exit contract.
Coordination note (2026-07-22 P0-076): tower wall-walk passage wiring is complete on current `HEAD`: typed RRMap fields compile and validate, eight Lower Town round towers expose authored visual/collision openings, builder call sites receive map bounds, and canonical parity stays stable. Verified in a clean detached worktree with `test_map_view_3d_fortification` (7/7), `test_map_tower_doors` (6/6), `test_map_rrmap_parser` (11/11), and map audit (11 registered/discovered, 0 errors, no unknown typed fields). The remaining smithy entrance/courtyard baseline failures are explicitly owned by **P0-078**.
Coordination note (2026-07-22 P0-072 in progress): research and executable coverage now match all eleven retained registry blueprints, including `viru_gate_foreland`, with separate confidence-labelled vegetation-species, garden/agricultural-use, and fauna bands backed by archaeobotanical and zooarchaeological sources. `tools/verify_historical_dossier.py` enforces registry/category/source coverage and prevents P0-072/P1-036 closeout while review is pending. Maintainer sign-off remains **P0-111** per ADR 0014.
Coordination note (2026-07-21 P1-028 in progress): auditing every vertical-slice input action and adding persistent keyboard/mouse plus gamepad rebinding with focus-safe settings navigation and automated coverage.
Coordination note (2026-07-21 P0-073): Archbishop's Garden map-audit/conversion baseline is reconciled without release activation; its scene target, explicit `active=false` gate, scene inventory, migration matrix, conversion plan, and canonical 1600x900 audit capture now agree, and all four contract checks pass.
Coordination note (2026-07-21 P0-075): sky/weather compile regression closed. `_advance_gust()` restores the rain-front gust envelope; weather profiles push `storm_intensity` and wind-scaled cloud drift into the sky shader; `--filter=test_sky_weather_3d` passes 22/22 with zero script compile errors.
Coordination note (2026-07-22 P0-078 in progress): direction-sign contract drift from the Lower Town `.rrmap` migration is closed on current work: tests now assert the four authored signs (`viru gate and eastern road`, `to town centre`, `to knights district`, `karja gate`) and `--filter=test_direction_sign_3d` is green. Rebound mouse-confirm focus is green under `--filter=test_input_bindings`. Smithy entrance/courtyard bucket is closed: `smithy_door_transition` now carries `building_id=kalev_smithy` with `transition_visual=door`, and `viru_gate_south_jamb` shifts east so the courtyard apron stays walkable at the tested cells; `--filter=test_lower_town_slice_map` passes 14/14 and the parity fixture is refreshed. Remaining P0-078 buckets: prototype route/adjacency drift, banner geometry, and generated fixture UID cleanup.
Coordination note (2026-07-22 P0-078 active implementation): selected as the highest-priority dependency-free high-complexity task on current `HEAD`; running the required clean import and complete Godot suite first, then fixing every remaining failure by root cause without weakening assertions.
Coordination note (2026-07-22 P0-078): clean-clone Godot baseline is green. Verified: `tools/run_godot_checked.sh clean-import` and `--require-test-summary full-suite` pass 742/742 tests with zero failures. Closed the remaining prototype adjacency/patrol drift (harbor north/east reciprocal edges, Toompea and Kalamaja patrols), Viru Gate arch/jamb geometry, species-aware woodland density assertions, rebound mouse `ui_accept` focus flow in `UserSettings`, and detached-runtime `MusicDirector` lookup in `MapViewRuntime`. **P0-070** closeout is unblocked.
Coordination note (2026-07-22 P2-011): Hingepuu reflection overlay landed with `ReflectionModel`, `ReflectionOverlay`, and `ReflectionController` on the player host. Reflection morning auto-opens when `flag.reflection.completed` is false; quick-access exposes a mouse-reachable `Reflect` button; Duty/Fury/Mercy each apply one allowlisted effect; visual consequence marks and a plain-text summary mirror prior forged records and pressures; `--filter=test_reflection_overlay` passes 5/5.
Coordination note (2026-07-22 P0-120): `tools/update_todo_counts.py` now reports separate P3-P6 act-band rows in the TODO quick-reference table instead of a single collapsed P3+ bucket; suffix IDs such as `P4-027a` bucket correctly; `python3 -m unittest tests.python.test_update_todo_counts -v` passes 4/4.
Coordination note (2026-07-22 P0-080): `tools/update_todo_counts.py` regenerates the TODO quick-reference table from task rows, including suffix IDs such as `D-004a` and `P1-031a`; `python3 -m unittest tests.python.test_update_todo_counts -v` passes and the summary table is refreshed.
Coordination note (2026-07-22 P0-079): procedural sky resource baking (cloud noise, lunar albedo, star catalog, rain particles) now lives in `SkyWeatherResources`; `SkyWeather3D` keeps weather simulation and astronomy facades. `--filter=test_sky_weather_3d` passes 27/27 and `--filter=test_map_view_3d_lighting` passes 12/12 with zero behavior regressions.
Coordination note (2026-07-22 P0-081): all ten P1-037/P1-037a Estonia `world_travel` developer wrappers are reconciled with map audit, migration matrix, outdoor-prototype policy, and `assets/UI/estonia_world_map.png` C2PA provenance while every destination stays `release=false`; `MapAuditRegistry` executes every `world.*` definition and audit captures exist for each wrapper.
Coordination note (2026-07-21 P0-074): Viru Gate Foreland is reconciled as an `active=false` / `release=false` prototype across conversion plan, scene inventory, migration matrix, audit manifest, and the existing 1600x900 capture. Map discovery now ignores `.a2gent-worktrees` so local agent mirrors cannot fake inventory drift.
Coordination note (2026-07-22 P0-077): depth-layered harbour water already landed in `7b0f7e6` and is independently closed on current `HEAD`. A clean detached worktree import followed by the strict runner passes `test_coastal_sea_3d` 6/6, `test_map_view_3d_core` 15/15, and `test_map_view_3d_lighting` 11/11 with zero unallowlisted diagnostics or generated untracked files. The committed 1280x720 harbour capture records readable shallow bed detail against dark deep water.
Coordination note (2026-07-21 P1-035): manual load and debug presets now share one ordered `SessionState.replace_state()` path. `state_replaced(previous, current, reason)` emits once after the new bag is bound to `ContentDB`; phase, world-item, 3D equipment, inventory, journal, forge/dialogue, and interaction consumers rebind, and focused load/debug verification passes 2/2 (related state/phase/dialogue/world suites pass 68/68).
Coordination note (2026-07-21 P1-030): repeatable Lower Town performance reporting landed on the real production scene. `tools/run_performance_report.sh` records frame-time distributions, static/delta memory, authoritative actor count, detected host data, and a separate declared target-hardware profile; generated JSON remains ignored. Focused tests pass 3/3 and the quick report contract passes. P3-011 still owns selection and acceptance of minimum hardware.
Coordination note (2026-07-21 P1-034): `WorldMapOverlay` is now a 230-line public facade over focused `WorldMapLocalView` and `WorldMapFastTravelView` children; stable node names and public map/travel APIs remain intact, and only `WorldMapController` calls `DoorNavigator`. `--filter=test_world_map_overlay` passes 9/9 and `--filter=test_quick_access_menu` passes 5/5.
Coordination note (2026-07-22 P1-037a): extended the Estonia global-map graph with six campaign traversal placeholders - Rebel Kings' Camp, Kanavere Bog, Sõjamäe, Paide Castle, Pärnu, and Pöide Castle - with reciprocal roads/ferries, manifest spawns, and conversion-plan inventory coverage; all remain `release=false`. See `docs/reports/global_map_mockups.md`.
Coordination note (2026-07-22 P1-037): three map layers are now Local map, District map, and Estonia map. The `release=false` Estonia mockup graph includes Sacred Grove, Harju, Padise, Saaremaa, Rebel Kings' Camp, Kanavere, Sõjamäe, Paide, Pärnu, and Pöide; reciprocal roads/ferries connect neighboring sites while Reval remains linked through its south/east/west/harbour gates. The Estonia basemap runtime copy lives at `assets/UI/estonia_world_map.png`; see `docs/reports/global_map_mockups.md` for selection and route decisions.
Coordination note (2026-07-21 district-life planning): Kalamaja (`reval_harbor_east`) still uses `barrels` placeholders for `drying_nets_*` props; harbour research in `docs/reports/reval_harbour_1343_research.md` already names net yards, fish sheds, smoke sheds, and boatwright ground but the 3D vocabulary does not. District-life work is split: **P2-025** shared static prop kinds and mesh builders, **P2-026** Lower Town slice dressing, and **P4-028a** through **P4-028h** per-district specialization passes after the relevant environment tasks.
Coordination note (2026-07-20 P1-024c): shared health/death/guard/parry/dodge resolution landed in `CombatVitals` for player and combat actors; `--filter=test_combat_vitals` passes 10/10. Next combat step is **P1-024d** (Iron technique).
Coordination note (2026-07-20 P1-024d): Iron forge technique layers onto AttackProfile / CombatVitals - equipped Iron jams braced guards into open hits (parry and dodge still win); `--filter=test_forge_technique_iron` passes 5/5. Next combat step is **P1-024** (integrated combat room). Technique equip UI discoverability is **P1-024e**.
Coordination note (2026-07-20 P1-024): integrated combat test room at `scenes/tests/combat_room.tscn` with readable `CombatFeedbackHud`, hammer light/charged, guard/parry, dodge, Iron via quick-access, and keyboard/mouse/gamepad coverage in `--filter=test_combat_room`. Next combat step is **P1-025** (watchman/sergeant enemy state machine).
Coordination note (2026-07-20 P1-025 in progress): shared `EnemyCombatStateMachine` for watchman/sergeant archetypes (patrol → detect → telegraph → attack → react → disengage). Scene wiring stays **P1-025a**.
Coordination note (2026-07-20 P1-025): shared watchman/sergeant `EnemyCombatStateMachine` landed; `--filter=test_enemy_combat_state_machine` passes 8/8. Next combat step is **P1-025a** (wire archetypes into the combat room / encounter stub).
Coordination note (2026-07-20 P1-025a in progress): wiring watchman/sergeant into `scenes/tests/combat_room.tscn` with readable detect/telegraph/attack feedback; keep P1-024 dummies as the player-attack smoke host.
Coordination note (2026-07-20 P1-025a): watchman/sergeant wired into `scenes/tests/combat_room.tscn` via `CombatRoomEnemy`; readable detect/telegraph/attack feedback; `--filter=test_combat_room` passes 8/8. Next combat step is **P1-026** (surrender/escape/bypass outcomes).
Coordination note (2026-07-20 P1-026): authored surrender/escape/bypass outcomes via `EncounterOutcomeResolver`; non-lethal closes force-disengage without killing and write the same `GameState` quest keys as `kill`; combat-room mouse buttons; `--filter=test_encounter_outcome` passes 5/5. Next combat step is **P1-027** (retry checkpoint after failure).
Coordination note (2026-07-20 P1-027 in progress): combat failure retry via `EncounterCheckpoint` - arm GameState snapshot at encounter start, restore on death without clearing completed dialogue or prior quest progress; combat-room Retry button.
Coordination note (2026-07-20 P1-027): `EncounterCheckpoint` arms a GameState payload at encounter start; player death exposes a mouse-reachable Retry control that restores dialogue/quest without writing a false outcome; `--filter=test_encounter_checkpoint` passes 4/4. Night-scene wiring stays **P1-027a**.
Coordination note (2026-07-20 P1-025b in progress): optional night-encounter stub outside `combat_room` that reuses `CombatRoomEnemy` / `EnemyCombatStateMachine` for both archetypes so P2-009 / P1-027a can host a watch route without forking AI; keep unreachable from release demo navigation.
Coordination note (2026-07-20 P1-025b): night-encounter stub at `scenes/tests/night_encounter_stub.tscn` reuses `CombatRoomEnemy` / `EnemyCombatStateMachine`; `--filter=test_night_encounter_stub` passes 5/5 (both archetypes detect-to-disengage; unreachable from release demo navigation). Next combat step is **P1-027a** (checkpoint retry on the stub) or **P1-026a** (content-authored outcome package).
Coordination note (2026-07-20 P1-027a in progress): wire `EncounterCheckpoint` into `scenes/tests/night_encounter_stub.tscn` so player death outside the combat room exposes the same mouse-reachable Retry path; keep the stub unreachable from release demo navigation.
Coordination note (2026-07-20 P1-027a): night stub arms `EncounterCheckpoint` at reset, player death shows mouse-reachable Retry, restore keeps pre-fight dialogue/quest; `--filter=test_night_encounter_stub` passes 6/6. Next combat step is **P1-026a** (content-authored outcome package).
Coordination note (2026-07-20 P1-026a): `encounter.watch_checkpoint` + `quest.bitter_brew` package loads through ContentDB; schema/validator wired; combat room resolves from content; `--filter=test_encounter_outcome` passes 7/7 (escape-from-content + unknown-kind rejection). Follow-up **P1-026b** wires the same content package into the night stub UI for P2-009.
Coordination note (2026-07-20 P1-026b in progress): mouse-reachable surrender/escape/bypass on `scenes/tests/night_encounter_stub.tscn` resolving via ContentDB `encounter.watch_checkpoint` (same quest contract as the combat room); keep the stub unreachable from release demo navigation.
Coordination note (2026-07-20 P1-026b): night stub wires ContentDB `encounter.watch_checkpoint` surrender/escape/bypass mouse controls; `--filter=test_night_encounter_stub` passes 8/8 (escape-from-content + unknown-kind rejection; still unreachable from release demo). Next combat/slice step is **P2-009** (night consequence host) once forge/investigation deps land; optional discoverability polish for outcome buttons in quick-access stays out of scope for the stub.
Coordination note (2026-07-21 monastery planning): `monastery_quarter` had a map-wide cobblestone base that violated `docs/HISTORICAL_AUDIT.md` / `docs/MAP_AUTHORING.md` street-surface rules. Prototype base is now packed earth with cobble limited to Pikk/Lai/civic/vene spines, mud secondary lanes, and compact stone closes. Full district quality remains split: **P4-023a** streets/surfaces, **P4-023b** ordinary fabric, **P4-023c/d** St Olaf and St Michael exceptional models, **P4-023e** later guild/Blackheads facade correction. Revised **P4-023** covers North only.
Coordination note (2026-07-22 environmental realism priority): maintainer decision - environmental realism is the top production priority after **P0-078** baseline green. The sky/weather cycle (**P0-075**) is accepted; do not reopen unless fixing a defect. Prioritize historically correct streets, building fabric, and landmark silhouettes over slice quest authoring until Lower Town passes the **P2-021** composition gate. Living-world work is ordered: place correctness (**P0-072**, **P2-022**, **P2-023**), surfaces (**P0-053**, **P2-003**), vegetation (**P2-027**, **P2-028**), daily-life dressing (**P2-025**, **P2-026**, **P2-030**), then fauna and birds (**P2-024**, **P2-031**, **P2-029**), then district rollout (**P4-022** through **P4-026**, **P4-028***, **P4-029**).
Coordination note (2026-07-20 P1-024e): quick-access `IronTechniqueButton` equips and clears Iron via mouse; `GameState.equipped_forge_technique()` and attack-profile guard-pierce update; `--filter=test_quick_access_menu` passes 5/5. Next combat step is **P1-024** (integrated combat room).

## Archived planning notes

Delivery order remains strict: playable demo, vertical-slice MVP, Act 1, Act 2, Act 3. D-004 packaging is closed; resume visual acceptance (P0-053 / P0-037) and vertical-slice systems without waiting on further packaging work. Prototype and slice chunk-boundary ownership reviews are closed under P0-067a / P0-067b; production chunk-streaming readiness remains **P0-067c**.
Player-facing discoverability is part of every feature's definition of done. Any new player action, screen, or overlay must add a visible entry point to the persistent quick-access menu or an equally visible contextual prompt; a hotkey alone is not sufficient. Its verification must cover both mouse activation and the documented shortcut so players and developers can find shipped functionality without reading source code.
### 1. Stabilize ownership and make verification honest (blocks all feature work)
1. **P0-061 / P0-062 / P0-062a / P0-063** - **Done 2026-07-20.** Harness fails on runtime exceptions; named fixtures and full-suite exit are green; content/Python/map-audit/matrix/docs gates and discovery CI are restored. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
2. **P0-060** - **Done 2026-07-20.** Stale DEF-003/004/005 retired; DEF-001/002/006 retained with fresh repro; README/AGENTS status matches D-003 done / D-004 open.
3. **P0-066** - **Done 2026-07-20** (`606a6e5`). Shared-rig gait overrides and regenerated NPC bodies landed; `--filter=test_character_rig` passes 15/15.
4. **P0-067** - **Done 2026-07-20.** District-adjacency/terrain mesh polish and procedural mesh reuse landed green; `run_map_pipeline_ci.sh audit` and `--filter=test_map_view_3d_core` pass. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
5. **P0-062b** - **Done 2026-07-20.** Prototype watch routes again cover Pikk/Lai/guild, King's/Karja/Viru, and Lühike/Pikk Jalg/south-slope corridors with walkable segments and multi-street span assertions.
6. **P0-067a** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for the four inactive/dev-gated prototypes; activation still blocked until split or re-assert. Slice maps continue under **P0-067b**.
7. **P0-067b** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for `kalev_smithy`, `lower_town_slice`, and leftover `market_civic_quarter`. Object chunk streaming on slice maps stays non-production until **P0-067c**.
Required exit condition for the packaging gate: clean clone import, startup smokes, the full Godot suite, Python discovery, content validation, map pipeline, docs validation, and export all complete with zero unallowlisted errors. Exit code `0` alone is not sufficient.
### 2. Freeze release scope and make status documentation accurate
1. **P0-060** - **Done.** Re-triaged known runtime defects and updated README/AGENTS status claims.
2. **P0-064 and P0-065** - record repository-storage and architecture cleanup, but do not let broad refactoring delay D-004 unless it blocks a clean clone or reliable test run.
### 3. Finish and package the demo
- **D-003 is complete:** movement, Mart conversation, anvil spearhead pickup, bag UI, and session re-entry exist on the current slice.
- **D-004 is complete:** `tools/verify_packaged_demo.sh` exports the macOS `rr` build, proves packaged launch, runs the headless move-talk-pickup proof, and links the frame walkthrough from `README.md`.
- **D-004a is complete:** no release-only move-talk-pickup defects remain; see `docs/reports/d004a_release_only_triage.md`. Optional: D-004b human video; D-004c in-binary packaged walkthrough.
### 4. Close the visual acceptance gate and rebuild Reval environments in evidence-first order
Maintainer priority (2026-07-22): realism of place and daily life over slice quest content. Weather is frozen at **P0-075**; do not spend cycles on sky/weather unless fixing a regression.
1. **P0-072** - first establish sourced, map-specific 1343 Reval targets for street layout, property density, ordinary and exceptional architecture, relief, vegetation species mixes, agricultural land use, domestic and wild fauna ranges, and landmark states; do not encode the current cobblestone coverage as the historical baseline.
2. **P0-053** - finish the surface/weathering kit; P0-037 shared-rig production evidence is complete.
3. **P0-038, then P0-040** - measure the candidate and freeze ART_BIBLE v2 with maintainer sign-off before broader production art ([ADR 0013](adr/0013-authorial-visual-direction-without-blind-ux-panels.md)).
4. **P2-003, P1-029, and P1-036** - after the art gate, build and lint the historically grounded modular environment kit and enforce the sourced composition ranges with a map-quality audit.
5. **P2-022, then P2-023** - highest player-visible realism pass on the playable Lower Town: historically correct streets and plot pattern, dense property footprints, period architecture and materials, relief, and sparse plausible vegetation. **P2-021** visual parity follows these passes.
6. **P2-027, then P2-028** - replace decorative tree silhouettes with species-accurate Estonian trees, then expand ground-cover to at least twenty historically plausible grasses, herbs, and meadow plants for urban margins and open ground.
7. **P2-025 and P2-026** - land the shared district-life static prop kit, then dress the playable Lower Town slice so yards and street frontages read as specific trades without NPC actors.
8. **P2-030** - rural and agricultural static-life kit for forelands, garden plots, hay meadows, pastures, and peri-urban margins so life outside the wall reads as 1343 Estonian countryside rather than empty filler.
9. **P2-024, then P2-031** - urban ambient fauna (cats, dogs, horses, rats) first; then domestic stock (chickens, ducks, geese, pigs, cows) and cautious wild margins (hares, foxes, wolves, bears) with flee/tether behavior and no navigation blocking.
10. **P2-029** - ambient bird flight and song after core layout and fauna budgets are stable.
11. **P4-022 through P4-026** - apply the same sourced quality bar district by district after slice approval. Central, then North (**P4-023**) and Monastery (**P4-023a** through **P4-023e**), precede their activation gates; south, Toompea/garden, and harbor remain inactive prototypes unless separately approved.
12. **P4-028a through P4-028h** and **P4-029** - per-district static life dressing, then foreland/rural perimeter passes using the agricultural and fauna kits.
### 5. Resume vertical-slice systems only after the environment baseline is credible
1. **P1-025 through P1-027** - add enemies, non-lethal outcomes, and retry state (enemy state machine is **P1-025**, done; room wiring **P1-025a** is done; night stub **P1-025b** is done; outcomes **P1-026** are done; retry checkpoint **P1-027** is done; night-scene retry wiring **P1-027a** is done; content outcome package **P1-026a** is done; night-stub outcome UI from content **P1-026b** is done).
2. **P1-022** follows P0-040; **P1-031** district map overlay is done; click-to-travel polish is **P1-031a** (done); keyboard/gamepad focus travel is **P1-031b** (done).
3. Then build the authored slice in order: P2-006 tutorial, P2-007 investigation, P2-008 forge choice, P2-009 night consequence, P2-010 aftermath, P2-011 reflection, P2-012 end-to-end slice. Slice quest authoring (**P2-006** through **P2-012**) stays below **P2-021** environment realism until Lower Town reads as historically grounded Reval rather than a gameplay greybox.
