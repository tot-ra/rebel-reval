# TODO

Format: `- [ ] ID | deps: unresolved ID,ID or none | deliverable: concrete artifact or behavior | verify: objective completion check`

Dependency policy: `deps` lists unresolved blockers only. Remove a dependency when it completes; use Git history for completed work. Completed rows remain at the end only when repository validators require their exact task contracts.

## Immediate priorities

Coordination note (2026-07-20 evening re-audit, committed `HEAD` `606a6e5`): the morning audit's trust findings (13 masked `SCRIPT ERROR` exceptions behind 482 "passes", 2 Python contract failures, failing content corpora, stale report, audit/matrix drift) are resolved history - P0-061 made the harness fail on runtime exceptions, and P0-062/P0-062a/P0-063 cleared the errors and reconciled the validators. Verified on this HEAD: `python3 -m unittest discover -s tests/python`, `verify_map_audit.py`, `verify_migration_matrix.py`, `verify_map_conversion_plan.py`, `verify_map_activation.py`, and `generate_active_docs_report.py --check` all pass, and `--filter=test_character_rig` passes 15/15. The macOS export evidence (414 MiB `rr.dmg`) still proves package generation only, not packaged play - that remains D-004.

Coordination note (2026-07-20 P0-062b): restored multi-street walkable patrols on `north_quarter`, `south_quarter`, and `toompea_quarter` after the P0-062a spawn-adjacent temporary loops. Open baseline packaging step remains D-004; reviewable chunk-boundary warnings stay under P0-067a.

Coordination note (2026-07-20 D-004 closeout): packaged demo gate closed. Verified: `--filter=test_demo_walkthrough` (2/2), `tools/verify_packaged_demo.sh` export + packaged launch, and frame walkthrough linked from `README.md` / `docs/reports/demo_walkthrough_d004.md`. Follow-ups: D-004a (release-only defect notes) and optional D-004b (human video capture).

Coordination note (2026-07-20 D-004a): no release-only move-talk-pickup defects remain. Packaged `--quit-after` launch is green without `--path`; `verify_packaged_demo.sh` no longer probes release binaries with path overrides; triage in `docs/reports/d004a_release_only_triage.md`. Optional follow-ups: D-004b (human video), D-004c (in-binary packaged walkthrough).

Coordination note (2026-07-21 D-004c): packaged in-binary move-talk-pickup automation landed. The extracted release app starts from its real main menu, proves movement, completes Mart dialogue, picks up the anvil spearhead through runtime `Interactable` paths, prints `D-004C_PACKAGED_WALKTHROUGH_PASS`, and exits 0 without editor, `--path`, or scene overrides.

Coordination note (2026-07-20 P1-031): district/world map overlay on every `player.tscn` host; `toggle_world_map` (M) plus quick-access `Districts [M]`; graph nodes/edges match `DoorNavigator` active manifest; `--filter=test_world_map_overlay` passes 5/5. Optional click-to-travel is **P1-031a**.

Coordination note (2026-07-20 P0-063a): active Markdown discovery now prunes hidden/tool-cache directories before classifying active or excluded docs; `.pytest_cache/README.md` regression coverage is part of Python discovery, and the committed report stays stable when pytest creates its cache.

Coordination note (2026-07-20 P1-031a in progress): mouse click on a connected district-map neighbor travels through `DoorNavigator.go_to_scene` with the edge's registered destination spawn; current-scene node stays non-interactive; unknown/disconnected nodes do not travel.

Coordination note (2026-07-20 P1-031a): district-map click-to-travel landed; neighbor clicks resolve the authored edge spawn via `WorldMapGraph.resolve_travel_spawn` and call `DoorNavigator.go_to_scene`; current-scene / unknown / disconnected nodes stay non-traveling; `--filter=test_world_map_overlay` passes 7/7.

Coordination note (2026-07-20 P1-031b in progress): gamepad/keyboard focus navigation on district-map neighbor nodes; Confirm/ui_accept travels the focused connected district; current-scene node stays non-focusable and non-interactive.

Coordination note (2026-07-20 P1-031b): district-map keyboard/gamepad focus travel landed; open seeds focus on a travelable neighbor, arrow/D-pad neighbors are wired spatially, Confirm/`ui_accept` uses the same `resolve_travel_spawn` contract as click; current-scene node stays `FOCUS_NONE`; `--filter=test_world_map_overlay` passes 8/8.

Coordination note (2026-07-20 P1-031c): focused travelable district nodes now use a thick warm focus outline distinct from their unfocused style; the current-scene gold highlight stays unchanged; `--filter=test_world_map_overlay` passes 8/8.

Coordination note (2026-07-21 P1-028): persistent keyboard/mouse and gamepad rebinding now covers all 18 shipped slice actions; the Controls overlay captures mouse inputs before GUI consumption, provides deterministic two-column focus navigation, and replacement input events complete keyboard/mouse and gamepad focus/confirm flows.

Coordination note (2026-07-20 P0-067a): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on `north_quarter`, `south_quarter`, `toompea_quarter`, and `reval_harbor` are owned in `docs/reports/map_chunk_boundary_review_p0_067a.md` with ADR 0010 ownership; audit still prints the warnings. Slice-map follow-up is **P0-067b**.

Coordination note (2026-07-20 P0-067b): retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on playable slice maps `kalev_smithy` and `lower_town_slice`, plus registry leftover `market_civic_quarter`, are owned in `docs/reports/map_chunk_boundary_review_p0_067b.md` with ADR 0010 ownership; audit still prints the warnings. Object chunk streaming on slice maps stays non-production until **P0-067c**.

Coordination note (2026-07-21 P0-067c): object chunk streaming is production-ready on `kalev_smithy` and `lower_town_slice` at 32x32 cells; the executable ownership fixture locks all 69 retained warning records, exclusions remain baked/static, transitions persistent, and full-load/parity/route checks pass. Prototype-map activation remains separately gated.

Coordination note (2026-07-21 P0-037): shared low-poly rig production gate closed. Kalev and swap-only variants share the retargeted skeleton and canonical clips at the frozen 64 px scale; strict focused verification passes 16/16. An isolated Innkeeper rebuild plus verification took 17.42 seconds, canonical `pickup` integration took 21 seconds, and the reproducible procedure/evidence is in `docs/reports/character_rig_production_p0_037.md`.

Coordination note (2026-07-20 P0-069): restored exact map conversion-plan coverage for `scenes/tests/night_encounter_stub.tscn` as a developer-only retained test scene; the scene inventory records the same partial integration host and its reconciled counts. Python discovery and the standalone conversion-plan validator pass.

Coordination note (2026-07-21 P0-064): storage ownership policy and CI validator landed; generated `bin/rr.zip`, benchmark JSON, and Python bytecode are removed/ignored. Nine existing standard-Git binaries at least 10 MiB are checksum-locked pending authenticated LFS/external migration in **P0-070**. Existing quarantine classification drift is owned by **P0-071**.

Coordination note (2026-07-21 P0-071): active visual-source quarantine drift is closed. Unknown-rights NPC/tower sprites and sidecars are mirrored under `quarantine/`; runtime uses the existing NPC atlas and an internal procedural turret; the deterministic greybox floor has complete provenance; CI enforces zero active unapproved media exceptions.

Coordination note (2026-07-21 P0-070 in progress): the already-landed scoped Git LFS migration is verified for all 110 objects / 569,387,736 bytes: indexed pointers match the byte/SHA-256/owner/license manifest, `origin` reports no pending LFS uploads, an SSH clean clone keeps inactive corpora as pointers and restores only the two runtime objects, Python discovery passes 78/78, clean import passes, and clean-clone macOS export produces a 707,700,679-byte DMG. Closeout remains blocked only by **P0-078**: the unrelated `c12fa53` clean-clone Godot baseline completes 713 tests with 23 failures / 3 errors.

Coordination note (2026-07-21 P0-078): clean-clone baseline triage assigns Lower Town wall-walk/parity drift to the already-active **P0-076**; the post-P0-076 follow-up owns the remaining direction-sign runtime errors, rebound mouse-confirm failure, prototype route/adjacency failures, banner geometry regression, and generated fixture UID cleanup required before P0-070 can satisfy its literal green-test exit contract.

Coordination note (2026-07-21 P0-072 in progress): sourced research draft landed for all ten retained urban map blueprints, separating attested evidence, bounded reconstruction, later-survival analogy, gameplay invention, and explicit unknowns. It remains in progress until the independent human historical review table in `docs/HISTORICAL_AUDIT.md` signs or amends the ranges for P1-036 / district acceptance use.

Coordination note (2026-07-21 P1-028 in progress): auditing every vertical-slice input action and adding persistent keyboard/mouse plus gamepad rebinding with focus-safe settings navigation and automated coverage.

Coordination note (2026-07-21 P0-073): Archbishop's Garden map-audit/conversion baseline is reconciled without release activation; its scene target, explicit `active=false` gate, scene inventory, migration matrix, conversion plan, and canonical 1600x900 audit capture now agree, and all four contract checks pass.

Coordination note (2026-07-21 P0-075): sky/weather compile regression closed. `_advance_gust()` restores the rain-front gust envelope; weather profiles push `storm_intensity` and wind-scaled cloud drift into the sky shader; `--filter=test_sky_weather_3d` passes 22/22 with zero script compile errors.

Coordination note (2026-07-21 P0-074): Viru Gate Foreland is reconciled as an `active=false` / `release=false` prototype across conversion plan, scene inventory, migration matrix, audit manifest, and the existing 1600x900 capture. Map discovery now ignores `.a2gent-worktrees` so local agent mirrors cannot fake inventory drift.

Coordination note (2026-07-21 P1-035): manual load and debug presets now share one ordered `SessionState.replace_state()` path. `state_replaced(previous, current, reason)` emits once after the new bag is bound to `ContentDB`; phase, world-item, 3D equipment, inventory, journal, forge/dialogue, and interaction consumers rebind, and focused load/debug verification passes 2/2 (related state/phase/dialogue/world suites pass 68/68).

Coordination note (2026-07-21 P1-030): repeatable Lower Town performance reporting landed on the real production scene. `tools/run_performance_report.sh` records frame-time distributions, static/delta memory, authoritative actor count, detected host data, and a separate declared target-hardware profile; generated JSON remains ignored. Focused tests pass 3/3 and the quick report contract passes. P3-011 still owns selection and acceptance of minimum hardware.

Coordination note (2026-07-21 P1-034): `WorldMapOverlay` is now a 230-line public facade over focused `WorldMapLocalView` and `WorldMapFastTravelView` children; stable node names and public map/travel APIs remain intact, and only `WorldMapController` calls `DoorNavigator`. `--filter=test_world_map_overlay` passes 9/9 and `--filter=test_quick_access_menu` passes 5/5.

Coordination note (2026-07-21 district-life planning): Kalamaja (`reval_harbor_east`) still uses `barrels` placeholders for `drying_nets_*` props; harbour research in `docs/reports/reval_harbour_1343_research.md` already names net yards, fish sheds, smoke sheds, and boatwright ground but the 3D vocabulary does not. District-life work is split: **P2-025** shared static prop kinds and mesh builders, **P2-026** Lower Town slice dressing, and **P4-028a** through **P4-028h** per-district specialization passes after the relevant environment tasks.

Coordination note (2026-07-20 P1-024c): shared health/death/guard/parry/dodge resolution landed in `CombatVitals` for player and combat actors; `--filter=test_combat_vitals` passes 10/10. Next combat step is **P1-024d** (Iron technique).

Coordination note (2026-07-20 P1-024d): Iron forge technique layers onto AttackProfile / CombatVitals - equipped Iron jams braced guards into open hits (parry and dodge still win); `--filter=test_forge_technique_iron` passes 5/5. Next combat step is **P1-024** (integrated combat room). Technique equip UI discoverability is **P1-024e**.

Coordination note (2026-07-20 P1-024): integrated combat test room at `scenes/tests/combat_room.tscn` with readable `CombatFeedbackHud`, hammer light/charged, guard/parry, dodge, Iron via quick-access, and keyboard/mouse/gamepad coverage in `--filter=test_combat_room`. Next combat step is **P1-025** (watchman/sergeant enemy state machine).

Coordination note (2026-07-20 P1-025 in progress): shared `EnemyCombatStateMachine` for watchman/sergeant archetypes (patrol → detect → telegraph → attack → react → disengage). Scene wiring stays **P1-025a**.

Coordination note (2026-07-20 P1-025): shared watchman/sergeant `EnemyCombatStateMachine` landed; `--filter=test_enemy_combat_state_machine` passes 8/8. Next combat step is **P1-025a** (wire archetypes into the combat room / encounter stub).

Coordination note (2026-07-20 P1-025a in progress): wiring watchman/sergeant into `scenes/tests/combat_room.tscn` with readable detect/telegraph/attack feedback; keep P1-024 dummies as the player-attack smoke host.

Coordination note (2026-07-20 P1-025a): watchman/sergeant wired into `scenes/tests/combat_room.tscn` via `CombatRoomEnemy`; readable detect/telegraph/attack feedback; `--filter=test_combat_room` passes 8/8. Next combat step is **P1-026** (surrender/escape/bypass outcomes).

Coordination note (2026-07-20 P1-026): authored surrender/escape/bypass outcomes via `EncounterOutcomeResolver`; non-lethal closes force-disengage without killing and write the same `GameState` quest keys as `kill`; combat-room mouse buttons; `--filter=test_encounter_outcome` passes 5/5. Next combat step is **P1-027** (retry checkpoint after failure).

Coordination note (2026-07-20 P1-027 in progress): combat failure retry via `EncounterCheckpoint` - arm GameState snapshot at encounter start, restore on death without clearing completed dialogue or prior quest progress; combat-room Retry button.

Coordination note (2026-07-20 P1-027): `EncounterCheckpoint` arms a GameState payload at encounter start; player death exposes a mouse-reachable Retry control that restores dialogue/quest without writing a false outcome; `--filter=test_encounter_checkpoint` passes 4/4. Night-scene wiring stays **P1-027a**.

Coordination note (2026-07-20 P1-025b in progress): optional night-encounter stub outside `combat_room` that reuses `CombatRoomEnemy` / `EnemyCombatStateMachine` for both archetypes so P2-009 / P1-027a can host a watch route without forking AI; keep unreachable from release demo navigation.

Coordination note (2026-07-20 P1-025b): night-encounter stub at `scenes/tests/night_encounter_stub.tscn` reuses `CombatRoomEnemy` / `EnemyCombatStateMachine`; `--filter=test_night_encounter_stub` passes 5/5 (both archetypes detect-to-disengage; unreachable from release demo navigation). Next combat step is **P1-027a** (checkpoint retry on the stub) or **P1-026a** (content-authored outcome package).

Coordination note (2026-07-20 P1-027a in progress): wire `EncounterCheckpoint` into `scenes/tests/night_encounter_stub.tscn` so player death outside the combat room exposes the same mouse-reachable Retry path; keep the stub unreachable from release demo navigation.

Coordination note (2026-07-20 P1-027a): night stub arms `EncounterCheckpoint` at reset, player death shows mouse-reachable Retry, restore keeps pre-fight dialogue/quest; `--filter=test_night_encounter_stub` passes 6/6. Next combat step is **P1-026a** (content-authored outcome package).

Coordination note (2026-07-20 P1-026a): `encounter.watch_checkpoint` + `quest.bitter_brew` package loads through ContentDB; schema/validator wired; combat room resolves from content; `--filter=test_encounter_outcome` passes 7/7 (escape-from-content + unknown-kind rejection). Follow-up **P1-026b** wires the same content package into the night stub UI for P2-009.

Coordination note (2026-07-20 P1-026b in progress): mouse-reachable surrender/escape/bypass on `scenes/tests/night_encounter_stub.tscn` resolving via ContentDB `encounter.watch_checkpoint` (same quest contract as the combat room); keep the stub unreachable from release demo navigation.

Coordination note (2026-07-20 P1-026b): night stub wires ContentDB `encounter.watch_checkpoint` surrender/escape/bypass mouse controls; `--filter=test_night_encounter_stub` passes 8/8 (escape-from-content + unknown-kind rejection; still unreachable from release demo). Next combat/slice step is **P2-009** (night consequence host) once forge/investigation deps land; optional discoverability polish for outcome buttons in quick-access stays out of scope for the stub.

Coordination note (2026-07-21 monastery planning): `monastery_quarter` had a map-wide cobblestone base that violated `docs/HISTORICAL_AUDIT.md` / `docs/MAP_AUTHORING.md` street-surface rules. Prototype base is now packed earth with cobble limited to Pikk/Lai/civic/vene spines, mud secondary lanes, and compact stone closes. Full district quality remains split: **P4-023a** streets/surfaces, **P4-023b** ordinary fabric, **P4-023c/d** St Olaf and St Michael exceptional models, **P4-023e** later guild/Blackheads facade correction. Revised **P4-023** covers North only.

Coordination note (2026-07-20 P1-024e): quick-access `IronTechniqueButton` equips and clears Iron via mouse; `GameState.equipped_forge_technique()` and attack-profile guard-pierce update; `--filter=test_quick_access_menu` passes 5/5. Next combat step is **P1-024** (integrated combat room).

Delivery order remains strict: playable demo, vertical-slice MVP, Act 1, Act 2, Act 3. D-004 packaging is closed; resume visual acceptance (P0-053 / P0-037) and vertical-slice systems without waiting on further packaging work. Prototype and slice chunk-boundary ownership reviews are closed under P0-067a / P0-067b; production chunk-streaming readiness remains **P0-067c**.

Player-facing discoverability is part of every feature's definition of done. Any new player action, screen, or overlay must add a visible entry point to the persistent quick-access menu or an equally visible contextual prompt; a hotkey alone is not sufficient. Its verification must cover both mouse activation and the documented shortcut so players and developers can find shipped functionality without reading source code.

### 1. Stabilize ownership and make verification honest (blocks all feature work)

1. **P0-061 / P0-062 / P0-062a / P0-063** - **Done 2026-07-20.** Harness fails on runtime exceptions; named fixtures and full-suite exit are green; content/Python/map-audit/matrix/docs gates and discovery CI are restored. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
2. **P0-060** - **Done 2026-07-20.** Stale DEF-003/004/005 retired; DEF-001/002/006 retained with fresh repro; README/AGENTS status matches D-003 done / D-004 open.
3. **P0-066** - **Done 2026-07-20** (`606a6e5`). Shared-rig gait overrides and regenerated NPC bodies landed; `--filter=test_character_rig` passes 15/15.
4. **P0-067** - **Done 2026-07-20.** District-adjacency/terrain mesh polish and procedural mesh reuse landed green; `run_map_pipeline_ci.sh audit` and `--filter=test_map_view_3d_core` pass. Historical multi-street patrol restoration is **P0-062b** (**Done 2026-07-20**).
5. **P0-062b** - **Done 2026-07-20.** Prototype watch routes again cover Pikk/Lai/guild, King's/Karja/Viru, and Lühike/Pikk Jalg/south-slope corridors with walkable segments and multi-street span assertions.
6. **P0-067a** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for the four inactive/dev-gated prototypes; activation still blocked until split or re-assert. Slice maps continue under **P0-067b**.
7. **P0-067b** - **Done 2026-07-20.** Owned retained chunk-boundary warnings for `kalev_smithy`, `lower_town_slice`, and leftover `market_civic_quarter`. Object chunk streaming on slice maps stays non-production until **P0-067c**.

Required exit condition for the packaging gate: clean clone import, startup smokes, the full Godot suite, Python discovery, content validation, map pipeline, docs validation, and export all complete with zero unallowlisted errors. Exit code `0` alone is not sufficient.

### 2. Freeze release scope and make status documentation accurate

1. **P0-060** - **Done.** Re-triaged known runtime defects and updated README/AGENTS status claims.
2. **P0-064 and P0-065** - record repository-storage and architecture cleanup, but do not let broad refactoring delay D-004 unless it blocks a clean clone or reliable test run.

### 3. Finish and package the demo

- **D-003 is complete:** movement, Mart conversation, anvil spearhead pickup, bag UI, and session re-entry exist on the current slice.
- **D-004 is complete:** `tools/verify_packaged_demo.sh` exports the macOS `rr` build, proves packaged launch, runs the headless move-talk-pickup proof, and links the frame walkthrough from `README.md`.
- **D-004a is complete:** no release-only move-talk-pickup defects remain; see `docs/reports/d004a_release_only_triage.md`. Optional: D-004b human video; D-004c in-binary packaged walkthrough.

### 4. Close the visual acceptance gate and rebuild Reval environments in evidence-first order

1. **P0-072** - first establish sourced, map-specific 1343 Reval targets for street surfaces, property density, architecture, relief, vegetation, and landmark states; do not encode the current cobblestone coverage as the historical baseline.
2. **P0-053** - finish the surface/weathering kit; P0-037 shared-rig production evidence is complete.
3. **P0-038, then P0-039 and P0-040** - measure the candidate, run human readability review, and freeze ART_BIBLE v2 before broader production art.
4. **P2-003, P1-029, and P1-036** - after the art gate, build and lint the historically grounded modular environment kit and enforce the sourced composition ranges with a map-quality audit.
5. **P2-022, then P2-023** - prioritize the playable Lower Town: correct streets, plot/building density, ground-surface balance, relief and vegetation first, then apply varied architecture, weathering, and exceptional landmark models. **P2-021** visual parity follows these passes.
6. **P2-024** - add ambient cats, dogs, horses, and rats only after routes and environment composition are stable; fauna must not hide empty geometry or block navigation.
7. **P2-025 and P2-026** - after P2-003/P0-053, land the shared district-life static prop kit, then dress the playable Lower Town slice so yards and street frontages read as specific trades without NPC actors.
8. **P4-022 through P4-026** - apply the same sourced quality bar district by district after slice approval. Central, then North (**P4-023**) and Monastery (**P4-023a** through **P4-023e**), precede their activation gates; south, Toompea/garden, and harbor remain inactive prototypes unless separately approved.
9. **P4-028a through P4-028h** - after each district environment pass and **P2-025**, add historically grounded static life dressing so a player can infer what each yard, house row, and foreland does from props alone (fishing nets at Kalamaja, rope coils at the north ropeyard, guild stalls at market square, and so on).

### 5. Resume vertical-slice systems only after the baseline is green

1. **P1-025 through P1-027** - add enemies, non-lethal outcomes, and retry state (enemy state machine is **P1-025**, done; room wiring **P1-025a** is done; night stub **P1-025b** is done; outcomes **P1-026** are done; retry checkpoint **P1-027** is done; night-scene retry wiring **P1-027a** is done; content outcome package **P1-026a** is done; night-stub outcome UI from content **P1-026b** is done).
2. **P1-022** follows P0-040; **P1-031** district map overlay is done; click-to-travel polish is **P1-031a** (done); keyboard/gamepad focus travel is **P1-031b** (done).
3. Then build the authored slice in order: P2-006 tutorial, P2-007 investigation, P2-008 forge choice, P2-009 night consequence, P2-010 aftermath, P2-011 reflection, P2-012 end-to-end slice.

## Demo - MVP playable demo (highest priority)

Goal: a small runnable demo proving the ADR 0007 look and the core interaction loop - one controllable character in the eastern quarter, a conversation with Mart, and one forge item picked up into the existing visible bag. Demo-only dialogue code may later be replaced by P1-011/P1-012, but the demo must reuse `Interactable`, `ContentDB`, session `GameState`, `WorldItemController`, and the current inventory overlay.

- [x] D-003 | deps: none | deliverable: the authored anvil forge item exposed through the existing `WorldItemController`, focus/prompt interaction, pickup feedback, and bag overlay, with placement and ownership stored in session `GameState` | verify: mouse, keyboard, and gamepad can pick up the item; it disappears from the smithy, appears in the bag, survives smithy exit/re-entry, and an over-capacity pickup leaves state unchanged
- [x] D-004 | deps: none | deliverable: packaged demo: desktop export preset producing a runnable build of the demo flow plus a captured walkthrough recording for the README | verify: a clean export from a fresh clone starts, completes move-talk-pickup without errors or debug intervention, and the capture is referenced from `README.md`
- [x] D-004a | deps: none | deliverable: after the packaged demo walkthrough lands, capture a short failure-mode note for any release-build-only defect found during move-talk-pickup that does not reproduce under editor/`--path .` play, with repro steps and owner | verify: either no release-only defects remain, or `docs/reports/known_runtime_defects.md` lists each with packaged-build repro and a follow-up TODO ID
- [ ] D-004b | deps: none | deliverable: optional human-narrated screen recording of the packaged `Reval Rebel.app` move-talk-pickup path for marketing/README embed, complementing the existing frame sequence in `docs/reports/demo_walkthrough_d004.md` | verify: README links a playable video or animated capture that shows Start, Mart talk, and bag pickup in the release binary
- [x] D-004c | deps: none | deliverable: in-binary packaged move-talk-pickup automation that runs inside `Reval Rebel.app` without editor `--path` or scene CLI args (release templates reject path overrides), writing a pass/fail artifact under `user://` or stdout via the console wrapper | verify: `tools/verify_packaged_demo.sh` (or a sibling) proves Start-Mart-pickup inside the extracted release app with exit 0 and no editor binary driving the loop
## P0 - Product, canon, and reproducible baseline

- [x] P0-056 | deps: none | deliverable: green headless test baseline: triage of the 427 test failures recorded on 2026-07-19 (393 in `tests/godot/test_map_quality_audit.gd` after the Eastern Quarters, Lower Town, and north-quarter map edits; the remainder across `test_map_view_3d_lighting.gd`, `test_map_object_chunk_streaming.gd`, `test_world_items.gd`, `test_map_click_input_controller.gd`, `test_map_rrmap_parser.gd`, `test_map_prefabs.gd`, `test_map_view_3d_runtime.gd`, `test_map_view_3d_core.gd`, and `test_direction_sign_3d.gd`), fixing map data or updating stale test contracts case by case without deleting audit coverage or weakening assertions | verify: `godot --headless --path . --script tools/run_godot_tests.gd` reports zero failures on a clean clone and the CI "Godot headless tests" step passes on `main`
- [x] P0-058 | deps: none | deliverable: resolution of the uncommitted working-tree changes: the `SkyWeather3D` dome and deterministic weather cycle, the `wall_material`/`roof_material` `.rrmap` style keys with shingle and log patterns, the `project.godot` shadow-setting edits, and the orphaned `.uid` files for already-committed scripts - either landed with headless tests for the weather state machine and lighting modifiers plus a `docs/MAP_AUTHORING.md` entry for the new style keys, or reverted | verify: `git status` reports a clean working tree, the full headless suite passes, and if landed a fixture map exercises `wall_material`/`roof_material` through the parser while the clear-weather state reproduces the authored day/night lighting values exactly
- [x] P0-058a | deps: none | deliverable: commit or delete orphaned Godot `.uid` sidecars for already-tracked scripts (`map_view_runtime_camera.gd`, dialogue/inventory/world-item splits, `test_sky_weather_3d.gd`) and either land or remove the untracked `tools/run_focused_godot_tests.gd` helper | verify: `git status` reports no untracked `.uid` files for tracked scripts and the focused-test helper is either documented in `docs/SETUP.md` or absent
- [x] P0-059 | deps: none | deliverable: scope decision for the first-person camera toggle, which shipped outside ADR 0007's fixed orthographic presentation and without a TODO task: either classify it as a developer-only inspection tool excluded from release UI and input help, or record an ADR amendment and give it the standard mouse-accessible discoverability entry point | verify: the decision is recorded in an ADR note or developer-tool doc, and release-facing UI either omits the toggle or exposes it per the discoverability policy with both mouse and shortcut activation covered by a test
- [x] P0-060 | deps: none | deliverable: stale-status sweep: `docs/reports/known_runtime_defects.md` re-triaged against the current slice (retire or re-confirm DEF-003, DEF-004, and DEF-005, which reference legacy `DoorNavigator` districts, and recheck the DEF-001 `--check-only` hang) and the `README.md` development-status section updated to reflect the completed D-003 demo interaction with D-004 packaging as the open step | verify: every retained defect reproduces on current HEAD with its recorded steps, every retired defect names the replacing system, and `python3 tools/generate_active_docs_report.py --check` passes
- [x] P0-061 | deps: none | deliverable: trustworthy Godot test harness and CI wrapper that fail on runtime `SCRIPT ERROR`, test-method interruption, parser/load errors, and unexpected engine errors instead of printing `PASS` after an exception; retain an explicit narrow allowlist only for documented shutdown-only DEF-002 diagnostics | verify: the clean-clone suite passes only when all test methods reach completion, seeded runtime and parser exceptions both exit nonzero, and the 13 runtime exceptions observed in the 2026-07-20 audit are reported as failures rather than 482 passes
- [x] P0-062 | deps: none | deliverable: current Godot runtime/test error cleanup: fix the typed-array error in `test_grid_region_merger.gd`, chunk-streaming dictionary access, missing HUD/view test nodes, `WorldItemController` placement type mismatch and stale `_update_pickup_cursor` call, plus Lower Town material errors and the invalid `input/ui_shift/deadzone` project setting; classify any remaining engine diagnostic as an intentional tested warning or defect | verify: clean import, main menu, `reval_east`, transition validation, blueprint validation, and all 482 or more Godot tests complete with zero unallowlisted `SCRIPT ERROR` or `ERROR` log lines
- [x] P0-062a | deps: none | deliverable: finish P0-062 full-suite exit: clear remaining map-quality patrol reachability failures on `south_quarter` / `toompea_quarter` / `north_quarter`, and any leftover unallowlisted engine ERROR lines outside the named P0-062 fixtures (106 tests already green under `tools/run_godot_checked.sh --require-test-summary` with `--filter` covering grid/chunk/world-item/HUD/camera/click/forge/sign/mesh/fortification) | verify: `tools/run_godot_checked.sh --require-test-summary p0-062a /Applications/Godot.app/Contents/MacOS/Godot --headless --path . --script tools/run_godot_tests.gd` reports 0 failures, 0 errors, and no unallowlisted `ERROR:` lines
- [x] P0-062b | deps: none | deliverable: restore historically grounded multi-street walkable patrol routes on `north_quarter`, `south_quarter`, and `toompea_quarter` in place of the temporary spawn-adjacent loops landed for P0-062a; keep spawn-clearance offsets that already pass `test_transition_spawn_clearance.gd` | verify: `test_map_quality_audit.gd::test_required_spawns_transitions_and_mandatory_points_are_reachable` passes with multi-street patrol coverage on each prototype
- [x] P0-063 | deps: P0-062a | deliverable: repository validation reconciliation: support numeric combat schema fields, repair the `mart_locked` dialogue edge, align `content/map_audit_manifest.json` with the archived warehouse and benchmark scene, bring `docs/reports/migration_matrix_p0_034.md` up to current scene coverage, regenerate the active Markdown report, and run full Python discovery in CI rather than only one content test module | verify: `python3 -m unittest discover -s tests/python -v`, both `validate_content.py` CI corpora, `verify_map_audit.py`, `verify_migration_matrix.py`, `verify_map_conversion_plan.py`, and `generate_active_docs_report.py --check` all pass from a clean clone
- [x] P0-063a | deps: none | deliverable: make active Markdown discovery ignore generated hidden/tool-cache directories such as `.pytest_cache` so local test artifacts cannot change the committed report counts | verify: a Python test seeds `.pytest_cache/README.md` under a fixture root and proves `collect_active_docs` excludes it from both active and excluded docs; `generate_active_docs_report.py --check` stays stable before and after pytest creates its cache
- [x] P0-069 | deps: none | deliverable: restore exact map conversion-plan coverage for the committed `scenes/tests/night_encounter_stub.tscn` by documenting its disposition and detailed inventory entry, or explicitly excluding non-map test scenes under a validator-owned rule | verify: `python3 -m unittest discover -s tests/python -v` passes all map-audit and map-conversion-plan tests, and `python3 tools/verify_map_conversion_plan.py` exits 0
- [x] P0-066 | deps: none | deliverable: resolve leftover shared-rig working-tree polish (henning/innkeeper regenerated glbs, `animation_overrides` docs/code, townswoman closeup captures, and related builder/test edits) by committing a coherent character-pipeline change or reverting to HEAD | verify: `git status` is clean for those paths and `godot --headless --path . --script tools/run_godot_tests.gd -- --filter=test_character_rig` passes
- [x] P0-067 | deps: none | deliverable: resolve leftover district-adjacency and terrain-mesh working-tree polish (`lower_town_slice` / `north_quarter` / `south_quarter` / `toompea_quarter` transition rewires, indexed ground mesh builder, and `test_map_view_3d_core` coverage) by committing a coherent map change with map-pipeline verification or reverting to HEAD | verify: `git status` is clean for those paths; `tools/run_map_pipeline_ci.sh audit` and `godot --headless --path . --script tools/run_godot_tests.gd -- --filter=test_map_view_3d_core` pass
- [x] P0-067a | deps: none | deliverable: review and resolve or explicitly document the retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on `north_quarter`, `south_quarter`, `toompea_quarter`, and `reval_harbor` before any chunk-streaming activation of those prototypes | verify: `tools/run_map_pipeline_ci.sh audit` either reports zero `MAP_CHUNK_BOUNDARY_AMBIGUOUS` for those maps or each remaining warning cites an owned decision in `docs/MAP_AUTHORING.md` / map conversion notes
- [x] P0-067b | deps: none | deliverable: review and resolve or explicitly document the retained `MAP_CHUNK_BOUNDARY_AMBIGUOUS` warnings on playable/slice maps `kalev_smithy` and `lower_town_slice` (and any other registered maps still emitting the warning after P0-067a) before treating object chunk streaming as production-ready on those maps | verify: `tools/run_map_pipeline_ci.sh audit` either reports zero `MAP_CHUNK_BOUNDARY_AMBIGUOUS` for those maps or each remaining warning cites an owned decision in `docs/MAP_AUTHORING.md` / a dated report under `docs/reports/`
- [x] P0-067c | deps: none | deliverable: production object-chunk-streaming readiness for playable slice maps `kalev_smithy` and `lower_town_slice`: either split every owned `MAP_CHUNK_BOUNDARY_AMBIGUOUS` subject into single-owner footprints with parity/route proof, or re-assert ADR 0010 lex-smallest ownership against the production 32x32 chunk size after human map review, then mark object chunk streaming production-ready on those maps | verify: `tools/run_map_pipeline_ci.sh audit` plus slice parity/route checks pass; `docs/MAP_AUTHORING.md` states object chunk streaming is production-ready on both slice maps or lists the remaining blocking subjects
- [x] P0-064 | deps: none | deliverable: repository storage hygiene decision and cleanup plan for the roughly 1.3 GiB tracked tree, including the 100 MiB `bin/rr.zip`, tracked `tools/__pycache__/*.pyc`, generated `build/benchmarks/*.json`, 662 MiB archive music, and quarantine/history source material; keep required asset provenance and source references intact | verify: `python3 tools/verify_storage_hygiene.py`, full Python discovery, and clean-clone import/export pass; policy identifies runtime/source/archive/quarantine/generated/release ownership; generated caches are untracked/ignored; remaining >=10 MiB standard-Git exceptions have byte/SHA-256/owner/follow-up records
- [ ] P0-070 | deps: P0-078 | deliverable: migrate the nine checksum-locked standard-Git binary exceptions plus the 662 MiB inactive audio archive and eligible historical/narrative sources to authenticated Git LFS or versioned external storage without deleting provenance or rights evidence | verify: every moved file has stable retrieval URL/object key, byte count, SHA-256, owner and license/approval metadata; a documented fetch recreates repository-relative runtime inputs; CI and a clean clone import/test/export pass; `docs/storage_binary_exceptions.json` is empty or contains only explicitly re-approved exceptions
- [x] P0-071 | deps: none | deliverable: reconcile `tools/quarantine_assets.py --check` drift for current active visual sources (building sprites, style-lock materials, KayKit texture, forge/food/furniture props, and `img/banner.jpg`) by approving each source with valid provenance or moving each source plus sidecar to its quarantine mirror without breaking active runtime references | verify: `python3 tools/quarantine_assets.py --check`, `python3 tools/validate_asset_sources.py`, `python3 tools/generate_asset_inventory.py --check`, clean import, and full Godot tests pass
- [x] P0-073 | deps: none | deliverable: reconcile the existing Archbishop's Garden map-audit/conversion baseline drift by aligning its scene target, activation gate, scene inventory, migration matrix, and conversion plan without activating the prototype for release | verify: `python3 -m unittest discover -s tests/python -v`, `python3 tools/verify_map_audit.py`, `python3 tools/verify_map_conversion_plan.py`, and `python3 tools/verify_migration_matrix.py` pass
- [x] P0-074 | deps: none | deliverable: reconcile the Viru Gate Foreland prototype across the map audit manifest, conversion plan, scene inventory, migration matrix, and canonical audit capture without enabling release traversal | verify: `python3 -m unittest discover -s tests/python -v`, `python3 tools/verify_map_audit.py`, `python3 tools/verify_map_conversion_plan.py`, `python3 tools/verify_migration_matrix.py`, and `python3 tools/verify_map_activation.py` pass
- [ ] P0-076 | deps: none | deliverable: finish tower wall-walk passage wiring (`round_tower` / `wall_walk_axis` allowlist, CSG passages, collision openings, builder call sites, and Lower Town parity) without regressing map pipeline or fortification tests | verify: `--filter=test_map_view_3d_fortification`, `--filter=test_map_tower_doors`, `--filter=test_map_rrmap_parser`, and `tools/run_map_pipeline_ci.sh audit` pass with zero unknown-typed-field diagnostics
- [ ] P0-078 | deps: P0-076 | deliverable: restore the clean-clone Godot baseline after current map/view/input drift: clear direction-sign runtime diagnostics, rebound mouse-confirm focus flow, remaining prototype transition/patrol reachability and adjacency failures, banner parametric geometry, Lower Town entrance/chunk-ownership assertions left after P0-076, and generated untracked fixture UIDs without weakening coverage | verify: after `rm -rf .godot`, `tools/run_godot_checked.sh clean-import Godot --headless --editor --quit` and `tools/run_godot_checked.sh --require-test-summary full-suite Godot --headless --script tools/run_godot_tests.gd` complete all 713 or more tests with zero failures, zero unallowlisted errors, and no generated untracked files
- [ ] P0-077 | deps: none | deliverable: allowed files `TODO.md`, `scripts/map/view3d/map_view_material_shaders.gd`, `tests/godot/test_coastal_sea_3d.gd`, and `docs/reports/images/view3d/reval_harbor_north_water_layers.png`; layer the existing 3D water material with deterministic sand, stone, algae, and deep-bed treatments plus depth-dependent spectral light attenuation and shallow caustics; preserve gameplay geometry, navigation, authored water IDs, weather waves, and celestial reflections | verify: focused water/material Godot tests pass with zero shader or script errors, and the committed rendering-capable harbour capture visibly separates shallow bed detail from dark deep water
- [x] P0-075 | deps: none | deliverable: repair the staged `sky_weather_3d.gd` compile regression by restoring or replacing the missing `_advance_gust()` implementation without regressing cloud or weather behavior | verify: the focused sky/weather tests and the full Godot suite load with zero script compile errors
- [x] P0-065 | deps: none | deliverable: architecture and file-ownership baseline documenting autoloads, state/content/map/view/UI boundaries and refactor thresholds; reconcile the stale expected count of 11 runtime scripts over 400 lines with the current inventory and explicitly review every current match, including the split building-mesh stack and rrmap statement parser, without creating parallel frameworks | verify: `docs/ARCHITECTURE.md` maps dependencies and ownership, names targeted extractions only where responsibilities are mixed, and each later refactor is protected by existing behavior tests
- [ ] P0-053 | deps: P0-072 | deliverable: slice surface and weathering kit: seamless, gameplay-readable textures for cobblestone, packed dirt, mud, grass, hay/straw, water and stone; historically grounded log, plank, lime-plaster and limestone walls; shingle, thatch and tile roofs; timber, iron and forge props; deterministic variants for wear, repairs, dirt, damp, discoloration and edge damage; wire the approved materials to every smithy and Lower Town surface with the frozen post-process grade | verify: asset lint and source/provenance rules pass for every texture; repeated adjacent surfaces do not share an obvious identical treatment; both slice maps render with zero untextured placeholders; matched day/night captures preserve material identity and gameplay value hierarchy
- [x] P0-037 | deps: none | deliverable: shared low-poly character rig proven on Kalev with retargeted idle, walk, forge strike, hammer attack, guard, hit, and fall animations, per-character variation via texture and equipment swaps, and a documented retarget procedure with speed budget (new NPC variant with core set under one working day; single new animation under one hour) | verify: one rig plays every required animation in-engine at the frozen character scale from any facing without per-direction assets, a second character variant is produced by swap only, and recorded production times meet the budget
- [ ] P0-038 | deps: P0-053 | deliverable: comparison report for import time, frame time, texture memory, navigation defects, animation reuse, and NPC-variant production time measured on the 3D view layer with the P0-053 surface kit, including minimum-hardware frame-time results and renderer-setting escalation if budgets are missed | verify: report contains repeatable procedure, hardware, raw measurements, and result
- [ ] P0-039 | deps: P0-053 | deliverable: blind gameplay-scale readability test of the 3D isometric candidate with at least five participants | verify: results record silhouette, interaction, depth, and motion recognition per target
- [ ] P0-040 | deps: P0-038,P0-039 | deliverable: approved engine, perspective, and visual-style decision plus approval of `docs/ART_BIBLE.md` v2; candidate: programmatic 3D isometric presentation with AI-generated materials per `docs/adr/0007-ai-generated-isometric-presentation.md`; not approved and no active-district conversion until dependencies and human sign-off pass | verify: decision freezes internal resolution, orthographic size, camera angles, character world scale, mesh-builder height rules, light angles, shadow settings, post-grade parameters, value hierarchy, day/night lighting, and the style-lock kit version
- [ ] P0-072 | deps: none | deliverable: sourced 1343 Reval environment target dossier for each retained urban map, with confidence-labelled street/property layout, built-to-open-space density ranges, ordinary and exceptional building typologies/materials, roof covers, period-appropriate cobbled-versus-packed-earth/mud/grass surface ranges, drainage, fences, vegetation, topography, and attested landmark states; explicitly separate archaeological or documentary evidence from later Old Town survivals and gameplay invention | verify: `docs/HISTORICAL_AUDIT.md` and map-specific source references provide at least one cited decision or an explicitly labelled unknown for every category and district; a human historical review signs the ranges before P1-036 or any district quality pass may use them as acceptance thresholds
- [x] P0-054 | deps: none | deliverable: legacy-document reconciliation pass for ADR 0008: status headers of `GAME-PILLARS.md`, `GAMEPLAY.md`, `GAMEPLAY-NIGHT.md`, `QUESTS.md`, `characters/README.md`, and `story/STORY.md` updated to link ADR 0008 and state which seeds are reactivated (factions, act structure, night missions, world locations) versus still superseded (meters, tower capture, magic systems, plague epilogue) | verify: `python3 tools/generate_active_docs_report.py --check` passes and each named file's status header links `docs/adr/0008-three-act-campaign-and-faction-scope.md`

## P1 - Tested runtime and content foundation

- [x] P1-007 | deps: none | deliverable: atomic one-slot manual save and phase-boundary autosave with one backup | verify: round-trip tests preserve all state and interrupted writes retain a loadable backup
- [x] P1-008 | deps: P1-007 | deliverable: save validation and migration harness | verify: tests cover truncated data, wrong types, unknown versions, and every released fixture
- [x] P1-009 | deps: none | deliverable: debug state inspector with deterministic reset and branch/phase jump | verify: a developer reaches every slice phase and valid branch without replaying earlier content
- [x] P1-011 | deps: none | deliverable: `DialogueRunner` supporting choices, conditions, effects, once-only lines, and phase barks | verify: content-only test dialogue changes state and the next conversation without custom NPC code
- [x] P1-009a | deps: P1-009 | deliverable: refresh `WorldItemController` and map overlays when `SessionState.debug_state_applied` fires so debug jumps update visible props without a scene reload | verify: applying `debug.reset.demo_post_pickup` in the forge removes the anvil spearhead immediately
- [x] P1-012 | deps: P1-011 | deliverable: dialogue UI with speaker, portrait, text, choices, continue, skip, backlog, and disabled-choice reason | verify: keyboard, mouse, and gamepad complete a branching dialogue at all supported text scales
- [x] P1-013 | deps: P1-012 | deliverable: dialogue settings for text speed, font size, contrast, subtitle background, and reduced motion | verify: settings persist across restart and affect the dialogue test scene
- [x] P1-014 | deps: P1-012 | deliverable: pseudo-localization and dialogue overflow test | verify: expanded pseudo-localized strings fit or scroll without clipping at target resolutions
- [x] P1-016 | deps: none | deliverable: journal showing current objective and discovered evidence without hidden outcomes | verify: journal updates from quest state and persists through save/load
- [x] P1-017 | deps: none | deliverable: authored morning, investigation, night, and reflection phase transitions | verify: phase tests update participating NPC placement, props, barks, patrols, and music hooks
- [x] P1-017a | deps: P1-017 | deliverable: gameplay triggers that call `PhaseDirector.advance_to_next_phase()` at authored bed/rest boundaries and quest `set_phase` effects | verify: debug preset jump from prologue to investigation morning updates an in-scene forge or Lower Town load without reload errors
- [x] P1-019 | deps: none | deliverable: `ForgeCommission` flow displaying customer, object, known purpose, materials, and discovered leverage | verify: one content-defined commission opens and resolves without quest-specific UI code
- [x] P1-019a | deps: P1-019,P1-011 | deliverable: forge interactable opens the content-defined `commission.watch_buckle_repair` flow through `ForgeCommissionController` without quest-specific UI code | verify: interacting at the smithy ledger or commission anchor opens the overlay, resolves to a forged record, and blocks movement until closed
- [x] P1-020 | deps: P1-019 | deliverable: honest, subtle-defect, and secret-feature modification support gated by facts or materials | verify: tests confirm unavailable options stay locked and chosen modification creates one explicit forged record
- [x] P1-021 | deps: P1-020 | deliverable: generic mechanism response to a forged modification | verify: changing content/state alters mechanism behavior without quest-specific branching code
- [ ] P1-022 | deps: P1-019,P0-040 | deliverable: forge feedback for heat, hammer rhythm, quench, maker stamp, and object reveal | verify: automated scene trace emits the five feedback events in order and the forge scene exposes no temperature, strike-accuracy, or timing-score state
- [x] P1-023 | deps: none | deliverable: coherent player movement and action state machine with input buffering | verify: automated input test finds no stuck state across movement, attack, guard, dodge, hit, and recovery
- [x] P1-023b | deps: none | deliverable: empty-right-hand punch using the shared-rig unarmed animation, one timed melee impact, frontal range filtering, and the common damage reaction, bound to left mouse, Space, and gamepad X | verify: automated tests prove one impact per attack, only in-front/in-range targets take damage, health clamps at zero, hit reaction starts, and an equipped right-hand item cannot borrow unarmed damage
- [x] P1-023a | deps: P1-023 | deliverable: player movement can gently push non-hostile NPCs out of blocked pathways and entrances while respecting world collision and NPC navigation | verify: automated movement scenes confirm the player can clear a narrow route blocked by an NPC, cannot push the NPC through walls or closed doors, and the displaced NPC resumes its normal idle or navigation behavior without either actor becoming stuck
- [x] P1-024a | deps: P1-023b | deliverable: data-driven attack profile contract selecting animation, damage, reach, impact timing, stamina cost, and damage type from the item equipped in either hand, with the unarmed profile as fallback | verify: swapping empty hand, hammer, and a test item changes combat behavior from content/state without branching on item IDs
- [x] P1-024b | deps: P1-024a | deliverable: hammer light attack and charged attack using the shared rig and equipped forge hammer | verify: combat tests demonstrate distinct telegraph, impact timing, damage, reach, and stamina costs while release-before-threshold remains a light attack
- [x] P1-024c | deps: P1-024a | deliverable: shared health, death, guard/parry, and dodge resolution for player and combat actors | verify: tests cover damage clamping, invulnerability windows, guarded stamina loss, parry timing, death notification, and no duplicate hit per swing
- [x] P1-024d | deps: none | deliverable: Iron technique layered onto the same attack and damage contracts | verify: combat test demonstrates the authored Iron behavior without a parallel state machine or item-specific enemy code
- [x] P1-024e | deps: P1-024d | deliverable: mouse-reachable forge-technique equip/clear control in the persistent quick-access menu (or equally visible contextual UI) so players can select Iron without reading source or relying on a hotkey alone; keyboard shortcut optional and secondary | verify: mouse activation equips and clears Iron; `GameState.equipped_forge_technique()` updates; attack profiles gain or lose guard-pierce; documented shortcut if any still works
- [x] P1-024 | deps: P1-024d | deliverable: integrate hammer light attack, charged attack, guard/parry, dodge, and Iron technique into one playable combat test room with readable feedback | verify: keyboard/mouse and gamepad combat runs demonstrate damage, stamina costs, invulnerability, parry, Iron behavior, item use, and recovery without stuck states
- [x] P1-025 | deps: P1-024 | deliverable: shared watchman and sergeant enemy state machine | verify: both archetypes patrol, detect, telegraph, attack, react, and disengage without duplicated controller code
- [x] P1-025a | deps: none | deliverable: wire watchman/sergeant archetypes into `scenes/tests/combat_room.tscn` (or a night-encounter stub) with readable feedback for detect/telegraph/attack so P1-024 room stays the combat smoke host | verify: headless combat-room (or encounter) tests show both archetypes complete one detect-to-disengage loop without stuck states
- [x] P1-026 | deps: none | deliverable: authored surrender, escape, or bypass outcome support | verify: one encounter resolves without killing and updates the same quest state used by combat
- [x] P1-027 | deps: none | deliverable: combat reset checkpoint after failure | verify: player retries the encounter without replaying completed dialogue or corrupting quest state
- [x] P1-027a | deps: P1-027 | deliverable: wire `EncounterCheckpoint` into the night-encounter stub (P1-025b / P2-009 host) so player death outside the combat room offers the same mouse-reachable Retry path without dialogue replay or quest corruption | verify: headless night-stub death restores armed quest/dialogue state and remains unreachable from release demo navigation
- [x] P1-025b | deps: P1-025a | deliverable: optional night-encounter stub scene that reuses `CombatRoomEnemy` / `EnemyCombatStateMachine` outside the combat room so P2-009 can host a non-dummy watch route without forking AI | verify: headless encounter stub boots both archetypes through one detect-to-disengage loop and remains unreachable from release demo navigation
- [x] P1-026a | deps: P1-026 | deliverable: content-authored encounter outcome package (JSON) for `encounter.watch_checkpoint` loaded through ContentDB so P2-009 / P5-004 can swap quest mappings without code edits | verify: `tools/validate_content.py` accepts a valid fixture; headless test resolves escape from content and rejects an unknown kind without mutating quest state
- [x] P1-026b | deps: P1-026a,P1-025b | deliverable: mouse-reachable surrender/escape/bypass controls on `scenes/tests/night_encounter_stub.tscn` that resolve via the ContentDB `encounter.watch_checkpoint` package (same quest contract as the combat room) so P2-009 can host non-lethal closes outside the smoke room | verify: headless night-stub tests resolve escape from content without killing and reject an unknown kind without mutating quest state; stub remains unreachable from release demo navigation
- [x] P1-028 | deps: none | deliverable: remappable keyboard/mouse and gamepad actions with focus navigation | verify: all slice actions can be rebound and completed on both input methods
- [ ] P1-029 | deps: P0-040 | deliverable: automated asset lint for texture dimensions and seamless tiling, texel density, naming, mesh pivots and character scale, portrait dimensions, and source-manifest provenance rows | verify: valid fixture passes and one seeded error per rule fails CI
- [x] P1-030 | deps: none | deliverable: repeatable performance scene and report command | verify: command reports frame time, memory, actor count, and target hardware metadata
- [x] P1-031 | deps: none | deliverable: world/scene map overlay showing the current scene and how authored scenes are connected, toggleable with the `M` input action (default keyboard key `M`) | verify: pressing `M` opens and closes the overlay from every playable scene, highlights the current scene, and displays connections matching the active transition manifest with no missing or stale scene nodes
- [x] P1-031a | deps: P1-031 | deliverable: mouse click on a connected district-map node travels through `DoorNavigator` to a registered default spawn for that edge while the current-scene node stays non-interactive | verify: headless test selects a neighbor of `reval_east` or `forge` and records `go_to_scene` with an active manifest spawn; unknown/disconnected nodes do not travel
- [x] P1-031b | deps: P1-031a | deliverable: gamepad/keyboard focus navigation on district-map neighbor nodes with Confirm/ui_accept traveling the focused connected district (current scene stays non-interactive) | verify: headless test focuses a neighbor of `reval_east` or `forge`, activates ui_accept, and records the same `go_to_scene` spawn contract as mouse click; focus cannot activate the current-scene node
- [x] P1-031c | deps: P1-031b | deliverable: visible focus ring / selected-state styling on the focused district-map neighbor so keyboard and gamepad users can see which node Confirm will travel, matching mouse hover readability | verify: headless test asserts the focused travelable node uses a distinct modulate or theme override from unfocused travelable neighbors while the current-scene highlight stays unchanged
- [x] P1-032 | deps: none | deliverable: in-scene minimap HUD for every `MapSceneBootstrap` map showing terrain, blocked areas, exits, and live player position, toggleable with the `toggle_minimap` input action (default keyboard key `N`) | verify: minimap appears in the bottom-right corner on forge and Lower Town maps, tracks player movement, marks transition exits, and toggles with `N`; `godot --headless --path . --script tools/run_godot_tests.gd` includes `tests/godot/test_minimap.gd`
- [x] P1-034 | deps: none | deliverable: split `WorldMapOverlay` local-map and fast-travel rendering/focus internals into focused child views while preserving the `WorldMapOverlay` public facade, stable node names, and `WorldMapController` ownership of `DoorNavigator` travel side effects | verify: `test_world_map_overlay` and `test_quick_access_menu` pass unchanged for mouse, keyboard, and gamepad; `WorldMapOverlay` no longer builds or updates both mode-specific node trees itself; no second map model or travel path is introduced
- [x] P1-035 | deps: none | deliverable: add one ordered `SessionState.state_replaced(previous, current, reason)` notification for every live `GameState` replacement (manual load and debug preset), then rebind long-lived consumers such as `PhaseDirector`, `MapViewRuntime`, and `WorldItemController` without duplicating replacement paths | verify: headless tests replace state through both load and debug flows and prove world items, equipment view, phase presentation, inventory/journal reopen, and subsequent saves all read and mutate only the new state; each replacement emits once after the bag is rebound to `ContentDB`
- [ ] P1-036 | deps: P0-040,P0-072,P1-029 | deliverable: evidence-driven urban map composition audit that reports street/road surface ratios by material, built-footprint and open-space density, repeated building style/material usage, terrain/vegetation coverage, local elevation range, empty contiguous regions, and required landmark presence against the map-specific P0-072 ranges without treating cobblestone as the default ground fill | verify: valid Lower Town and prototype fixtures pass at their documented ranges; one seeded excess-cobble, sparse-building, missing-landmark, flat-relief, and repeated-style violation each fails with map ID, measured value, expected range, and source-decision reference
- [x] P1-033 | deps: none | deliverable: persistent in-game quick-access menu exposing the existing inventory, journal, and manual save through visible mouse controls while retaining the `I` and `J` shortcuts | verify: every scene using `player.tscn` displays Inventory, Journal, and Save game actions; mouse activation opens or saves the existing systems, direct shortcuts remain functional, and inventory and journal cannot overlap

## P2 - Vertical-slice production (the playable MVP)

- [ ] P2-003 | deps: P0-040,P0-053,P0-072,P1-029 | deliverable: modular 3D medieval Reval environment kit for forge, street/well, brewery and checkpoint: historically grounded log, plank, plastered-timber and limestone building families with varied footprints, heights, gables, doors, windows, chimneys and shingle/thatch/tile roofs; fences/gates, drainage, carts, barrels, crates, signs, workshops, yards, grass/shrub/tree modules and the P0-053 worn material variants; preserve a separate exceptional-landmark path so churches, guild halls, gates and civic buildings are not assembled as ordinary houses | verify: the four slice spaces are assembled from shared modules without bespoke camera, scale or material exceptions; at least three visibly distinct ordinary facade/roof combinations coexist in one gameplay capture without anachronistic Fachwerk repetition; every module passes asset lint, provenance, pivot, collision, navigation and day/night readability checks
- [ ] P2-004 | deps: P0-040,P1-029 | deliverable: approved low-poly character models and AI-generated portraits for Kalev, Mart, Aita, Kaja, Henning, and Jürgen | verify: all six use the shared P0-037 rig and animation library, differ only by texture/equipment/silhouette swaps, pass asset lint, and match art-bible silhouette and palette rules
- [ ] P2-005 | deps: P2-004 | deliverable: approved watchman and sergeant visual variants | verify: both are distinguishable at gameplay scale without relying on color alone
- [ ] P2-006 | deps: P1-011,P1-017,P1-019,P1-023 | deliverable: playable `The Maker's Mark` tutorial | verify: new game teaches movement, interaction, commission, maker-mark incident, and all three ledger outcomes
- [ ] P2-007 | deps: P2-003,P2-004,P2-005,P1-011,P1-017,P1-021,P1-026 | deliverable: playable daytime `A Bitter Brew` investigation | verify: player can inspect well, brewery, merchant supply, and checkpoint and produce the expected evidence states
- [ ] P2-008 | deps: P2-007,P1-022 | deliverable: playable `A Bitter Brew` commission with honest, defective, and secret-feature results | verify: each result creates the expected forged record and communicates it audiovisually
- [ ] P2-009 | deps: P2-008,P1-026,P1-027 | deliverable: playable night consequence with combat and non-combat routes | verify: forged record changes encounter behavior and every route reaches a valid aftermath
- [ ] P2-010 | deps: P2-009,P1-017 | deliverable: three visible `A Bitter Brew` aftermath states | verify: brewery state, Aita state, Mart reaction, and patrol barks differ according to outcome
- [ ] P2-011 | deps: P1-011,P1-017 | deliverable: Hingepuu reflection screen with Duty, Fury, Mercy, and visual consequence marks | verify: reflection reacts to prior state, applies one allowlisted effect, and provides equivalent plain-text information
- [ ] P2-012 | deps: P2-006,P2-007,P2-008,P2-009,P2-010,P2-011,P2-021 | deliverable: complete 30-45 minute vertical-slice flow | verify: new game reaches all three aftermath outcomes without debug intervention
- [ ] P2-022 | deps: P0-072,P1-036,P2-003 | deliverable: historically grounded layout and terrain-density pass for the playable `lower_town_slice`: retain required smithy, brewery, cistern, checkpoint, transition and quest routes while replacing oversized empty regions and excess blanket cobblestone with sourced narrow streets/lanes, dense property footprints, service yards, packed earth, mud, sparse grass/hay, drainage, fences, trees/vegetation where plausible, and locally varied terrain elevation that remains accessible | verify: P1-036 reports every composition measure within the P0-072 Lower Town ranges and no unowned empty region above the documented threshold; route, patrol, transition, collision, chunk-streaming and navigation tests remain green; matched day/night captures show dense readable streets and terrain variation without hiding player, interactables or entrances
- [ ] P2-023 | deps: P2-022 | deliverable: Lower Town architecture and landmark art pass applying P2-003 building/material variety and worn or repaired details across ordinary properties, plus historically reviewed exceptional high-detail models and silhouettes for every attested landmark visible in the slice, including St. Catherine's church and the 1343 Viru Gate state; special buildings must not be scaled-up ordinary-house variants | verify: map audit finds no unexplained repeated facade/material run and all required landmark IDs; close and gameplay-scale captures distinguish ordinary log/plank/plaster/limestone buildings, roof covers, wear states and each landmark in day/night; human historical and art review signs the 1343 silhouettes, while existing routes, collision, occlusion and performance budgets pass
- [ ] P2-024 | deps: P2-023 | deliverable: lightweight ambient urban-fauna kit and authored Lower Town placements for cats, dogs, horses and rats, with scale/coat/pose variation, simple idle/wander/flee or tether behavior, deterministic density limits and no combat or pet system | verify: one day/night Lower Town walkthrough observes every required species in historically plausible contexts; seeded movement tests prove animals never block required routes, transitions, doors or interaction anchors and unload safely with map chunks; performance and asset-lint budgets pass, and disabling fauna leaves gameplay state unchanged
- [ ] P2-025 | deps: P0-053,P2-003 | deliverable: district-life static prop kit and authoring contract: add gameplay-readable prop kinds and 3D mesh builders for trade-specific dressing (minimum set: `fishing_nets`, `fish_drying_rack`, `smoke_rack`, `fish_splitting_table`, `boat_timber_stack`, `rope_coil`, `sail_cloth_bale`, `cooper_staves`, `malt_sack_pile`, `brewery_keg_stack`, `charcoal_pile`, `iron_scrap_pile`, `weapon_rack`, `herb_drying_rack`, `market_goods_pallet`, `salt_pile`, `tanning_frame`, `wash_tub`); document allowed kinds, footprint rules, and P0-072 sourcing notes in `docs/MAP_AUTHORING.md`; retire barrel/crate stand-ins where a dedicated kind exists | verify: parser/compiler accept every new kind; focused Godot tests render each kind in 3D day/night without collision/nav regressions; asset lint and provenance pass; one seeded invalid kind fails validation with a stable diagnostic code
- [ ] P2-026 | deps: P2-025,P2-023 | deliverable: Lower Town slice district-life dressing on `lower_town_slice` and `kalev_smithy`: smithy yard (`charcoal_pile`, `iron_scrap_pile`, scrap tongs/readable forge clutter), brewery service yard (`brewery_keg_stack`, `malt_sack_pile`), cistern/checkpoint apron (`wash_tub`, watch barrels kept only where barrels are correct), and at least three ordinary house frontages on the playable route that read as distinct crafts through adjacent static props only; preserve every stable prop, anchor, and quest route ID | verify: day/night gameplay captures distinguish forge, brewery, and checkpoint yards without UI labels; `--filter=test_lower_town_slice_map` and smithy map tests stay green; no new prop blocks `street_start`, evidence, transition, or patrol reachability; map audit lists zero barrel placeholders where a P2-025 kind is defined for that trade
- [ ] P2-013 | deps: P2-012 | deliverable: slice dialogue reduced to at most 2,500 approved words | verify: generated word-count report excludes IDs/metadata and reports 2,500 or fewer spoken/displayed words
- [ ] P2-014 | deps: P2-012 | deliverable: slice soundtrack reduced to at most 12 minutes of unique approved music | verify: manifest reports duration, reuse, streaming settings, and rights for every included track
- [ ] P2-015 | deps: P2-012 | deliverable: quest/tool pouch displaying at most three slice quest items | verify: every branch completes without exceeding three visible quest-item slots
- [ ] P2-016 | deps: P2-012,P1-007,P1-008 | deliverable: save/reload coverage at every phase and branch boundary | verify: automated matrix preserves quest, modification, characters, pressures, player state, and phase
- [ ] P2-017 | deps: P2-012,P1-028 | deliverable: end-to-end keyboard/mouse and gamepad completion | verify: recorded test runs complete every required action without input fallback
- [ ] P2-021 | deps: P1-030,P2-023 | deliverable: visual-parity and gameplay-parity gate for the converted smithy and Lower Town slice with matched captures, annotated anchor accounting, topology traces, collision and navigation overlays, Y-sort and fade cases, and identical-framing day/night review | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `docs/reports/map_conversion_parity.md`, `docs/reports/images/map_conversion_smithy_before.png`, `docs/reports/images/map_conversion_smithy_after_day.png`, `docs/reports/images/map_conversion_smithy_after_night.png`, `docs/reports/images/map_conversion_lower_town_before.png`, `docs/reports/images/map_conversion_lower_town_after_day.png`, `docs/reports/images/map_conversion_lower_town_after_night.png`, `tools/verify_map_conversion_parity.py`, `tests/python/test_verify_map_conversion_parity.py` | constraints: semantic parity rather than pixel tracing, no map or runtime-art edits, no acceptance by hue alone, unresolved parity failures block P2-012 | verify: `python3 tools/verify_map_conversion_parity.py` and `python3 -m unittest tests.python.test_verify_map_conversion_parity -v` pass with 100 percent anchor accounting, all required route pairs reachable, all hard exclusions blocked, zero spawn overlaps, zero missing references, and signed human review of composition, depth, readability, and day/night value hierarchy

## P3 - Slice validation, accessibility, performance, and release

- [ ] P3-001 | deps: P2-012 | deliverable: automated traversal of every valid slice branch and deliberate invalid state | verify: traversal report shows all intended endings reachable and all invalid transitions rejected
- [ ] P3-002 | deps: P2-012 | deliverable: first external playtest round with at least five players focused on comprehension | verify: report records completion time, missed evidence, unclear prompts, deaths, and explanation of the core loop
- [ ] P3-003 | deps: P3-002 | deliverable: fixes for comprehension blockers discovered in round one | verify: every critical/high finding is fixed or explicitly rejected with rationale
- [ ] P3-004 | deps: P3-003 | deliverable: second external playtest round with at least five players focused on choice impact | verify: report records branch distribution, noticed consequences, character understanding, and perceived choice quality
- [ ] P3-005 | deps: P3-004 | deliverable: removal or rewrite of choices that differ only in wording or reward | verify: branch review finds a distinct state or consequence for every retained major choice
- [ ] P3-006 | deps: P3-005 | deliverable: third external playtest round with at least five players focused on usability and polish | verify: report shows no unresolved critical usability issue
- [ ] P3-007 | deps: P1-012,P1-013,P1-028,P2-012 | deliverable: accessibility options for remapping, hold/toggle, text speed, scalable text, subtitle background, focus contrast, screenshake, and reduced flashing | verify: accessibility checklist passes at supported resolutions and on both input methods
- [ ] P3-008 | deps: P3-007 | deliverable: information design with no required color-only, audio-only, or prior-history dependency | verify: accessibility review completes slice with color removed, audio muted, and no external historical explanation
- [ ] P3-009 | deps: P2-014 | deliverable: approved forge, town, tension, night, and consequence music stems with runtime streaming | verify: runtime loads only approved tracks and music memory remains within recorded budget
- [ ] P3-010 | deps: P2-012 | deliverable: normalized forge, footsteps, UI, combat, gate, crowd, and environment sound families with concurrency limits | verify: loudness report passes and stress scene produces no uncontrolled sound stacking
- [ ] P3-011 | deps: P1-030,P2-012 | deliverable: optimized slice meeting the explicit minimum-hardware target | verify: busiest scene sustains the recorded frame-time and memory budgets with headroom
- [ ] P3-012 | deps: P2-012,P3-007,P3-011 | deliverable: tested desktop export set and honest supported-platform declaration | verify: each declared platform installs, starts, saves, loads, and exits without critical error
- [ ] P3-013 | deps: P2-012 | deliverable: third-party notices and final slice asset/license report | verify: every exported non-original asset maps to a notice and approved manifest row
- [ ] P3-014 | deps: P3-001,P3-006,P3-008,P3-012,P3-013 | deliverable: signed vertical-slice gate report containing every acceptance criterion copied from README | verify: report records pass evidence for each criterion, including at least five players explaining forge-to-consequence and at least four rating the major choice difficult for narrative rather than UI reasons
- [ ] P3-015 | deps: P3-014 | deliverable: tagged vertical-slice release with frozen compatible save and content schema versions | verify: release tag rebuilds from clean clone and loads its published save fixture

## P4 - Act 1: The Simmering City (after slice approval)

Scope per ADR 0008: Act 1 extends the approved slice into the full five-cycle campaign, adds the faction ledger, the quest content pipeline, the market/civic and north quarters, and two faction quest lines, and ends at St. George's Night as a recorded act boundary rather than a game ending.

- [ ] P4-001 | deps: P3-014 | deliverable: approved branch map and state table for `The Bell and the Chain` | verify: map reuses commission, mechanism, gate, patrol, and consequence systems without a new major framework
- [ ] P4-002 | deps: P4-001 | deliverable: playable and playtested `The Bell and the Chain` cycle | verify: gate-chain modification changes act-climax-ready state and the cycle passes its branch matrix
- [ ] P4-003 | deps: P4-002 | deliverable: approved branch map and state table for `Bread and Iron` | verify: map reuses evidence, supplier, inventory, and location-state systems within campaign budgets
- [ ] P4-004 | deps: P4-003 | deliverable: playable and playtested `Bread and Iron` cycle | verify: one named family receives a visible consequence in every valid outcome
- [ ] P4-005 | deps: P4-004 | deliverable: approved branch map and state table for `The Price of a Name` | verify: map reuses relationships, detention, false evidence, and Hingepuu without a new major system
- [ ] P4-006 | deps: P4-005 | deliverable: playable and playtested `The Price of a Name` cycle | verify: Mart, Kaja, Henning, and Kalev receive causally consistent state changes in every branch
- [ ] P4-007 | deps: P4-006 | deliverable: one approved ambiguous folklore quest introducing Ellen, Ember or Root | verify: quest remains understandable without literal magic and introduces no second-game subsystem
- [ ] P4-016 | deps: P3-014 | deliverable: faction ledger: per-faction standing for the eight active factions (README roster) derived only from explicit recorded events — forged records, quest outcomes, discoveries, betrayals — with a visible ledger UI and no aggregate meter | verify: content-only test events change standing, standing gates at least one quest, price, and route in tests, and GameState contains no universal morality or balance-of-power value
- [ ] P4-017 | deps: P1-017,P4-016 | deliverable: district pressure states: per-district patrol density, prices, and bark sets driven by explicit flags and faction standing | verify: phase tests show two districts diverge visibly from the same base state under different ledger fixtures
- [ ] P4-018 | deps: P1-011,P3-014 | deliverable: quest content pipeline: agent-authorable quest package format (quest JSON, dialogue, branch map, spawn/anchor bindings) plus a generator that emits branch-traversal tests from the package | verify: an example quest added as pure content passes `tools/validate_content.py` and its generated traversal test without engine code changes
- [ ] P4-019 | deps: P0-040,P2-021,P3-014,P4-022 | deliverable: unified Central District and Town Hall market square activation from the P4-014 prototype through the parity and activation gates (approval artifact: ADR 0008) | verify: `python3 tools/verify_map_activation.py`, the transition verifier, and traversal/collision tests pass with the new destinations, and day/night captures pass parity review
- [ ] P4-020 | deps: P0-040,P2-021,P4-019,P4-023 | deliverable: north quarter activation from the P4-015 prototype through the same gates (approval artifact: ADR 0008) | verify: activation guard, transition verifier, and traversal/collision tests pass, and day/night captures pass parity review
- [ ] P4-021 | deps: P4-016,P4-018,P4-019 | deliverable: two Act 1 faction quest lines (Livonian Order and Black Cloaks), three quests each, authored as P4-018 content packages reusing slice systems | verify: both lines complete via generated traversal tests, every quest writes ledger events, and at least one quest per line resolves through a night consequence encounter with combat and non-combat routes
- [ ] P4-022 | deps: P0-072,P1-036,P2-003,P2-023,P3-014 | deliverable: Central District and market/civic prototype environment pass replacing broad empty or blanket-cobble regions with the sourced 1343 street/property pattern, dense merchant and service frontages, yards, surface/vegetation/relief variation and exceptional high-detail models for the early Town Hall, Holy Spirit chapel-almshouse and documented guild buildings; keep the map inactive until P4-019 | verify: P1-036 passes every Central District range and landmark requirement; no unexplained empty region or repeated facade run exceeds its threshold; navigation, transition, chunk and activation guards pass; signed day/night close and gameplay-scale captures confirm historically distinct landmark silhouettes and ordinary-building variety
- [ ] P4-023a | deps: P0-072,P1-036 | deliverable: Monastery District street and ground-surface planning pass on `monastery_quarter`: keep packed-earth base (not map-wide cobble), cobble limited to Pikk/Lai/civic/vene primary spines, mud or packed-earth secondary lanes, compact stone closes at St Michael / St Olaf / guild thresholds only, garden and wall-margin grass within HISTORICAL_AUDIT monastery ranges, plus localized drainage gutters; preserve all stable IDs, transitions, patrols and anchors; keep `active=false` | allowed files: `content/maps/monastery_quarter.rrmap`, `tests/godot/test_monastery_quarter_prototype_map.gd`, `docs/MAP_CONVERSION_PLAN.md`, `docs/HISTORICAL_AUDIT.md`, `TODO.md` | verify: `--filter=test_monastery_quarter_prototype_map` passes including earth-base and surface-share assertions; P1-036 monastery surface measures land inside the signed P0-072 ranges once that gate exists; patrol/transition/spawn clearance stay green; audit capture shows readable streets against earth/garden rather than a cobble field
- [ ] P4-023b | deps: P4-023a,P2-003 | deliverable: Monastery District ordinary fabric pass filling historically supportable merchant/service property rows and rear yards along Pikk/Lai, adding fences/plot edges, trees/vegetation where plausible, local terrain relief, and varied ordinary materials/wear without converting precinct interiors into generic house fill; keep inactive | verify: P1-036 monastery density/variety/relief ranges pass; required routes/patrols remain connected; signed day/night gameplay captures show dense street frontage beside open convent/church closes without oversized empty cobble fields
- [ ] P4-023c | deps: P0-072,P2-003,P4-023a | deliverable: exceptional high-detail St Olaf church landmark model for the compact 1343 mass (massive west tower and completed 1330-era vault work; omit 15th-century chancel, basilica and giant spire); replace the current ordinary `house.church` silhouette while preserving `st_olaf_silhouette` / `st_olaf_frontage` stable IDs | verify: map audit requires the landmark ID; close and gameplay-scale day/night captures distinguish St Olaf from ordinary limestone houses; human historical/art review signs the 1343 silhouette; nav/collision/occlusion budgets pass
- [ ] P4-023d | deps: P0-072,P2-003,P4-023a | deliverable: exceptional St Michael's Cistercian convent precinct model (church/convent mass, service wing and garden close) using low-detail placeholders only where 1343 above-ground mass remains labelled unknown; preserve convent building and `monastery_close` stable IDs | verify: audit requires convent landmark presence; signed captures distinguish the precinct from merchant rows and show garden/service ground rather than house fill; routes through convent_lane stay walkable
- [ ] P4-023e | deps: P0-072,P4-023a | deliverable: correct Great Guild / Brotherhood / Blackheads frontages on `monastery_quarter` to period-safe generic merchant or St Olaf craft-guild masses (present Great Guild Hall 1407-10 and Blackheads institutional forms are later); keep stable building IDs, retarget styles/labels/source notes, and do not claim monumental 15th-century guild halls as 1343 fabric | verify: HISTORICAL_AUDIT landmark exclusions are reflected in map source comments and conversion-plan inventory; captures no longer read as the later Great Guild / Blackheads monuments; navigation and IDs unchanged
- [ ] P4-023 | deps: P4-022 | deliverable: North/Merchant district prototype environment pass preserving Pikk/Lai routes while replacing its map-wide cobblestone base with sourced street/property pattern, work yards, surface/vegetation/relief variation, ordinary material wear, and exceptional models for the Coastal Gate and other P0-072-required north landmarks; Monastery work is owned by P4-023a through P4-023e; keep both maps inactive until their activation gates | verify: P1-036 passes map-specific density, surface, variety, relief and landmark ranges for `north_quarter`; all required routes/patrols remain connected; signed day/night captures distinguish each landmark and show no oversized unexplained empty area or blanket cobblestone field
- [ ] P4-024 | deps: P4-023,P4-023b | deliverable: South/Knights district prototype environment pass with sourced irregular lanes, dense eastern ward and service plots, period road-surface balance, fences/stable yards, vegetation and relief, varied worn architecture, and exceptional models for Rataskaev well, the knights' complex and P0-072-required landmarks; keep the map inactive | verify: P1-036, navigation, patrol, collision, transition and activation guards pass; signed day/night captures meet the district ranges and distinguish landmarks without turning ordinary houses into monumental models
- [ ] P4-025 | deps: P4-024 | deliverable: Toompea and Archbishop's Garden prototype environment pass using sourced plateau/slope relief, sparse-versus-built precinct balance, garden/orchard vegetation and enclosures, varied noble/canonical/service architecture and wear, and exceptional high-detail models for St. Mary's Cathedral, the 1343 castle state and P0-072-required ecclesiastical landmarks; keep both maps inactive | verify: terrain-height continuity at transitions, P1-036, navigation and activation guards pass; signed day/night captures read Toompea as an elevated fortified hill and distinguish every required landmark at gameplay and close scale
- [ ] P4-026 | deps: P4-025 | deliverable: Trade and Fishing Harbour prototype environment pass replacing empty aprons and excess paving with sourced quays, mud/packed-earth lanes, timber work yards, warehouses/sheds, cranes, nets, boats, fences and sparse coastal vegetation, plus varied weathering and exceptional high-detail Great Coast Gate treatment; keep both maps developer-only unless separately activated | verify: P1-036 uses harbour-specific rather than inland density/surface ranges; water exclusion, routes, transitions, collision, chunk and activation guards pass; signed day/night captures distinguish the two working harbours and show historically plausible land-to-road balance without oversized unexplained empty regions
- [ ] P4-028a | deps: P2-025,P4-026 | deliverable: Kalamaja fishing-shore district-life dressing on `reval_harbor_east`: replace every `drying_nets_*` barrel placeholder with `fishing_nets` and `fish_drying_rack`; add `fish_splitting_table`, `smoke_rack`, `salt_pile`, `boat_timber_stack`, and net-yard fence clutter beside `fisher_hut_*`, `net_house_mid`, `smoke_shed_west`, `boatwright_shed`, and `salt_shed_east`; keep moored `fishing_boat` props and walkable pier decks unchanged | allowed files: `content/maps/reval_harbor_east.rrmap`, `scripts/map/view3d/map_view_mesh_builder_props.gd`, `scripts/map/map_types.gd`, `docs/MAP_AUTHORING.md`, `docs/reports/reval_harbour_1343_research.md`, `tests/godot/test_reval_harbor_map.gd`, `tests/godot/test_map_view_fishing_boat.gd`, `TODO.md` | verify: `--filter=test_reval_harbor_map` passes; audit/day/night captures show three readable net yards, smoke/salt/boatwright yards, and working boats without relying on generic barrels; routes, pier walkability, and `kalamaja_shore` anchor stay green; map remains `active=false`
- [ ] P4-028b | deps: P2-025,P4-026 | deliverable: Coastal Gate merchant-landing district-life dressing on `reval_harbor_north`: cargo-yard `cargo_crates`/`trade_goods` clusters beside `cargo_shed_*`, `rope_coil` and tally ground at the rope bales, reversible crane marker kept readable, small landing boats at jetty tips, and Hanseatic `merchant_boat` roadstead left as scenery only per harbour research; no boarding mechanic | verify: day/night captures distinguish gate descent, cargo yard, rope ground, and roadstead; `--filter=test_reval_harbor_map` passes; no prop blocks the gate-to-shore route or `great_coast_gate` landmarks
- [ ] P4-028c | deps: P2-025,P4-023 | deliverable: North/Merchant ward workshop-life dressing on `north_quarter`: specialize named workshop rows (`sailmakers_loft`, `cooperage_shed`, `ropemakers_shed`, warehouse courts) with `sail_cloth_bale`, `cooper_staves`, `rope_coil`, `market_goods_pallet`, and pen/yard props that match building IDs; retain cattle/sheep pens until **P4-023g** | verify: gameplay-scale captures distinguish sail, cooper, rope, and warehouse yards along Pikk/Lai without reading every shed as a generic house; north-quarter map tests and patrol reachability stay green
- [ ] P4-028d | deps: P2-025,P4-023b | deliverable: Monastery District service-life dressing on `monastery_quarter`: garden/service props at `convent_well`, `guild_stall`, workshop rows (`herb_drying_rack`, `wash_tub`, `market_goods_pallet`, timber/service carts) that read merchant lane versus convent close; no house fill inside `monastery_close` | verify: day/night captures show dense Pikk/Lai frontage beside open convent garden ground; monastery quarter tests stay green; props do not block `convent_lane` or required patrols
- [ ] P4-028e | deps: P2-025,P4-022 | deliverable: Central market/civic district-life dressing on `market_civic_quarter`: differentiate `fish_stall`, `cloth_stall`, `grain_stall`, and `pottery_stall` with trade-specific static goods (`market_goods_pallet`, `fish_splitting_table`, cloth bales, grain sacks, pottery crates) plus weigh-table and notice-cart clutter around `civic_well`; keep Town Hall and Holy Spirit landmarks exceptional, not stall props | verify: one market-square capture shows at least four visually distinct stall trades; market prototype tests and activation guard remain `active=false`
- [ ] P4-028f | deps: P2-025,P4-024 | deliverable: South/Knights ward district-life dressing on `south_quarter`: Rataskaev well apron (`wash_tub`, buckets), knights' court (`weapon_rack`, `banner`, service barrels), Karja gate service yard (`charcoal_pile` or armourer scrap only where historically plausible), and swordsmith lane static tools; avoid turning ordinary houses into monumental knight architecture | verify: captures distinguish well court, knights' court, and Karja gate service yards; south-quarter tests and patrol reachability stay green
- [ ] P4-028g | deps: P2-025,P4-025 | deliverable: Toompea and Archbishop's Garden precinct-life dressing: stable/official props on `toompea_quarter` (`stable_cart`, banners, cathedral terrace clutter) and orchard/kitchen garden dressing on `archbishops_garden` (`herb_drying_rack`, orchard fences, gardener cart, hay_store) that read elevated canonical precinct rather than dense merchant ward | verify: captures distinguish castle banner terrace, cathedral approach, and garden orchard/kitchen plots; both prototype test suites stay green
- [ ] P4-028h | deps: P2-025 | deliverable: Viru Gate foreland travel-apron dressing on `viru_gate_foreland`: roadside inn/traveler clutter (`hay_stack`, carts, barrels, scrub margins) that signals the walled-town threshold without duplicating Kalamaja fishing gear or merchant harbour cargo | verify: foreland map tests pass; day/night capture reads as gate approach/inn foreland, not harbour or fishing shore; map stays inactive until a separate activation gate
- [ ] P4-008 | deps: P4-002,P4-004,P4-006,P4-007,P4-021 | deliverable: St. George's Night climax at Viru Gate reusing all earlier forged objects, ledger standing, and character states; Open, Seal, and Break resolve to a recorded act-boundary state instead of a game ending | verify: all three act-boundary families are reachable through visible prior decisions and each writes a distinct validated act-transition record
- [ ] P4-009 | deps: P4-008 | deliverable: Act 1 aftermath states for Mart, Aita, Kaja, Henning, Jürgen, Ellen, forge, and each active district recorded in the act-transition save | verify: automated matrix finds no impossible combination, missing core character, or universal morality score
- [ ] P4-010 | deps: P4-009,P4-017,P4-020 | deliverable: Act 1 content-budget report | verify: act remains within three districts, seven core characters plus named faction casts, eight substantial quests plus two three-quest faction lines, five cycles, one climax, and approved dialogue/audio budgets
- [ ] P4-023f | deps: P0-072,P1-036,P4-023 | deliverable: finish North/Merchant district historical acceptance after the environment pass: add localized open drainage/gate runoff, terrain-height fall toward the north/east harbour lowland, and a historically reviewed 1343 Coastal Gate exceptional model that omits later barbican/Fat Margaret forms; preserve stable routes, IDs and inactive status | verify: P1-036 north drainage/relief and landmark checks pass; day/night close and gameplay captures receive historical/art sign-off; transition, patrol, collision, chunk and activation guards remain green
- [ ] P4-023g | deps: P2-024,P4-023 | deliverable: replace the Merchant District's static cattle/sheep environment models with lightweight ambient animal actors only where route and performance budgets permit, retaining static pen dressing as fallback | verify: cattle/sheep idle within authored pens, cannot block Pikk/Lai or transitions, respect day/night population rules, and pass navigation/performance soak tests
- [ ] P4-027 | deps: P0-072,P4-023,P4-023a,P4-024 | deliverable: historical review and exterior art pass for every Lower Town 1343 fortification-registry position: retain the four completed named towers, turn Sand/Viru/Hinke/Karja/Harju uncertain positions into dated construction mockups where evidence supports them, and keep post-1343 towers absent; preserve stable map IDs and sealed wall collision | verify: historian/maintainer signs `docs/reports/reval_fortifications_1343.md`; registry/map tests agree on completed/construction/excluded positions; day/night close and harbour-facing captures show no later tower silhouette or unplanned wall breach
- [ ] P4-027a | deps: P4-027,P1-026,P1-027 | deliverable: shared enterable-tower mini-dungeon contract and first completed-tower vertical slice with reciprocal exterior/interior transitions, multi-floor navigation, named boss with authored alternate resolution, loot/evidence, persistent state, retry, lighting and audio | verify: exterior tower door enters and returns safely; stairs/ladder/wall-walk routes are reachable; boss outcomes and loot persist across re-entry/save; collision, camera, transition and packaged-build tests pass
- [ ] P4-027b | deps: P4-027a | deliverable: dedicated `Nunnatorn` interior map and boss package using its open-backed early-14th-century form rather than a later horseshoe rebuild | verify: historical/art review signs the floor plan; reciprocal door, all floors, boss outcomes, persistence and retry tests pass
- [ ] P4-027c | deps: P4-027a | deliverable: dedicated `Kuldjala` interior map and boss package using the two-level circa-1310 horseshoe form and wall-walk relationship | verify: historical/art review signs the floor plan; reciprocal door, all floors, boss outcomes, persistence and retry tests pass
- [ ] P4-027d | deps: P4-027a | deliverable: dedicated `Rentenitorn` interior map and boss package using a conservative pre-mid-fourteenth-century form | verify: unknown fabric stays reversible/labelled; reciprocal door, all floors, boss outcomes, persistence and retry tests pass
- [ ] P4-027e | deps: P4-027a,P4-023f | deliverable: dedicated Great Coastal Gate tower interior map and boss package for the probable 1311-1340 first tower, excluding later barbicans and Fat Margaret | verify: archaeology review signs the gate phase; reciprocal door, gate route, boss outcomes, persistence and retry tests pass
- [ ] P4-027f | deps: P4-027b,P4-027c,P4-027d,P4-027e | deliverable: completed-tower portfolio acceptance and activation plan covering map audit, difficulty/loot progression, unique boss identity, save migration, performance and accessibility | verify: every completed 1343 Lower Town registry tower has exactly one dedicated interior, no construction/excluded position is enterable as a completed dungeon, full traversal/branch/soak suites pass, and signed captures cover every exterior and interior
- [ ] P4-011 | deps: P4-010 | deliverable: automated Act 1 branch traversal and save compatibility suite | verify: every intended act-boundary state is reachable and every published save fixture loads or migrates
- [ ] P4-012 | deps: P4-011 | deliverable: external Act 1 playtest and critical/high issue closure | verify: report contains completion, comprehension, pacing, combat, choice, and continuity results with no unresolved critical/high issue
- [ ] P4-013 | deps: P4-012 | deliverable: Act 1 release candidate playable as a standalone chapter | verify: clean-clone CI, supported-platform smoke tests, license report, accessibility checklist, and act acceptance matrix all pass

## P5 - Act 2: The Fire of Rebellion

Scope per ADR 0008: the uprising and the siege of Reval, April-May 1343. Attested milestones occur on schedule on every path; the player steers local cost. New locations activate only from existing inactive prototypes through the standard parity and activation gates.

- [ ] P5-001 | deps: P4-013 | deliverable: approved Act 2 design: uprising-night opening derived from the act-boundary state, three-phase siege structure (investment, sortie/supply, assault), mission list, faction war-state table, and `docs/CANON.md` timeline entries for Kanavere Bog, Sõjamäe, and Paide with confidence labels | verify: every mission maps to an existing system or a named task in this file and every named event carries a canon confidence label
- [ ] P5-002 | deps: P5-001 | deliverable: world travel layer: map screen connecting Reval districts to activated world locations with authored travel events and act-gated availability | verify: reachable-location tests match act state and there is no seamless overworld or free roam
- [ ] P5-003 | deps: P5-002 | deliverable: world activation wave 1: Harju village, rebel kings' camp, and sacred grove from existing prototypes through the parity and activation gates (approval artifact: ADR 0008) | verify: activation guard, transition verifier, and traversal/collision tests pass and day/night captures pass parity review
- [ ] P5-004 | deps: P5-001,P1-026 | deliverable: night mission framework: sabotage, theft, escort, and defense templates as content-defined packages with combat and non-combat routes | verify: one content-only mission per template completes both routes and writes ledger events
- [ ] P5-005 | deps: P4-016,P5-004 | deliverable: forged-object recall: objects with Act 1 forged records resurface in Act 2 encounters with behavior matching their records | verify: automated matrix shows each recallable object class alters at least one encounter and a sabotaged versus honest record produces observably different outcomes
- [ ] P5-006 | deps: P5-003,P5-004 | deliverable: playable siege arc: investment, sortie/supply, and assault phases each offering rebel-aligned and ruler-aligned missions | verify: both allegiance paths complete every phase and attested milestones occur on schedule on all paths
- [ ] P5-007 | deps: P5-006 | deliverable: authored Kanavere Bog and Sõjamäe event missions with attested outcomes and steerable local cost | verify: battle outcomes match `docs/CANON.md` while named survivor and casualty states differ across branches
- [ ] P5-008 | deps: none | deliverable: two additional enemy archetypes (Order knight, crossbowman) and scripted allied NPC support inside missions | verify: archetypes reuse the shared enemy state machine and allies never require party-control UI
- [ ] P5-009 | deps: P5-005,P5-007 | deliverable: Act 2 finale: the fate of the Four Kings at Paide with a branch-dependent player role and a recorded Act 3 opening state | verify: the attested killing occurs in every branch and the player's knowledge and warning states produce distinct validated act-transition records
- [ ] P5-010 | deps: P5-008,P5-009 | deliverable: Act 2 gate: branch traversal suite, save fixtures, external playtest, and content-budget report | verify: every intended branch is reachable, all fixtures load or migrate, critical/high playtest issues are closed, and budgets are recorded

## P6 - Act 3: The Iron Harvest and full release

- [ ] P6-001 | deps: P5-010 | deliverable: approved Act 3 design: occupation state model, forced-forge arc, location list (Padise phases, Paide, Pöide/Saaremaa, Maasilinna concept metadata), ending families for Kalev, the forge, the people, and the land, and `docs/CANON.md` entries through the attested 1346 sale of the Duchy of Estonia | verify: design reuses existing systems or names tasks in this file and every named event carries a canon confidence label
- [ ] P6-002 | deps: P6-001 | deliverable: world activation wave 2: Padise monastery (both phases) and the Act 3 castle and event locations from existing prototypes through the parity and activation gates (approval artifact: ADR 0008) | verify: activation guard, transition verifier, and traversal/collision tests pass and day/night captures pass parity review
- [ ] P6-003 | deps: P6-001 | deliverable: forced-forge arc: coerced commissions with inspection risk and quiet-sabotage options built on the P1-020 modification system | verify: a content-only coerced commission supports compliant, flawed, and secret-feature outcomes with distinct discovered and undiscovered consequences
- [ ] P6-004 | deps: P5-005,P6-002,P6-003 | deliverable: Saaremaa campaign: authored Pöide siege event where player-forged siege materiel and prior records shape the encounter | verify: at least three distinct Pöide outcomes are reachable from differing Act 1-2 states
- [ ] P6-005 | deps: P6-004 | deliverable: final choice and ending families with epilogues for the seven core characters, the active factions, the forge, and the district, closing at the 1346 sale of Estonia | verify: automated matrix finds no impossible combination, no missing core character, and no universal morality score
- [ ] P6-006 | deps: P6-005 | deliverable: full-campaign branch traversal and cross-act save compatibility suite | verify: every intended ending is reachable and every published save fixture loads or migrates
- [ ] P6-007 | deps: P6-006 | deliverable: external full-campaign playtest and critical/high issue closure | verify: report contains completion, comprehension, pacing, combat, choice, and continuity results with no unresolved critical/high issue
- [ ] P6-008 | deps: P6-007 | deliverable: full-campaign release candidate | verify: clean-clone CI, supported-platform smoke tests, license report, accessibility checklist, and campaign acceptance matrix all pass

## Completed (retained for plan verification)

- [x] P0-043 | deps: none | deliverable: production map-definition contract adding canonical location, scope and activation, P0-040 palette, transitions and spawns, walkable and excluded areas, patrols, interaction anchors, camera bounds, fade volumes, source references, and deterministic fingerprints to the P0-042 schema | allowed files: `TODO.md`, `docs/MAP_AUTHORING.md`, `docs/MAP_CONVERSION_PLAN.md`, `scripts/map/map_definition.gd`, `scripts/map/map_types.gd`, `scripts/map/map_assembler.gd`, `scripts/map/smithy_courtyard_definition.gd`, `tests/godot/test_programmatic_map.gd`, `tests/godot/test_map_definition_contract.gd` | constraints: typed GDScript factories only, no second JSON geometry framework, no map conversion, no runtime-art change, no activation change | verify: `godot --headless --path . --script tools/run_godot_tests.gd` passes contract fixtures for every field, rejects out-of-bounds or duplicate anchors and transitions, rejects `active=true` for prototype or archive scope, and preserves the P0-042 fingerprint across two builds
- [x] P0-044 | deps: none | deliverable: map catalog and activation guard covering every retained or convertible map and rejecting archived or prototype scenes from Start flow, active destinations, active content locations, and release traversal | allowed files: `TODO.md`, `.github/workflows/ci.yml`, `scripts/map/map_catalog.gd`, `tools/verify_map_activation.py`, `tests/python/test_verify_map_activation.py`, `tests/godot/test_map_catalog.gd` | constraints: catalog metadata only, no map geometry, no scene activation, no edits to `content/transitions/active_destinations.json` or `scenes/intro/start_label.gd` | verify: `python3 tools/verify_map_activation.py`, `python3 -m unittest tests.python.test_verify_map_activation -v`, and `godot --headless --path . --script tools/run_godot_tests.gd` pass; one seeded active prototype and one seeded archived destination each fail the guard
- [x] P0-045 | deps: none | deliverable: runtime-import exclusion for archived event, world, castle, sacred-grove, and Toompea scene folders while preserving their markdown and image references | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `scenes/events/.gdignore`, `scenes/world/.gdignore`, `scenes/reval_toompea/.gdignore`, `tools/verify_map_activation.py`, `tests/python/test_verify_map_activation.py` | constraints: do not convert, delete, activate, or rewrite archived `.tscn`, markdown, or images; no gameplay or art changes | verify: `python3 tools/verify_map_conversion_plan.py` and `python3 tools/verify_map_activation.py` pass, Godot import reports no archived folder resource, and source-reference resolution still covers every archived scene row
- [x] P0-046 | deps: none | deliverable: release-scope reconciliation after the `.rrmap` harbor route: keep `reval_harbor` available only as an explicit developer destination, remove the obsolete `harbor_warehouse` destination and Lower Town route, and archive the static world-map, empty intro wrapper, obsolete root wrapper, and legacy diamond comparison shell while retaining their documentation and visual evidence | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `content/transitions/active_destinations.json`, `scenes/harbor/.gdignore`, `scenes/map/.gdignore`, `scripts/map/map_catalog.gd`, `tools/verify_map_activation.py`, `tests/python/test_verify_map_activation.py` | constraints: keep `game.tscn`, `scenes/intro/intro.tscn`, and `scenes/comparison_room/diamond_isometric_8_direction.tscn` as inactive evidence; keep screenshots and reports; `reval_harbor` must remain `release=false` and unreachable from Start/release traversal; do not change main-menu Start behavior or activate another district | verify: `python3 tools/verify_map_activation.py`, transition verification, and main-scene headless smoke pass; release destinations remain exactly `forge` and `reval_east`, the harbor is developer-only, and archived wrappers plus the warehouse are absent from release traversal
- [x] P0-055 | deps: none | deliverable: reconcile `docs/MAP_CONVERSION_PLAN.md` and `docs/reports/scene_inventory.md` with the current `.tscn` set (character rigs, interaction scenes, benchmarks, and other support scenes added after P0-018) | verify: `python3 tools/verify_map_conversion_plan.py` passes with zero missing or unknown scene rows
- [x] P0-057 | deps: none | deliverable: map-plan and scene-inventory reconciliation for the four UI scenes added after P0-055 (`scenes/elements/gameplay_help_hud.tscn`, `scenes/elements/minimap_hud.tscn`, `scenes/ui/forge_commission_overlay.tscn`, `scenes/ui/journal_overlay.tscn`) | verify: `python3 tools/verify_map_conversion_plan.py` passes with zero missing or unknown scene rows
- [x] P2-018 | deps: none | deliverable: declarative conversion of Kalev's smithy interior preserving the prologue work triangle, interaction anchors, foreground treatment, phase states, and stable courtyard transition | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `scripts/map/definitions/lower_town/kalev_smithy_definition.gd`, `scenes/reval_east/forge/forge.tscn`, `scenes/reval_east/forge/forge.gd`, `content/locations/loc.kalev_smithy.json`, `tests/godot/test_kalev_smithy_map.gd` | constraints: P0-040-approved modules only, do not edit the active transition manifest, do not activate another map, do not preserve legacy isometric coordinates as production scale | verify: `godot --headless --path . --script tools/run_godot_tests.gd` and a headless forge load pass; the definition covers 40 x 24 cells, all required anchors and footprints validate, and a recorded route reaches `door_courtyard`, `anvil`, `ledger`, and `bed_alcove` without collision or navigation defects
- [x] P2-019 | deps: none | deliverable: declarative conversion of the bounded Lower Town exterior containing smithy courtyard, brewery, cistern, watch checkpoint, required evidence props, patrol routes, and day/night phase states | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `scripts/map/definitions/lower_town/lower_town_slice_definition.gd`, `scenes/reval_east/reval_east.tscn`, `scenes/reval_east/reval_east.gd`, `content/locations/loc.lower_town_slice.json`, `tests/godot/test_lower_town_slice_map.gd` | constraints: one connected 64 x 36-cell district only, P0-040-approved modules only, no center, north, harbor, Toompea, world, castle, grove, or event activation, do not edit the active transition manifest | verify: `godot --headless --path . --script tools/run_godot_tests.gd` and a headless Lower Town load pass; deterministic traversal reaches `street_start`, `smithy_door`, `brewery_door`, `checkpoint_west`, and `checkpoint_east`, patrols stay within nav zones, and every declared hard exclusion blocks movement
- [x] P2-020 | deps: none | deliverable: atomic Start-flow, location-content, transition-manifest, spawn-ID, music-route, and scene-path cutover from legacy district geometry to the converted smithy and Lower Town slice | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `content/transitions/active_destinations.json`, `content/locations/loc.kalev_smithy.json`, `content/locations/loc.lower_town_slice.json`, `scenes/intro/start_label.gd`, `scripts/global/music_director.gd`, `tests/godot/test_transition_manifest.gd`, `tools/verify_transitions.gd`, `tools/test_music_director.gd` | constraints: active destinations remain exactly the approved slice set, no prototype or archive destination, remove temporary legacy aliases in the same task after traversal passes | verify: `godot --headless -s tools/verify_transitions.gd`, `godot --headless --script tools/test_music_director.gd`, `python3 tools/verify_map_activation.py`, and Start-flow smoke pass; every active destination and spawn loads and no retired district-edge ID remains
- [x] P4-014 | deps: none | deliverable: inactive unified Central District prototype containing the historically grounded Town Hall market square and St. Olaf's Guild Hall interior without expanding the playable portfolio | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `docs/HISTORICAL_AUDIT.md`, `content/maps/market_civic_quarter.rrmap`, `scripts/map/definitions/prototypes/market_civic_quarter_definition.gd`, `scripts/map/definitions/prototypes/st_olafs_guild_hall_definition.gd`, `scripts/map/view3d/map_view_mesh_builder_buildings.gd`, `scenes/reval_center/reval_center.tscn`, `scenes/reval_center/market_civic_quarter/olaf_guild_hall.tscn`, `tests/godot/test_market_prototype_maps.gd` | constraints: `active=false` developer prototypes only, no edits to Start flow or release traversal, no new external art assets; gameplay activation requires the existing P4-019 gate | verify: map blueprint validation and the Godot suite validate 80 x 48 bounds, unique historical silhouettes, anchors, footprints, transitions, and deterministic navigation; `python3 tools/verify_map_activation.py` proves the unified prototype remains outside release traversal
- [x] P4-015 | deps: none | deliverable: inactive declarative north-quarter prototype preserving Pikk Street, St. Olaf's landmark silhouette, guild frontage, workshop anchors, and bounded street navigation | allowed files: `TODO.md`, `docs/MAP_CONVERSION_PLAN.md`, `scripts/map/definitions/prototypes/north_quarter_definition.gd`, `scenes/reval_north/reval_north.tscn`, `tests/godot/test_north_quarter_prototype_map.gd` | constraints: `active=false` developer prototype only, retire rather than connect legacy district-edge spawns, no edits to Start flow, active destinations, release traversal, or slice content; gameplay activation requires a separate approval artifact naming equivalent scope removal | verify: `godot --headless --path . --script tools/run_godot_tests.gd` validates the 64 x 36-cell definition, landmarks, footprints, nav spine and fingerprint, and `python3 tools/verify_map_activation.py` proves the prototype is unreachable from Start and absent from active destinations
